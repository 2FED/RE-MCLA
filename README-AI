# MCLA-R — AI Working Context and Execution Plan

## 1. Document purpose

This file is the authoritative working context and milestone plan for **MCLA-R**, an unofficial native recompilation port of the Xbox 360 version of **Midnight Club: Los Angeles Complete Edition**.

It is written for human contributors and AI agents. Update it whenever architecture, prerequisites, supported platforms, commands, milestone state, risks, or project conventions change materially.

Status markers used in this document:

- `[ ]` not started
- `[-]` in progress
- `[x]` complete and verified
- `[!]` blocked; the task must include a blocker note
- `[~]` intentionally deferred with an owner or destination milestone

A checkbox is not complete until its stated verification evidence exists. Code existing locally is not sufficient by itself.

### 1.1 Live task-ledger protocol

The milestone checklists in this file are the project's source of truth. They must reflect actual repository state, not intended or remembered progress.

- Update a task's marker in the same work session in which its state changes.
- Add the evidence path or command result to the milestone checkpoint log before marking a task `[x]`.
- Never mark a task complete based only on an unverified implementation claim.
- When a task is blocked, mark it `[!]` and record the blocker, owner, next diagnostic action, and the condition that will unblock it.
- When scope changes, update the task, its acceptance criterion, estimate, dependencies, and affected milestone gate together.
- After every **two completed milestone tasks**, stop before completing a third and perform a milestone checkpoint review.
- Also perform an immediate checkpoint after any S0/S1 defect, feasibility change, SDK/toolchain change, new external dependency, or estimate change greater than 25%.

Every checkpoint must record:

1. date and reviewer
2. tasks completed since the previous checkpoint and their evidence
3. current completion count and milestone health: `GREEN`, `YELLOW`, `RED`, or `BLOCKED`
4. newly discovered risks, removed risks, and changed assumptions
5. re-evaluated estimate and gate readiness
6. the next two tasks to execute
7. whether milestone scope or acceptance criteria need revision

Checkpoint entries belong in the milestone's `Checkpoint log` subsection. If no checkpoint has occurred, retain the explicit `No checkpoint yet` entry.

### 1.2 Per-task completion contract

Each task has an individual criterion in the **Task acceptance criteria register**. A task is complete only when:

- its unique criterion is satisfied
- the stated evidence exists
- the universal Definition of Done in section 6.3 is satisfied
- any dependency task is complete

If implementation reveals that a criterion is insufficient, revise it before marking the task complete. Do not weaken criteria merely to close a task.

## 2. Project summary

### 2.1 Objective

Create a maintainable native Windows port of the Xbox 360 Complete Edition by statically recompiling the original PowerPC executable and supplying the required Xbox 360 runtime behavior through ReXGlue and project-specific hooks, patches, and stubs.

Primary target:

- Windows 10/11 x86-64
- Direct3D 12 Xenos GPU backend
- Xbox-compatible and SDL-supported controllers
- original 30 FPS behavior until correctness is established
- fully offline single-player operation

Secondary targets, only after the primary target is stable:

- Linux x86-64 with Vulkan
- native macOS ARM64 after a Darwin/ARM64 and Vulkan-portability feasibility gate
- fixed and variable higher-frame-rate modes with correct delta-time behavior, including validated VSync-off/uncapped operation
- ultrawide and modern resolution support
- optional quality-of-life improvements

### 2.2 Non-goals for the initial port

- Connecting to Xbox Live or retired Rockstar services
- Shipping copyrighted game assets, XEX files, RPF archives, Bink videos, or generated proprietary game code
- Replacing the RAGE engine with a new engine
- Rewriting all RPF formats or converting all assets to native PC formats
- Supporting macOS ARM64, multiplayer, or modding APIs before the single-player Windows port is stable; macOS additionally requires an explicit ReXGlue Darwin/ARM64 and GPU-capability feasibility gate
- Enabling 60 FPS before stock 30 FPS gameplay is deterministic and validated

### 2.3 Recommended technical foundation

- **MCLA-R ReXGlue SDK v0.10.0.1 fork**, pinned to an exact tag and commit over upstream v0.9.0, is the primary runtime and code-generation base.
- **XenonRecomp** is a reference for PPC translation, function analysis, jump-table handling, and patch techniques, not the primary runtime.
- **Xenia Canary** is the behavioral oracle for baseline execution, import discovery, graphics comparison, and regression triage.

Do not silently track ReXGlue `main`. SDK upgrades are explicit changes with their own review, regression run, and documentation update.

## 3. Source baseline and verified facts

### 3.1 Source image

Current private source:

`C:\BDU\MCLA-Recomp\Midnight.Club.Los.Angeles.The.Complete.Edition.XBOX360\midmets4.iso`

Verified metadata:

| Property | Value |
|---|---|
| ISO size | `7,838,695,424` bytes |
| ISO SHA-256 | `AFCAAE68593246B1F83328681B690E254A13C37DD2E8DC34AB0DEB6C0F471FDB` |
| Disc descriptor | `LayerBreak=1913760` |
| XDVDFS partition base | `0x0FD90000` |
| Title ID | `545407F8` |
| Media ID | `5940C9DB` |
| Disc | `1/1` |
| Edition | Complete Edition |

### 3.2 Executable

| Property | Value |
|---|---|
| File | `default.xex` |
| Size | `9,252,864` bytes |
| SHA-256 | `C386F4001FA569E6AD4B982F441F67412F00B3F47C166134555CD4B59854A432` |
| Original executable name | `mc4_xenon_final.exe` |
| Image base | `0x82000000` |
| Entry point | `0x821322B8` |
| Raw XEX version | `0x00000008` |
| Encryption | enabled |
| Compression | XEX basic compression |
| Dynamic import libraries | `xam.xex`, `xboxkrnl.exe` |

Static library metadata includes `XAPILIB`, `XBOXKRNL`, `LIBCMT`, `XAUD`, `XMP`, `D3D9`, `XGRAPHC`, `XONLINE`, `XHV`, and `XFFB`.

The public Xenia Canary patch set for this edition declares module hash `1984A3354B78CE19`. That value must still be verified locally from a baseline Xenia log before being treated as evidence for this dump.

### 3.3 Game data layout

The disc contains 15 files plus `$SystemUpdate`. The game payload is primarily:

- four RPF archives totaling about 5.99 GB
- six Bink videos totaling about 561 MB
- `default.xex`
- NXE artwork and non-game update files

Initial runtime design should mount and serve the original files through the guest VFS. Do not unpack or rewrite RPF archives unless a demonstrated runtime blocker requires it.

### 3.4 Private-data rule

The following must never be committed or included in public release archives:

- ISO, DVD descriptor, extracted XEX, XEXP, RPF, BIK, save dumps, title updates, DLC packages
- generated C++ that directly reproduces proprietary game code, unless legal review explicitly approves its distribution
- Xenia logs containing local usernames or private filesystem paths without sanitization
- crash dumps or traces containing copyrighted memory pages

The repository must provide hash validation and local extraction/build instructions so users can supply their own legally obtained dump.

### 3.5 Consumer preparation and distribution model

The target public workflow is an asset-free local preparation launcher:

1. the user selects their own legally obtained supported disc dump;
2. the launcher validates the exact ISO/content identity before any extraction;
3. extraction is contained, source-read-only, and restricted to a project-owned staging root;
4. guest code generation and native compilation run locally using pinned tools;
5. success atomically publishes a fingerprinted prepared-game directory;
6. later launches reuse that directory until a repair or project/SDK update requires a new generation.

This is already the underlying developer architecture: the repository has exact dump validation, safe extraction, local non-force code generation, native compilation, launch, and integrity gates. What remains for M9 is the consumer launcher/UX, progress and actionable errors, resumable and atomic prepare/repair/update, a portable or otherwise reproducible toolchain, and clean-machine packaging tests.

The source dump must never be uploaded, modified, bundled, or deleted by prepare, repair, update, uninstall, or rollback. It need not be present on every ordinary launch once a prepared directory has been verified, but it remains user-owned and may be requested for repair/rebuild. Extracted assets, XEX content, and generated proprietary guest code remain local and untracked. A prebuilt host/runtime plus locally generated guest-module split is desirable if ReXGlue supports it, but local whole-target compilation is an acceptable first implementation and does not change the legal boundary.

## 4. Current environment

Known installed components:

- Visual Studio Build Tools 2022 `17.14`
- MSVC `14.44`
- Windows 11 SDK
- LLVM/clang-cl/lld `20.1.8` installed at `C:\Program Files\LLVM\bin`
- CMake `3.31.6` under the Visual Studio installation
- Ninja `1.12.1` under the Visual Studio installation
- Git `2.46.2`
- Rust/Cargo installed
- ast-grep `0.45.0`
- ReXGlue SDK project fork `0.10.0.1` pinned recursively at the immutable commit recorded in `docs/rexglue-sdk.md` (upstream v0.9.0 base)
- XboxDev `extract-xiso` release `build-202505152050`
- Xenia Canary `canary_experimental@7d8db5a2c`
- Eclipse Temurin JDK `21.0.12+8`
- Ghidra `12.0.4` with XEXLoaderWV `13.0.0`
- RenderDoc `1.45.0`

Resolved M2 feasibility questions:

- target-XEX non-force codegen is successful and deterministic; M3-005 preserves the 64-file/128,038,099-byte pre-instrumentation baseline, while the current M3-013 crash-instrumented corpus is 67 files / 133,908,410 bytes with 65 C++ sources and 30,025 mappings
- stock Xenia visual/save/controller baseline coverage is complete
- eight M2 analysis entries plus twelve runtime-discovered M3 callable entries are required; switch-table, exception, invalid-data, and setjmp/longjmp overrides remain zero
- all load-time variables and the 26-import pre-main function set have SDK registrations

Post-M4 unknowns entering the first-race slice:

- M4-001 proves guest-backed successful D3D12 presents and a nontrivial private
  readback; M4-002 additionally proves autonomous title reach and the scoped
  host-RTV frontend render path, but not general render correctness
- the reached single-local-user identity contract is verified, but profile
  writes/persistence and zero-hit optional XAM routes remain unverified
- the post-OOBE saved route through free roam, pause, and Settings/Options is
  repeatable, and M4-012 compares stable frontend regions at 1280x720 and
  2560x1440; first-run OOBE, races, detailed gameplay correctness, whole-frame
  parity, persistence, and event-level audio identity remain unverified
- runtime semantics of conditional/error-path imports not exercised on the stock pre-main route remain unverified

Host GitHub CLI authentication was user-verified on 2026-08-10:

- account: `2FED`
- active account: yes
- Git protocol: HTTPS
- scopes: `gist`, `read:org`, `repo`, `workflow`

Sandboxed agent shells may not always inherit the Windows keyring session. Treat the host result as authoritative, but rerun `gh auth status` immediately before the first remote mutation.

Before filing a confirmed tool defect upstream, search both open and closed
issues and pull requests using the failure signature, affected API, subsystem,
and likely user-facing wording. Inspect every plausible match rather than
relying only on titles. Publish only sanitized reproduction details, never
private game data or local identity/path details, and do not create an issue
when an existing report covers the same root cause.

CMake and Ninja are not currently on the normal shell `PATH`; scripts must locate them deterministically or initialize the Visual Studio environment.

## 5. Planned repository layout

```text
MCLA-Recomp/
├── README-AI
├── README.md
├── AGENTS.md                  # optional project-specific stricter rules only
├── LICENSE
├── VERSION                    # four-part version, for example 0.1.0.0
├── CMakeLists.txt
├── CMakePresets.json
├── mcla_manifest.toml         # ReXGlue v0.9.0 manifest-first codegen configuration
├── config/                    # reviewed manual ReXGlue analysis metadata
├── cmake/
├── docs/
│   ├── architecture.md
│   ├── build.md
│   ├── compatibility.md
│   ├── reverse-engineering.md
│   ├── testing.md
│   ├── known-issues.md
│   └── evidence/              # sanitized milestone evidence only
├── generated/                 # guest-generated output ignored; rexglue.cmake bootstrap tracked
├── private/                   # XEX/assets/logs; always ignored
├── rules/                     # ast-grep project rules
├── rule-tests/                # ast-grep rule fixtures/tests
├── utils/                     # reusable ast-grep helper rules when needed
├── scripts/
│   ├── bootstrap.ps1
│   ├── verify-source.ps1
│   ├── extract-game.ps1
│   ├── codegen.ps1
│   ├── build.ps1
│   ├── test.ps1
│   └── package.ps1
├── src/
│   ├── app/
│   ├── hooks/
│   ├── kernel/
│   ├── patches/
│   ├── platform/
│   └── diagnostics/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── smoke/
└── third_party/
    └── rexglue-sdk/           # pinned submodule or documented package
```

The exact layout may evolve during M0, but private and generated data separation is mandatory.

## 6. Cross-cutting engineering rules

### 6.1 Release workflow

Use this sequence without skipping steps:

1. Code
2. Review code
3. Run `ast-grep scan` when applicable
4. Validate ast-grep rules with `ast-grep test`
5. Run project tests
6. Fix all release-blocking findings
7. Bump the four-part version
8. Update all relevant documentation
9. Explicitly stage reviewed files
10. Commit
11. Push

No commit or push is permitted before review and required tests pass.

Project delivery cadence:

- Subagents may own bounded, explicitly scoped work or provide independent read-only research and review under the repository-local `AGENTS.md`. Because all agents share one worktree, edit ownership and GUI/runtime operations are serialized; the primary agent retains integration, acceptance testing, task status, commits, usage checks, milestone closure, and pushes.
- Continue autonomously until the active milestone is reviewed, closed, committed, and pushed; user input/authorization is genuinely required; or an observable weekly usage allowance reaches 20%. After each closed task, read the live ChatGPT Settings -> Usage value or the interactive Codex `/status` fallback before starting another task. If usage is not exposed to the agent, never invent a value—continue to milestone closure or a real blocker and rely on the user to signal an external quota threshold. The repository-local `AGENTS.md` is authoritative for this persistence rule.
- Close one task per commit. A task commit may be created only after that task's acceptance criterion, review, and relevant tests pass.
- Do not combine unrelated completed task IDs in one commit. If two tasks are inseparable, revise the ledger and dependency rationale before implementation.
- Recommended task commit subject: `task(Mx-nnn): concise outcome`.
- Task commits remain local during an open milestone unless an explicit backup/collaboration push is approved.
- After the final task needed by a milestone passes, perform the mandatory milestone review, update its checkpoint and gate decision, create a separate closure commit, then push all milestone commits.
- After a milestone closes and pushes with a `GO` decision, start the next milestone autonomously when no user action is required and the usage threshold permits. If closure or the next milestone requires a manual game route, visual judgment, authorization, source material, or another user-only action, stop and provide an exact checklist instead of guessing or bypassing the gate.
- Recommended closure commit subject: `milestone(Mx): close <milestone name>`.
- A milestone is not closed merely because its implementation tasks are committed; the closure commit and successful push are required.
- The repository bootstrap is the only ordering exception: Git must be initialized before the first task commit can exist. The first commit records M0-001; M0-002 is closed by a subsequent ledger/status commit after repository state is verified.

### 6.2 Versioning

Public versions use `major.minor.patch.hotfix`.

- Major: intentionally incompatible change
- Minor: new functionality or newly supported milestone capability
- Patch: compatible fix, maintenance, refactor, or documentation release
- Hotfix: urgent, narrow follow-up on the current patch line

Initial recommended line:

- `0.1.0.0`: reproducible project scaffold and feasibility tooling
- `0.2.0.0`: clean codegen/native link
- `0.3.0.0`: clean native build, reliable module boot, and bounded startup diagnostics
- `0.4.0.0`: stable frontend
- `0.5.0.0`: first playable race
- `0.6.0.0`: campaign-compatible alpha
- `0.9.0.0`: release candidate line
- `1.0.0.0`: validated public release

These are planning markers, not automatic version bumps.

### 6.3 Definition of Done for every task

A task may be checked only when all applicable conditions are met:

- implementation or document exists in its intended location
- relevant code has been reviewed
- warnings and failures are understood; none are silently ignored
- tests appropriate to the risk have passed
- evidence is recorded in a sanitized log, report, screenshot, test result, or reproducible command
- README-AI and other affected documentation are updated
- no proprietary or secret material is staged

### 6.4 Definition of Done for every milestone

A milestone closes only when:

- every mandatory task is `[x]`
- deferred tasks are explicitly marked `[~]` and assigned to a later milestone
- there are no open severity-0 or severity-1 defects for the milestone scope
- required tests pass from a clean build or clean documented environment
- artifacts and verification evidence are reproducible
- known limitations are documented
- the milestone review records a clear `GO` decision for the next milestone

### 6.5 Defect severity

- **S0:** data loss, security issue, copyrighted data leakage, destructive installer behavior
- **S1:** build failure, startup crash, progression blocker, persistent corruption, nondeterministic critical failure
- **S2:** major graphics/audio/gameplay defect with a workaround or limited scope
- **S3:** cosmetic defect, minor usability issue, non-blocking warning

### 6.6 Testing hierarchy

Run the smallest relevant test first, then broaden:

1. Unit tests for host helpers, patching, path mapping, endian conversions, and config parsing
2. ReXGlue PPC/codegen tests where SDK changes are involved
3. Static analysis and ast-grep rules
4. Headless or short startup smoke test
5. Boot-to-menu test
6. First-race deterministic route
7. Save/load and multi-session test
8. Campaign regression matrix
9. Clean-machine package test

### 6.7 ast-grep workflow

Use the non-deprecated `ast-grep` executable. The legacy `sg` alias may print a deprecation warning.

From the repository root:

```powershell
ast-grep scan
ast-grep test
```

During rule development, inspect matches before adding a rewrite. Rewrites require a focused diff review and rule tests before normal project tests. Once a rule becomes release-enforcing, add and review snapshots where stable diagnostic locations add value.

Current project rule:

- `no-unsafe-strcpy`: rejects unbounded `strcpy` calls in C++ host code.
- `no-unsafe-sprintf`: rejects unbounded `sprintf` and `vsprintf` calls.
- `no-unsafe-strcat`: rejects unbounded `strcat` calls.

These are the bootstrap safety rules. Add path-aware VFS boundaries, hook metadata rules, and other architectural checks after real C++ AST exists. Add reusable global utility rules under `utils/` only when duplication justifies them.

Project rules exclude `third_party/**`: vendored submodules are immutable upstream code and are reviewed against their own requirements. This exclusion must never be expanded to MCLA-R-owned paths such as `src/`, `tests/`, `scripts/`, or generated hook metadata.

## 7. Milestone dependency map

```text
M0 Repository foundation
 └─ M1 Reproducible toolchain and source ingestion
     └─ M2 Baseline and feasibility/codegen audit
         └─ M3 Clean native build and module boot
             └─ M4 Frontend/menu stability
                 └─ M5 First playable race
                     └─ M6 Core systems and repeatable gameplay
                         └─ M7 Full campaign/content compatibility
                             ├─ M8 Modern enhancements
                             └─ M9 Release hardening and distribution
```

M8 may begin experimentally after M6, but no M8 feature may destabilize or delay M7 correctness work.

## 8. Milestone M0 — Repository foundation and governance

**Goal:** establish a safe, documented, reproducible repository before proprietary extraction or code generation begins.

**Status:** `CLOSED — GO M1` on 2026-08-10.

**Estimate:** 0.5–2 working days.

### Prerequisites

- Workspace access to `C:\BDU\MCLA-Recomp`
- Global repository rules loaded
- Source ISO remains unchanged

### Tasks

- [x] **M0-001** Create the authoritative `README-AI` milestone plan.
- [x] **M0-002** Initialize Git with `main` as the default branch. Verified on 2026-08-10; root task commit is `83a4bd2` and `origin` points to `https://github.com/2FED/RE-MCLA.git`.
- [x] **M0-003** Add `.gitignore` entries for `private/`, `generated/`, build trees, SDK binaries, logs, dumps, caches, ISO/XEX/XEXP/RPF/BIK files, and package artifacts. Verified with tracked-source and prohibited-path fixtures on 2026-08-10.
- [x] **M0-004** Add `README.md` explaining scope, legal asset requirement, current status, and non-turnkey nature. Reviewed against the current project plan on 2026-08-10.
- [x] **M0-005** Select and add a license covering only original port code and scripts. MIT selected on 2026-08-10 with third-party/game-content boundaries documented in README.
- [x] **M0-006** Add `VERSION` with initial four-part version. Initialized to pre-alpha `0.0.1.0` on 2026-08-10, advanced to `0.1.0.0` at M1 toolchain closure, to `0.2.0.0` at M2 clean-codegen feasibility closure, to `0.3.0.0` at M3 clean native build/module-boot closure on 2026-08-11, to `0.4.0.0` at M4 frontend/menu-stability closure on 2026-08-14, to `0.5.0.0` at M5 first-playable-race closure on 2026-08-17, to `0.5.1.0` for the M6-005 transactional save/corruption/storage-full patch on 2026-08-18, to `0.6.0.0` for the M6 profile/settings feature line, to `0.6.1.0` for latest-active physical-controller routing during M6-014, to `0.6.1.1` for the named-suite, physical-capture focus, and ordered-resource-sample soak-harness hotfixes, to `0.6.1.2` for interactive completion, save-watcher host, and fail-closed stage-recovery fixes, to `0.6.2.0` for the expanded rendering-defect classification and evidence, to `0.6.3.0` for hash-bound mixed free-roam/race soak recovery without duplicate physical play, to `0.7.0.0` for model-agnostic racing-wheel input and bounded title force feedback, to `0.7.1.0` for the reviewed alpha compatibility matrix and synchronized known-issue inventory, to `0.7.1.1` for exact project-pinned soak SDK identity and the fresh current-artifact frontend checkpoint, to `0.7.2.0` for the reviewed variable-frame-rate and native macOS ARM64 future-work plan, to `0.7.2.1` for progressed-save soak-seed binding and fail-closed stale-attempt migration, and to `0.7.3.0` for the verified Photo Mode GPU-readback fix, stable virtual-controller routing, and host-display-aware soak recovery.
- [x] **M0-007** Create the planned directory scaffold without copying proprietary files. Source, test, rule, documentation-evidence, CMake, script, and third-party roots created on 2026-08-10; private/generated roots remain local-only and ignored.
- [x] **M0-008** Add `sgconfig.yml`, `rules/`, `rule-tests/`, and initial documented ast-grep workflow. Initial C++ smoke rule and fixtures added and verified with ast-grep 0.45.0 on 2026-08-10.
- [x] **M0-009** Define initial structural safety rules and fixtures. Bootstrap rules cover unbounded `strcpy`, `sprintf`/`vsprintf`, and `strcat`; path-aware VFS and hook-metadata rules remain required when real C++ AST lands. Verified with ast-grep 0.45.0 on 2026-08-10.
- [x] **M0-010** Create `docs/architecture.md`, `docs/build.md`, `docs/testing.md`, and `docs/known-issues.md` skeletons. Each document now records owner, purpose, current state, and update triggers.
- [x] **M0-011** Add a source-data policy describing files that must never enter Git or release archives. Policy covers Git/staging, releases, logs, traces, dumps, generated code, secrets, sanitization, and uninstall safety.
- [x] **M0-012** Verify GitHub CLI authentication before any remote operation. User-verified on the host on 2026-08-10 for account `2FED`; recheck immediately before the first remote mutation.
- [x] **M0-013** Decide repository visibility and remote destination; do not push until review and tests pass. Owner supplied `https://github.com/2FED/RE-MCLA.git`; GitHub CLI verified `2FED/RE-MCLA` is public, active, and currently has no default branch before the first push.

### Deliverables

- Git repository with safe ignore rules
- documented project scope and architecture skeleton
- four-part version file
- ast-grep scaffold
- no copyrighted game content in Git history

### Verification evidence

- `git status --ignored` demonstrates that test copies of prohibited extensions are ignored
- secret/proprietary file scan reports no tracked content
- `ast-grep scan` and `ast-grep test` execute successfully, even if the initial rule set is minimal
- a fresh clone can display project documentation without private files

### Exit criteria

- Repository safety review passes.
- No ISO/XEX/RPF/BIK content is tracked.
- README-AI and README accurately describe the project.
- GitHub remote work is possible but no release claim is made.

### Gate

`GO M1` only after private/generated content boundaries are enforced and verified.

### Checkpoint log

- 2026-08-10 — Initial document checkpoint. Completed: M0-001 and M0-012. Health: `GREEN`. Evidence: repository `README-AI`; user-provided `gh auth status` output. Remaining estimate: 0.5–2 working days. Gate readiness: not ready. Next tasks: M0-002, M0-003. Scope change: live-ledger protocol and per-task acceptance register added.
- 2026-08-10 — M0 checkpoint 2. Completed since prior checkpoint: M0-002 and M0-003. Health: `GREEN`. Evidence: commit `c51a579`, this M0-003 task commit, `main`/`origin` verification, and `git check-ignore` fixture audit. Risks: proprietary source was previously visible as untracked and is now covered by extension rules. Re-evaluated estimate: 0.5–2 working days. Gate readiness: not ready. Next tasks: M0-004, M0-005. Scope/criteria remain valid.
- 2026-08-10 — M0 checkpoint 3. Completed since prior checkpoint: M0-004 and M0-005. Health: `GREEN`. Evidence: commits `cee7de8` and this M0-005 task commit; README content checks; MIT text and third-party boundary review. Risks: public wording must continue to distinguish original project code from copyrighted game and dependency material. Re-evaluated estimate: 0.5–2 working days. Gate readiness: not ready. Next tasks: M0-006, M0-007. Scope/criteria remain valid.
- 2026-08-10 — M0 checkpoint 4. Completed since prior checkpoint: M0-006 and M0-007. Health: `GREEN`. Evidence: commits `4aa5b93` and this M0-007 task commit; four-part VERSION validation; repository-tree and prohibited-file audit. Risks: empty scaffold placeholders must be removed as real files land, and private/generated roots must remain ignored. Re-evaluated estimate: 0.5–1.5 working days. Gate readiness: not ready. Next tasks: M0-008, M0-009. Scope/criteria remain valid.
- 2026-08-10 — M0 checkpoint 5. Completed since prior checkpoint: M0-008 and M0-009. Health: `GREEN`. Evidence: commit `34b4288`, this M0-009 task commit, ast-grep 0.45.0 scan, and positive/negative rule fixtures. Risks: bootstrap syntax rules do not yet enforce VFS or hook architecture because no real C++ AST exists. Re-evaluated estimate: 0.5–1 working day. Gate readiness: not ready. Next tasks: M0-010, M0-011. Scope revised: architectural rules are added when the C++ scaffold lands; bootstrap safety rules satisfy M0.
- 2026-08-10 — M0 checkpoint 6. Completed since prior checkpoint: M0-010 and M0-011. Health: `GREEN`. Evidence: commit `c39d9a1`, this M0-011 task commit, documentation-contract audit, and source-data-policy coverage review. Risks: future diagnostic formats require renewed privacy/proprietary-data review before publication. Re-evaluated estimate: less than 0.5 working day. Gate readiness: pending M0-013 remote verification and final milestone review. Next tasks: M0-013, M0 closure review. Scope/criteria remain valid.
- 2026-08-10 — M0 closure review. Completed: all 13 M0 tasks. Health: `GREEN`; decision: `GO M1`. Evidence: all task commits through `84fcfe2`; ast-grep 0.45.0 with 3/3 rule tests and clean scan; 147/147 task-criteria coverage; valid four-part VERSION; documentation-contract audit; clean `git fsck`; zero prohibited tracked paths; private disc directory confirmed ignored; public `2FED/RE-MCLA` remote verified. Residual risks: architectural AST rules await real C++ code, and remote authentication must be rechecked immediately before mutation. Gate readiness: ready. Next tasks: M1-001 and M1-002; M1-003 was completed early. Scope/criteria remain valid.

## 9. Milestone M1 — Reproducible toolchain and source ingestion

**Goal:** create a one-command, hash-validated local environment that extracts or locates the exact supported game dump without modifying the source ISO.

**Status:** `CLOSED — GO M2` on 2026-08-11.

**Estimate:** 1–4 working days.

### Prerequisites

- M0 closed
- At least 30–50 GB free disk space confirmed
- Visual Studio Build Tools and Windows SDK available

### Tasks

- [x] **M1-001** Install or enable Clang/clang-cl 20+ for use with Visual Studio Build Tools. Pinned official WinGet LLVM 20.1.8 installed machine-wide; `clang-cl`, `lld-link`, `llvm-ar`, and a C++23 syntax-only compilation verified on 2026-08-10.
- [x] **M1-002** Make CMake 3.25+ and Ninja discoverable through deterministic scripts rather than relying on an interactive developer prompt. `scripts/Resolve-Toolchain.ps1` now uses `vswhere.exe`, validates CMake 3.25+, Ninja 1.10+, and clang-cl 20+, and can export their directories into the current dot-sourced PowerShell session.
- [x] **M1-003** Install and verify `ast-grep`. Host installation `ast-grep 0.45.0` verified on 2026-08-10 at the WinGet package path; use `ast-grep`, not the deprecated `sg` alias.
- [x] **M1-004** Obtain ReXGlue v0.9.0 recursively and pin its exact commit/tag. `third_party/rexglue-sdk` is pinned at `3eb9b511b4140d2769e27be63eae57d41bfa2afa`; all 25 nested dependency nodes are initialized, clean, and recorded in `docs/rexglue-sdk.md`.
- [x] **M1-005** Build or install ReXGlue using the supported Windows preset. The unmodified `win-amd64` preset configured with Clang 20.1.8 and its CI-equivalent `install` target completed all 2,258 actions; the installed CLI reports `0.9.0`. Evidence is recorded in `docs/evidence/M1-005-rexglue-build.md`.
- [x] **M1-006** Record ReXGlue build commands, dependency versions, and local install discovery in `docs/build.md`. The guide now covers recursive source initialization, exact host/dependency versions, fresh-shell VS setup, configure/install/rebuild commands, expected outputs, and deterministic `PATH`/`CMAKE_PREFIX_PATH` discovery.
- [x] **M1-007** Install a trusted XDVDFS extraction tool or implement a reviewed read-only extractor. XboxDev `extract-xiso` release `build-202505152050` at commit `b72e5b60d598ec6df80534cda19cdcd4361aa18c` is installed under ignored `private/tools/`; pinned source review confirmed read-only list input flags, and a before/after ISO hash test passed. Full hashes and inventory are in `docs/evidence/M1-007-xdvdfs-extractor.md`.
- [x] **M1-008** Implement `scripts/verify-source.ps1` to validate ISO size, SHA-256, Title ID, Media ID, `default.xex` size, and XEX SHA-256. The read-only verifier parses XDVDFS and XEX2 directly from the ISO, passed the supported dump, and rejected a correct-size sparse wrong-hash fixture. Evidence is in `docs/evidence/M1-008-source-verification.md`.
- [x] **M1-009** Implement `scripts/extract-game.ps1` with explicit source and destination paths, path containment checks, and no overwrite without confirmation. The wrapper verifies source/tool hashes, rejects paths outside `private/`, disallows reparse traversal and all existing destinations, extracts through contained staging, and passed positive `-WhatIf` plus outside/existing-path rejection tests. Evidence is in `docs/evidence/M1-009-safe-extraction-wrapper.md`.
- [x] **M1-010** Extract game data into `private/game/` and verify the expected 15-file payload. The safe wrapper produced exactly 15 files and 6,569,586,392 bytes; all relative paths, sizes, and SHA-256 values are captured in ignored `private/game-manifest.json` and sanitized evidence at `docs/evidence/M1-010-extracted-payload.md`. Post-extraction source verification passed unchanged.
- [x] **M1-011** Verify RPF and BIK sizes against a generated local manifest. `scripts/verify-game-manifest.ps1` validates schema/source identity, contained unique paths, exact 15-file inventory, every size, total bytes, and mandatory 4 RPF/6 BIK counts; its full `-VerifyHashes` run passed all 15 SHA-256 entries.
- [x] **M1-012** Create `private/README.local.md` automatically or manually to document the private directory without tracking its contents. The local guide records layout, verification/regeneration commands, no-distribution and source-preservation rules, staging cleanup, current pins, and update triggers; Git ignore/index audits pass.
- [x] **M1-013** Install Xenia Canary in a private tools location and pin the tested build. Official tag `7d8db5a` / commit `7d8db5a2cd06373aefba29dc019db74e78d6fb7d` is installed under ignored `private/tools/`; GitHub/local asset hashes agree, and a hidden null-backend smoke logged `canary_experimental@7d8db5a2c` before controlled shutdown. Evidence is in `docs/evidence/M1-013-xenia-canary.md`.
- [x] **M1-014** Install Ghidra/XEX analysis support and graphics-debug tooling, or document why a selected alternative is used. Compatible Temurin JDK 21.0.12+8, Ghidra 12.0.4, XEXLoaderWV 13.0.0, and RenderDoc 1.45.0 are pinned; a headless retail `default.xex` import succeeded with 20,071 functions and 257 import references. Evidence is in `docs/evidence/M1-014-re-tools.md`.
- [x] **M1-015** Add `scripts/bootstrap.ps1` to validate all prerequisites without performing destructive installation. The aggregate read-only gate reports 12 independent checks, passed 12/12 in a fresh shell, and continued through all checks before returning exit code 1 for a deliberately missing ast-grep path. Evidence is in `docs/evidence/M1-015-bootstrap.md`.

### Deliverables

- pinned ReXGlue SDK
- deterministic toolchain discovery
- verified private game extraction
- reproducible environment/bootstrap report

### Verification evidence

- `bootstrap.ps1` passes in a new PowerShell session
- `verify-source.ps1` rejects an intentionally wrong hash fixture
- extraction output manifest exactly matches expected XEX and asset inventory
- source ISO hash remains unchanged after extraction
- ReXGlue CLI prints its pinned version and can initialize a sample project

### Exit criteria

- The supported dump is unambiguously identified before codegen.
- No manual copying step is required but undocumented.
- ReXGlue and the compiler can build a minimal sample.
- Tool versions and paths are recorded.

### Gate

`GO M2` only if source verification, extraction, and a minimal ReXGlue build are reproducible.

### Checkpoint log

- 2026-08-10 — M1 checkpoint 1. Completed: M1-001 and M1-003. Health: `GREEN`. Evidence: official LLVM 20.1.8 WinGet install; clang-cl/lld-link/llvm-ar version checks; C++23 syntax-only compile; ast-grep 0.45.0 version plus 3/3 project rule tests. Risks: standalone LLVM must be selected deterministically alongside the Visual Studio SDK environment; PATH cannot be assumed in already-running shells. Re-evaluated estimate: 1–4 working days. Gate readiness: not ready. Next tasks: M1-002 and M1-004. Scope/criteria remain valid.
- 2026-08-10 — M1 checkpoint 2. Completed since prior checkpoint: M1-002 and M1-004. Health: `GREEN`. Evidence: deterministic `vswhere.exe`-based resolver passed in fresh read-only and dot-sourced PowerShell sessions; ReXGlue `v0.9.0` resolved independently through the GitHub API and local refs to `3eb9b511b4140d2769e27be63eae57d41bfa2afa`; all 25 nested dependency nodes are initialized with clean status and an immutable SHA manifest. Risks: ReXGlue's documented Windows preset and optional Vulkan/FFmpeg dependencies have not yet been built on this host. Re-evaluated estimate: 1–3 working days. Gate readiness: not ready. Next tasks: M1-005 and M1-006. Scope/criteria remain valid.
- 2026-08-10 — M1 checkpoint 3. Completed since prior checkpoint: M1-005 and M1-006. Health: `GREEN`. Evidence: clean upstream `win-amd64` configure; 2,258-action install build across Debug, Release, and RelWithDebInfo; ReXGlue CLI 0.9.0/version/help checks; installed CMake package; and a fresh-session dry run of the documented setup/discovery flow. Risks: ReXGlue unit tests remain intentionally disabled by the stock preset, and the supported game source has not yet passed a scripted verification/extraction gate. Re-evaluated estimate: 1–3 working days. Gate readiness: not ready. Next tasks: M1-007 and M1-008. Scope/criteria remain valid.
- 2026-08-10 — M1 checkpoint 4. Completed since prior checkpoint: M1-007 and M1-008. Health: `GREEN`. Evidence: pinned XboxDev extractor source/license/hash review; successful read-only 15-file XDVDFS listing with unchanged ISO hash/size/timestamp; direct ISO/XDVDFS/XEX2 verifier success; and correct-size wrong-hash fixture rejection. Risks: extraction still needs containment and overwrite controls, and no full private payload manifest exists yet. Re-evaluated estimate: 1–2 working days. Gate readiness: not ready. Next tasks: M1-009 and M1-010. Scope/criteria remain valid.
- 2026-08-10 — M1 checkpoint 5. Completed since prior checkpoint: M1-009 and M1-010. Health: `GREEN`. Evidence: containment/reparse/no-overwrite preflight and rejection tests; corrected options-before-ISO regression test; successful staged extraction; exact 15-file/6,569,586,392-byte local manifest with per-file SHA-256; and unchanged post-extraction ISO/XEX identifiers. Risks: local manifest regeneration/validation and private-directory operator guidance are not yet automated; Xenia and RE/debug tooling remain unpinned. Re-evaluated estimate: 1–2 working days. Gate readiness: not ready. Next tasks: M1-011 and M1-012. Scope/criteria remain valid.
- 2026-08-10 — M1 checkpoint 6. Completed since prior checkpoint: M1-011 and M1-012. Health: `GREEN`. Evidence: reusable contained manifest validator passed all 15 hashes and exact 4 RPF/6 BIK counts; ignored local operator guide covers layout, regeneration, preservation, cleanup, pins, and update triggers; private/index audits remain clean. Risks: Xenia Canary and reverse-engineering/graphics tools are not pinned, and bootstrap does not yet aggregate all gates or regenerate the local manifest. Re-evaluated estimate: 1 working day. Gate readiness: not ready. Next tasks: M1-013 and M1-014. Scope/criteria remain valid.
- 2026-08-11 — M1 checkpoint 7. Completed since prior checkpoint: M1-013 and M1-014. Health: `GREEN`. Evidence: pinned Xenia Canary build produced the expected null-backend build log; compatible Ghidra 12.0.4/XEXLoaderWV 13.0.0/JDK 21 stack imported the supported retail XEX, recovered its entry point and sections, and created 20,071 functions plus 257 import references; RenderDoc 1.45 CLI passed. Risks: the aggregate bootstrap and minimal ReXGlue sample gate remain incomplete, and actual GPU captures wait for the M2 emulator baseline or native scaffold. Re-evaluated estimate: less than 1 working day. Gate readiness: pending M1-015 and closure verification. Next task: M1-015. Scope/criteria remain valid.
- 2026-08-11 — M1 checkpoint 8. Completed since prior checkpoint: M1-015. Health: `GREEN`. Evidence: fresh-shell aggregate bootstrap passed all 12 checks, including full source/extraction hashes, and the missing-tool test reported all checks then returned exit code 1. Risks: the milestone's minimal ReXGlue initialization/build exit criterion still requires closure verification; no application scaffold is claimed yet. Re-evaluated estimate: less than 0.5 working day. Gate readiness: pending minimal sample and final milestone review. Next task: M1 closure verification. Scope/criteria remain valid.
- 2026-08-11 — M1 closure review. Completed: all 15 M1 tasks. Health: `GREEN`; decision: `GO M2`. Evidence: task commits through `b0c2f9c`; fresh-shell bootstrap 12/12; intentional missing-tool exit 1; clean ast-grep scan and 3/3 tests; 15/15 task-criteria coverage; exact unchanged ISO/XEX and 15-file payload hashes; ReXGlue v0.9.0 recursive pin; successful `rexglue init`; and a C++23 installed-SDK consumer that linked `rex::runtime` and printed `0.9.0`. Zero proprietary/private paths are tracked and Git integrity has no errors. Residual risks: target codegen, control-flow quality, runtime imports, real Xenia gameplay, and graphics/audio behavior are unmeasured and remain M2+ work. Gate readiness: ready. Next tasks: M2-001 and M2-002. Closure evidence is in `docs/evidence/M1-closure-review.md`. Scope/criteria remain valid.

## 10. Milestone M2 — Behavioral baseline and feasibility/codegen audit

**Goal:** prove that the exact XEX can be analyzed and converted, and quantify every blocker before substantial runtime implementation begins.

**Status:** `CLOSED — GO WITH SDK FORK M3` on 2026-08-11.

**Estimate:** 2–10 working days; extend only with an explicit feasibility review.

### Prerequisites

- M1 closed
- Verified `private/game/default.xex`
- Pinned Xenia Canary and ReXGlue builds

### Tasks

- [x] **M2-001** Run the unmodified Complete Edition in Xenia Canary at stock 30 FPS. The pinned build loaded exact module hash `1984A3354B78CE19` with patches/title updates disabled and time scalar 1; a clean profile/save, intro route, and active HUD gameplay were observed in an isolated private session with no fatal/crash markers. Reproducible launcher and evidence: `scripts/run-xenia-baseline.ps1`, `docs/evidence/M2-001-stock-xenia-gameplay.md`.
- [x] **M2-002** Record sanitized baseline metadata: module hash, title/media IDs, mounted paths, imported modules, kernel build expectations, and first fatal/warning events. The exact private snapshot is identified by SHA-256 while the tracked report contains only whitelisted metadata, placeholder mounts, complete warning-class counts, and zero fatal markers. Reproducible exporter and evidence: `scripts/export-xenia-baseline.ps1`, `docs/evidence/M2-002-xenia-runtime-metadata.md`.
- [x] **M2-003** Capture baseline screenshots or video for intro, frontend, garage, daytime race, nighttime race, HUD, and pause menu; keep copyrighted raw captures private unless distribution is approved. Native 1280x720 Xenia F12 captures cover every required state, were visually reviewed and hash-indexed, and remain ignored/private. Evidence: `docs/evidence/M2-003-private-capture-index.md`.
- [x] **M2-004** Record baseline save creation/load behavior and controller mapping. An isolated save changed across confirmed autosaves, survived normal exit, was enumerated and loaded by a clean verified resume, and returned the operator to saved progress. Both sticks, D-pad, triggers, bumpers, XYAB, Start, and Back/Select responded. Resume safety and evidence: `scripts/run-xenia-baseline.ps1`, `docs/evidence/M2-004-save-controller-reload.md`.
- [x] **M2-005** Scaffold the MCLA ReXGlue application with `rexglue init`. ReXGlue v0.9.0 generated the canonical `mcla` CMake/C++23 application, preset matrix, manifest, and tracked non-proprietary `generated/rexglue.cmake` bootstrap; all guest-generated output remains ignored. Evidence: `docs/evidence/M2-005-rexglue-scaffold.md`.
- [x] **M2-006** Validate and harden the ReXGlue v0.9.0 `mcla_manifest.toml` (the current manifest-first replacement for the originally planned legacy `mcla_config.toml`) so it resolves only the approved private XEX/game root and ignored generated output. The exact-schema validator enforces the pinned SDK/project, portable contained paths, reparse safety, exact XEX size/hash, and the sole reviewed analysis include introduced by M2-012; the immutable M2-008 run used an empty include list. Evidence: `scripts/verify-rexglue-manifest.ps1`, `docs/evidence/M2-006-manifest-validation.md`.
- [x] **M2-007** Keep all codegen optimization flags disabled for the first successful analysis. All nine ReXGlue v0.9.0 entrypoint code-generation booleans remain explicit `false`; the pinned-source historical policy gate rejects manual/analysis overrides and reconstructs the empty-include M2-008 state in tests. Evidence: `scripts/verify-first-analysis-policy.ps1`, `docs/evidence/M2-007-conservative-codegen-policy.md`.
- [x] **M2-008** Run codegen without `--force` and retain the full sanitized validation report. The immutable first run completed all six analysis phases, returned exit code 1 for exactly seven `UnresolvedCall` validation findings, emitted no guest-derived output, and is retained privately with hashed streams. A fail-closed exporter publishes the complete path/thread-sanitized transcript. Evidence: `scripts/run-rexglue-codegen-gate.ps1`, `scripts/export-rexglue-codegen-gate.ps1`, `docs/evidence/M2-008-non-force-codegen.md`.
- [x] **M2-009** Run codegen with `--force` only to inventory unresolved calls, invalid regions, unsupported PPC instructions, jump-table failures, exception handlers, and oversized functions. The separate immutable force run returned exit code 0, retained all 7 original `UnresolvedCall` findings, reported 20 emitted-but-anomalous `float16_4` pack operand shapes, and reported zero findings for the remaining requested classes. Its 64-file/128,010,691-byte ignored output is hash-manifested privately. Evidence: `scripts/run-rexglue-force-inventory.ps1`, `scripts/export-rexglue-force-inventory.ps1`, `docs/evidence/M2-009-force-codegen-inventory.md`.
- [x] **M2-010** Classify every codegen finding as config fix, ReXGlue defect, project hook/stub, false positive, or unknown. All 27 unique findings have an explicit owner, severity, and next action: seven executable direct-branch boundary failures are MCLA-R config fixes (S1), while twenty valid `vpkd3d128` FLOAT16_4 mask=3/shift=0 sites expose a ReXGlue diagnostic/coverage defect (S2). No finding remains unknown. Reproducible private-address audit, exporter, tests, and evidence: `scripts/ghidra/AddressAudit.java`, `scripts/export-codegen-triage.ps1`, `docs/evidence/M2-010-codegen-triage.md`.
- [x] **M2-011** Identify save/restore helpers, `setjmp`/`longjmp`, exception metadata, function chunks, and discontinuous control flow. Exact GPR/FPR/VMX save/restore bases are verified; `.pdata` contains 20,071 records and 34 exception-marked functions; no standard setjmp/longjmp body exists, so both overrides remain unset. All seven unresolved targets are bounded entries in PDATA gaps: one parent-chunk candidate, three computed-dispatch entries, one leaf, and two tail thunks. Reproducible audit/export/tests and notes: `scripts/ghidra/ControlFlowAudit.java`, `scripts/export-control-flow-audit.ps1`, `docs/evidence/M2-011-control-flow-metadata.md`, `docs/reverse-engineering.md`.
- [x] **M2-012** Add manual `[functions]`, `[[switch_tables]]`, invalid-data regions, and exception-handler entries incrementally with an address rationale in `docs/reverse-engineering.md`. Eight evidence-bounded function entries (including one verified parent chunk and one follow-up entry exposed during iteration) reduce non-force `UnresolvedCall` counts from 7 to 0. Switch-table, invalid-region, exception-hint, and setjmp/longjmp override counts remain explicitly zero because no evidence justifies them. Two clean runs produced identical 64-file/128,031,984-byte private manifests. Evidence: `config/mcla_functions.toml`, `scripts/verify-analysis-config.ps1`, `scripts/run-rexglue-config-iteration.ps1`, `docs/evidence/M2-012-manual-analysis-config.md`.
- [x] **M2-013** Map import records to symbolic XAM/xboxkrnl exports and generate an import coverage table. All 503 XEX import records map to 257 known symbols (95 XAM, 162 xboxkrnl; 246 functions and 11 variables), with zero unknown ordinals. The accepted generated snapshot contains all 246 function thunks and 1,517 direct call sites across 240 functions; six functions are explicitly indirect-only and all variables are classified as data imports. Evidence: `scripts/export-import-coverage.ps1`, `scripts/test-export-import-coverage.ps1`, `docs/evidence/M2-013-import-coverage.md`.
- [x] **M2-014** Identify early startup calls and the minimum import/stub set required to reach the module entry point. The load-time gate requires all 11 imported variables, and the pinned SDK maps every one before guest entry. A bounded static closure from `xstart` through its seven initializer roots to the first title-main call covers 59 internal functions, depth 6, and 26 function imports; all 26 have concrete SDK registrations. No project-owned startup stub or variable mapping is missing; `XamShowMessageBoxUIEx` remains one explicit semantic stub on the conservative error-dialog branch. Evidence: `scripts/export-startup-import-set.ps1`, `scripts/test-export-startup-import-set.ps1`, `docs/evidence/M2-014-startup-import-set.md`.
- [x] **M2-015** Compare known Complete Edition Xenia patch addresses against the local image, including skip intro, delta-time/60 FPS, motion blur, MSAA, and imposter-shadow patches. The exact Complete Edition upstream file at commit `84d6682caf1b75b2fdb7adcd197c6559c09b2ed4` matches module hash `1984A3354B78CE19` and Media ID `5940C9DB`. Ghidra verified all seven requested writes plus the upstream DbgPrint diagnostic write against loaded-image bytes and instruction context. All six groups remain disabled; zero addresses were rejected and none were applied. Evidence: `scripts/ghidra/PatchAudit.java`, `scripts/export-xenia-patch-audit.ps1`, `scripts/test-export-xenia-patch-audit.ps1`, `docs/evidence/M2-015-xenia-patch-audit.md`.
- [x] **M2-016** Run ReXGlue codegen regression tests if the SDK or analyzer is modified. Fork v0.9.0.1 accepts valid FLOAT16_4 mask 3 and adds direct PPC coverage. The focused suite passes 17 cases/136 assertions, the full suite passes 1,459 cases/5,733 assertions, all twenty MCLA warnings disappear, and all 64 generated files remain byte-identical. Evidence: `scripts/run-rexglue-vector-regression.ps1`, `docs/evidence/M2-016-rexglue-vector-regression.md`, `docs/rexglue-sdk.md`.
- [x] **M2-017** Write `docs/evidence/M2-feasibility-report.md` with blocker counts, ownership, proposed fixes, and `GO`, `GO WITH SDK FORK`, or `NO-GO` recommendation. The reviewed report selects `GO WITH SDK FORK`: all 7 S1 control-flow and 20 S2 vector findings are closed, zero unknown codegen findings remain, M3 risks have owners/closure conditions, and no manual user action is required before M3-001. Evidence: `docs/evidence/M2-feasibility-report.md`.

### Deliverables

- exact local module hash and Xenia baseline
- generated-code feasibility report
- import coverage matrix
- documented manual analysis configuration
- explicit SDK fork decision

### Verification evidence

- Codegen is deterministic across two clean runs.
- Generated file manifests match between identical runs.
- Every validation error is classified; there are no uncatalogued failures.
- Manual boundaries and switch tables include address-level evidence.
- Unsupported instructions are enumerated by unique opcode and reachable execution phase.

### Exit criteria

- ReXGlue completes analysis without crashing.
- The remaining blockers have finite, testable remediation paths.
- No evidence suggests pervasive self-modifying code or an unsupported execution model that invalidates static recompilation.
- The team explicitly accepts the estimated runtime/SDK work.

### Stop conditions

Pause for a feasibility decision if:

- analysis is nondeterministic
- essential control flow cannot be reconstructed after targeted investigation
- startup requires broadly unsupported exception or MMIO behavior
- generated code size or build requirements are impractical for the intended development environment

### Gate

`GO M3` requires an approved feasibility report, not merely generated C++ files.

### Checkpoint log

- 2026-08-11 — M2 checkpoint 1. Completed: M2-001 (1/17). Health: `GREEN`. Evidence: exact pinned Xenia build/module hash; isolated stock command line with patches/title updates disabled and time scalar 1; clean profile/save creation; private intro/cutscene/gameplay F12 captures; responsive five-minute D3D12/XAudio2 session with zero fatal/crash/device-lost markers; and reviewed `scripts/run-xenia-baseline.ps1` preflight/rejection tests. New risks: optional SDL controller database is absent and multiple FFB/import warnings are present, but neither blocked the tested route. Re-evaluated estimate: 2–10 working days. Gate readiness: not ready; no codegen evidence exists. Next tasks: M2-002 and M2-003. Scope/criteria remain valid. Evidence: `docs/evidence/M2-001-stock-xenia-gameplay.md`.
- 2026-08-11 — M2 checkpoint 2. Completed: M2-001 through M2-002 (2/17). Health: `GREEN`. Evidence: immutable private log/config snapshot; exact module/title/media/entry identifiers; sanitized logical and placeholder host mounts; XEX import-record and unique-import coverage counts; explicit separation of static XBOXKRNL, requested import, and configured Xenia kernel-build metadata; 34/34 warning/error events assigned to seven finite classes; and zero fatal/crash/assert/device-lost/unhandled-exception markers. Re-evaluation: the import/FFB, undefined extern, file-sector stub, and missing development-device observations remain bounded triage inputs for M2-010/M2-014 rather than gameplay blockers. Estimate remains 2–10 working days. Gate readiness: not ready; visual/save baseline and codegen evidence remain open. Next tasks: M2-003 and M2-004. Scope/criteria remain valid. Evidence: `docs/evidence/M2-002-xenia-runtime-metadata.md`.
- 2026-08-11 — M2 checkpoint 3. Completed: M2-001 through M2-004 (4/17). Health: `GREEN`. Evidence: all seven required stock visual states are privately captured and hash-indexed; a clean isolated save changed across autosaves, survived normal exit, was rediscovered by a verified resume, and loaded the prior progress; and the full standard Xbox-compatible input surface responded across menus, driving, pause, garage, and races. Re-evaluation: the stock gameplay baseline is complete and no newly observed runtime behavior blocks static recompilation analysis. Known import/FFB/file-system warnings remain explicitly deferred to M2-010/M2-014. Estimate remains 2–10 working days. Gate readiness: not ready; application scaffold and deterministic codegen evidence remain open. Next tasks: M2-005 and M2-006. Scope/criteria remain valid. Evidence: `docs/evidence/M2-003-private-capture-index.md`, `docs/evidence/M2-004-save-controller-reload.md`.
- 2026-08-11 — M2 checkpoint 4. Completed: M2-001 through M2-006 (6/17). Health: `GREEN`. Evidence: canonical ReXGlue v0.9.0 manifest-first C++23 scaffold; successful Windows AMD64 configure exposing `mcla` and `mcla_codegen`; fresh-shell bootstrap 12/12; tracked non-proprietary CMake bootstrap with all guest output still ignored; and an exact manifest gate constraining the supported XEX/game root/output with identity and path-safety checks. Re-evaluation: legacy `mcla_config.toml` is superseded by the pinned SDK's `mcla_manifest.toml`; no parallel obsolete config will be fabricated. Estimate remains 2–10 working days. Gate readiness: not ready; first-analysis flags and codegen evidence remain open. Next tasks: M2-007 and M2-008. Scope revised only for the manifest-first SDK interface; feasibility intent is unchanged. Evidence: `docs/evidence/M2-005-rexglue-scaffold.md`, `docs/evidence/M2-006-manifest-validation.md`.
- 2026-08-11 — M2 checkpoint 5. Completed: M2-001 through M2-008 (8/17). Health: `GREEN WITH BOUNDED ANALYSIS FINDINGS`. Evidence: all nine code-generation options explicitly disabled and source-verified; immutable non-force command with `force=false`; Register/Scan/Discover/GapFill/Merge/Validate completion; exit code 1 with exactly seven `UnresolvedCall` details; no generated target output; and deterministic fail-closed publication of the full sanitized transcript. Re-evaluation: the first feasibility blocker is narrow and measurable—seven direct branch targets not assigned to recovered functions. It advances to the separate force inventory and finding triage; it does not justify config hints yet. Estimate remains 2–10 working days. Gate readiness: not ready; M2-009 through M2-017 remain open. Next tasks: M2-009 and M2-010. Scope and criteria remain valid. Evidence: `docs/evidence/M2-007-conservative-codegen-policy.md`, `docs/evidence/M2-008-non-force-codegen.md`.
- 2026-08-11 — M2 checkpoint 6 (Codex review). Completed since prior checkpoint: M2-009 and M2-010; total 10/17. Health: `YELLOW — FINITE S1/S2 REMEDIATION`. Evidence: immutable force generation completed with 64 ignored files and a closed inventory; a private 34-address Ghidra audit plus deterministic exporter assigns all 27 unique findings to seven MCLA-R config fixes (S1) and twenty ReXGlue vector-pack defects (S2), with zero unknowns. New risk: ReXGlue FLOAT16_4 mask=3 semantics need regression coverage before graphics acceptance. Removed risk: no missing jump table, invalid region, unimplemented PPC instruction, exception-handler failure, oversized output warning, or uncatalogued codegen class was observed. Re-evaluated estimate remains 2–10 working days. Gate readiness: not ready; function/EH analysis and justified configuration remain open. Next tasks: M2-011 and M2-012. Scope and acceptance criteria remain valid; M2-016 now owns the 20-site SDK regression corpus. Evidence: `docs/evidence/M2-009-force-codegen-inventory.md`, `docs/evidence/M2-010-codegen-triage.md`.
- 2026-08-11 — M2 checkpoint 7 (Codex review). Completed since prior checkpoint: M2-011 and M2-012; total 12/17. Health: `YELLOW — SDK REGRESSION REMAINS`. Evidence: all save/restore helpers and 20,071 PDATA records are mapped; 34 exception-marked functions parsed; setjmp/longjmp is explicitly not used; all seven original cross-gap branches and one follow-up entry have bounded rationales; non-force codegen reaches zero analysis errors; and two clean outputs have an identical 64-file/128,031,984-byte manifest. Removed risk: the seven S1 analysis blockers are closed by minimal reviewed configuration. Remaining risk: twenty S2 FLOAT16_4 warnings require M2-016 semantics tests. Estimate narrows to 1–6 working days. Gate readiness: not ready; import/startup coverage, patch comparison, SDK regression, and feasibility decision remain. Next tasks: M2-013 and M2-014. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M2-011-control-flow-metadata.md`, `docs/evidence/M2-012-manual-analysis-config.md`.
- 2026-08-11 — M2 checkpoint 8 (Codex review). Completed since prior checkpoint: M2-013 and M2-014; total 14/17. Health: `YELLOW — SDK VECTOR REGRESSION REMAINS`. Evidence: all 503 XEX import records resolve to 257 known XAM/xboxkrnl symbols with explicit static reachability; all 246 function thunks match the accepted dispatcher; all 11 variable imports have pinned SDK mappings; and the conservative pre-main envelope contains 26 registered functions with no missing project stub. Removed risk: no unknown ordinal or missing early-startup registration remains. Remaining risks: public patch addresses need local byte verification, and twenty FLOAT16_4 warnings still need SDK regression coverage. Estimate narrows to 1–4 working days. Gate readiness: not ready; M2-015 through M2-017 remain. Next tasks: M2-015 and M2-016. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M2-013-import-coverage.md`, `docs/evidence/M2-014-startup-import-set.md`.
- 2026-08-11 — M2 checkpoint 9 (Codex review). Completed since prior checkpoint: M2-015 and M2-016; total 16/17. Health: `GREEN — FEASIBILITY DECISION PENDING`. Evidence: all eight Complete Edition public patch writes match the local image and remain disabled; forked ReXGlue v0.9.0.1 has direct FLOAT16_4 mask-3 semantics coverage; focused and full PPC suites pass; the twenty vector warnings fall to zero; and all 64 generated files remain byte-identical to M2-012. Removed risks: patch-address mismatch and unverified vector-pack semantics. New dependency: MCLA-R must pin the public `2FED/rexglue-sdk` fork until the fix lands upstream. Estimate narrows to less than one working day for M2-017 and closure review. Gate readiness: technically ready for decision, not yet approved. Next task: M2-017, then milestone closure review. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M2-015-xenia-patch-audit.md`, `docs/evidence/M2-016-rexglue-vector-regression.md`.
- 2026-08-11 — M2 closure review. Completed: all 17 M2 tasks. Health: `GREEN`; decision: `GO WITH SDK FORK M3`; release: `0.2.0.0`. Evidence: task commits through `f0be783`; 17/17 task and acceptance-register coverage; project tests 10/10; ReXGlue PPC 1,459/1,459 cases and 5,733 assertions; clean ast-grep scan and 3/3 rule tests; bootstrap 12/12; final non-force codegen with zero diagnostics and the accepted 64-file manifest; complete 503-record import map; public fork/tag/SHA verification; zero prohibited tracked paths; and clean Git integrity apart from harmless dangling objects. Residual native build/startup/import-semantic risks are owned by M3. Gate readiness: ready. Next tasks: M3-001 and M3-002. Closure evidence: `docs/evidence/M2-closure-review.md`. Scope and criteria remain valid.

## 11. Milestone M3 — Clean native build and module boot

**Goal:** compile, link, and launch a native executable that initializes ReXGlue, loads the XEX image, and enters guest startup code with actionable diagnostics.

**Status:** `CLOSED — GO M4` on 2026-08-11.

**Estimate:** closed in one working day of milestone execution after M2.

### Prerequisites

- M2 closed with `GO` or `GO WITH SDK FORK`
- Stable generated-code manifest
- Import coverage table

### Tasks

- [x] **M3-001** Integrate generated sources through CMake without tracking private/generated proprietary content. The exact ReXGlue v0.9.0.1 preset consumes 62 generated C++ files from the accepted 64-file manifest; a clean 66-action Release build compiles all 62 objects and links an ignored 41,972,736-byte PE. Version mismatch, missing-generated fallback, hash/list/containment, and Git-ignore gates pass. Evidence: `scripts/verify-generated-integration.ps1`, `scripts/test-generated-integration.ps1`, `docs/evidence/M3-001-generated-source-integration.md`.
- [x] **M3-002** Add a project `ReXApp` subclass and document lifecycle hooks. `MclaApp` owns three minimal hooks and a guest-free probe. CDB traced the first Windows fast-fail to ReXGlue's locale-dependent `path.string()` conversion of a localized Documents path. Fork v0.9.0.2 converts all nine host paths to UTF-8 and adds non-ASCII regression coverage; final RelWithDebInfo and Release probes exit 0 with four ordered markers and controlled shutdown. Evidence: `scripts/test-app-lifecycle.ps1`, `scripts/run-app-lifecycle-smoke.ps1`, `scripts/debug-app-lifecycle-smoke.ps1`, `docs/evidence/M3-002-app-lifecycle.md`.
- [x] **M3-003** Configure guest memory layout, image base, function dispatch, and executable entry point. Static validation fail-closes on any mismatch in the exact image/code/dispatch ranges, bounded ordered 30,008-function map, sentinel, or entry mapping. Release and RelWithDebInfo probes load the real XEX, verify base `82000000`, entry `821322B8`, and dispatcher coverage, then exit 0 before guest-thread creation. The first probe exposed an SDK teardown use-after-free fixed in fork v0.9.0.3. Evidence: `scripts/verify-module-config.ps1`, `scripts/test-module-config.ps1`, `scripts/run-module-config-smoke.ps1`, `docs/evidence/M3-003-module-config.md`.
- [x] **M3-004** Mount `private/game` as the guest disc root through the VFS. ReXGlue v0.9.0.4 fail-closes root traversal, write-intent opens, rename, and writable mappings on read-only host devices. Release and RelWithDebInfo probes resolve representative XEX/BIK/RPF files through `game:`, `d:`, and the physical device, deny every tested mutation path, preserve the exact 15-file/6,569,586,392-byte source snapshot, and exit 0 before guest launch. Evidence: `scripts/test-vfs-policy.ps1`, `scripts/run-vfs-smoke.ps1`, `docs/evidence/M3-004-vfs-disc-root.md`.
- [x] **M3-005** Implement the minimum missing xboxkrnl imports required for startup. Runtime-guided, non-force analysis repair adds twelve bounded callable entries missed by static GapFill and raises the dispatch map from 30,008 to 30,020 entries. A CDB comparison at the title-main boundary proves all 11 variable imports and the exact 10-function stock xboxkrnl route out of the 22-function conservative set; 1 positive and 4 negative verifier fixtures fail closed on boundary, function, variable, and fatal-log regressions. No project-owned xboxkrnl semantic stub is required for the observed startup route. Evidence: `scripts/test-xboxkrnl-startup-import-trace.ps1`, `scripts/run-xboxkrnl-startup-import-trace.ps1`, `docs/evidence/M3-005-xboxkrnl-startup-imports.md`.
- [x] **M3-006** Implement the minimum missing XAM imports required for startup. CDB reviews all four conservative XAM bridges at the title-main boundary and proves the exact stock route is one `XGetAVPack` call returning guest `r3 = 6`; `XGetLanguage`, `XamShowMessageBoxUIEx`, and `XamLoaderTerminateTitle` remain zero-hit conditional/post-main paths. One positive and six negative fixtures reject boundary, required-call, result-value, route-expansion, semantic-stub, and fatal-log regressions. No project-owned XAM stub is required before title main. Evidence: `scripts/test-xam-startup-import-trace.ps1`, `scripts/run-xam-startup-import-trace.ps1`, `docs/evidence/M3-006-xam-startup-imports.md`.
- [x] **M3-007** Add explicit, logged offline stubs for nonessential XONLINE/XHV paths. ReXGlue v0.9.0.5 replaces nine generic stale-`r3` XAM/XONLINE exports and one silent XHV query with deterministic offline contracts and once-only diagnostics. Ten guest-visible regression assertions plus one positive and three negative project-verifier cases cover registration, logs, return state, accepted-import scope, and stale-register rejection. Evidence: `scripts/verify-offline-service-stubs.ps1`, `scripts/test-offline-service-stubs.ps1`, `docs/evidence/M3-007-offline-services.md`.
- [x] **M3-008** Implement deterministic guest timebase and thread-start behavior for early initialization. ReXGlue v0.9.0.6 retains the Xenia-compatible 10-ms guest-thread startup grace period, testable raw/XAPI dispatch plans, and direct 50-MHz clock regression coverage. A fail-closed project contract rejects four semantic mutations; three isolated CDB runs preserve the exact `create-thread -> system-time -> time-frequency -> start-plan` first-occurrence order within a 30-second/64-event bound. Evidence: `scripts/verify-early-init-contract.ps1`, `scripts/test-early-init-contract.ps1`, `scripts/verify-early-init-trace.ps1`, `scripts/test-early-init-trace.ps1`, `scripts/run-early-init-smoke.ps1`, `docs/evidence/M3-008-early-init.md`.
- [x] **M3-009** Add crash handling that reports guest PC, host stack, current PPC function, thread, and last import without dumping proprietary memory by default. ReXGlue v0.9.0.7 tracks function/basic-block and typed/raw/stub import breadcrumbs, catches C++ guest exceptions at `XThread`, and emits a bounded metadata-only schema. Project gates pass one positive plus nine negative contract/report fixtures; three consecutive live synthetic probes load the verified XEX, write exact required context with five host frames, exclude guest memory and private paths, and exit 0 without guest execution. Evidence: `scripts/verify-crash-report-contract.ps1`, `scripts/test-crash-report-contract.ps1`, `scripts/verify-crash-report.ps1`, `scripts/test-crash-report.ps1`, `scripts/run-crash-report-smoke.ps1`, `docs/evidence/M3-009-crash-reporting.md`.
- [x] **M3-010** Add structured logging categories for app, PPC, kernel, XAM, VFS, GPU, audio, input, and patches. The project layer registers nine exact schema-1 category names, routes existing lifecycle/PPC/VFS diagnostics away from the generic logger, and exposes an init-only `inherit|trace|debug|info|warn|error|critical|off` override for each. Static gates pass two positive and nine negative fixtures; nine isolated live runs set the global level off, enable exactly one category, require one correctly labelled marker, reject all other markers/private paths/errors, and exit 0. Evidence: `scripts/verify-logging-contract.ps1`, `scripts/test-logging-contract.ps1`, `scripts/verify-logging-schema.ps1`, `scripts/test-logging-schema.ps1`, `scripts/run-logging-smoke.ps1`, `docs/evidence/M3-010-structured-logging.md`.
- [x] **M3-011** Add a known-address skip-intro development patch behind an off-by-default configuration switch if Bink blocks startup. The condition is not met: a bounded unpatched native run reaches module launch, then exposes two `gpu_plugin not set` graphics-prerequisite markers before any post-launch Bink/intro access. No skip-intro code or switch was added; the prior `0x821F7F64` byte audit remains disabled evidence only. One positive/four negative classifier fixtures fail closed if launch/GPU markers disappear, Bink evidence appears, or a guest crash occurs; a 15-second live run confirms cleanup and zero patch state. Evidence: `scripts/verify-intro-blocker-trace.ps1`, `scripts/test-intro-blocker-trace.ps1`, `scripts/verify-skip-intro-decision.ps1`, `scripts/run-intro-blocker-smoke.ps1`, `docs/evidence/M3-011-skip-intro-decision.md`.
- [x] **M3-012** Build Debug, RelWithDebInfo, and Release configurations. The host now requests the installed `xenos` GPU plugin through `rexglue_setup_target`; a fail-closed runner removes known cross-configuration staged DLLs, configures each exact preset, performs three `--clean-first` 71-action builds, and verifies 65 generated objects plus the matching runtime, Tracy, and Xenos DLL hashes. The first Debug attempt exposed stale pre-v0.9.0.7 installed SDK binaries; rebuilding/installing the fork's Debug and Release runtime/plugin artifacts plus the Release CLI aligned all configurations and restored bootstrap 12/12. Evidence: `scripts/run-build-matrix.ps1`, `scripts/verify-build-matrix.ps1`, `scripts/test-build-matrix.ps1`, `docs/evidence/M3-012-build-matrix.md`.
- [x] **M3-013** Fix all compile/link failures and reachable startup `PPC_UNIMPLEMENTED` traps. Normal launches now select staged `xenos` through the project default while guest-free probes remain GPU-independent. Runtime-guided non-force repair adds five exact callable boundaries (`0x8249CBF0`, `0x8249CC00`, `0x823F3C68`, `0x823F6EF8`, `0x822C9DD8`) and raises the dispatch map from 30,020 to 30,025. After a final 3/3 clean matrix, the default RelWithDebInfo route remains alive for 20 seconds with GPU load, interrupt, pipeline, and audio markers; it has no fatal, invalid-function, `PPC_UNIMPLEMENTED`, guest-crash, or post-launch Bink evidence, and cleanup is confirmed. Evidence: `scripts/verify-startup-trap-trace.ps1`, `scripts/test-startup-trap-trace.ps1`, `scripts/run-startup-trap-smoke.ps1`, `docs/evidence/M3-013-startup-traps.md`.
- [x] **M3-014** Add a startup smoke test with a bounded timeout and expected log markers. `MclaApp` now fail-closes the loaded XEX on exact Title ID `545407F8`, Media ID `5940C9DB`, image `82000000-829E0000`, and entry `821322B8`, and emits one canonical identity marker. The reusable RelWithDebInfo runner validates the supported `default.xex` before launch, requires 15 ordered lifecycle/GPU/image/VFS/module/graphics/audio markers within 20 seconds, rejects known fatal/trap/intro regressions, and accepts only a harness-owned stop that signals within five seconds. Two positive plus 22 negative source/log/result/preflight cases pass; the accepted live run reverified the immutable final log/result and left no process. Evidence: `scripts/verify-startup-smoke.ps1`, `scripts/verify-startup-smoke-result.ps1`, `scripts/test-startup-smoke.ps1`, `scripts/run-startup-smoke.ps1`, `docs/evidence/M3-014-startup-smoke.md`.
- [x] **M3-015** Verify repeated launch/exit does not leak persistent processes or corrupt local data. The canonical gate clean-builds RelWithDebInfo, runs ten isolated crash probes followed by ten consecutive normal startups closed through `WM_CLOSE`, re-hashes every completed evidence tree after later cycles, and verifies the full 15-file game manifest plus four runtime artifacts before/after. The accepted run passes 10/10 + 10/10 with exit 0, zero force cleanups/orphans, and unchanged data. KI-010 was reproduced and localized with thread-only CDB to ReXGlue Audio Worker force-termination poisoning a guest-heap lock; the guest-free probe now flushes and hard-exits after its report, and the final ten probes pass without cleanup. Evidence: `scripts/run-launch-exit-cycles.ps1`, `scripts/verify-launch-exit-cycles.ps1`, `scripts/test-launch-exit-cycles.ps1`, `docs/evidence/M3-015-launch-exit-cycles.md`.

### Deliverables

- native executable in all required configurations
- bounded startup smoke test
- actionable diagnostic/crash pipeline
- VFS-mounted original game data

### Verification evidence

- clean build succeeds from documented commands
- executable reaches the guest module entry point
- startup log shows verified image/title/media IDs
- invalid game data is rejected with a clear error
- ten consecutive startup/exit cycles complete without host crash or private-data modification outside approved paths

### Exit criteria

- No compile or link errors.
- No S0/S1 defects in setup, image loading, or controlled shutdown.
- First guest-code failure, if any, is deterministic and localized beyond module entry.

### Gate

`GO M4` requires reliable guest startup and diagnostics; a one-off boot is insufficient.

### Checkpoint log

- 2026-08-11 — M3 checkpoint 1 (immediate S1 re-evaluation). Completed: M3-001 (1/15); M3-002 implementation is open. Health: `RED — HOST LIFECYCLE FAST-FAIL`. Evidence: commit `466ea63`; clean 66-action Release integration; 12/12 project tests; first private M3-002 smoke log; Windows Application Error/WER event `0xC0000409` with fast-fail subcode 7 in `ucrtbase.dll`. New risk: the native host aborts after the project logging-ready hook and before path-finalization, so no module-boot work may be claimed or started. Estimate is suspended pending a symbolized stack. Gate readiness: not ready. Next task: diagnose/fix M3-002, then rerun lifecycle smoke; M3-003 remains queued. Scope and acceptance criteria remain valid.
- 2026-08-11 — M3 checkpoint 2 (two-task and SDK-change review). Completed: M3-001 and M3-002 (2/15). Health: `GREEN — MODULE BOOT NOT YET STARTED`. Evidence: clean 67-action Release build of all 62 generated objects; CDB root cause in `ReXApp::SetupEnvironment`; reviewed ReXGlue v0.9.0.2 UTF-8-path fix and focused regression; full PPC suite 1,459/1,459 cases and 5,733 assertions; final RelWithDebInfo and Release probes exit 0 with four ordered lifecycle markers. Removed risk: localized Windows profile paths no longer abort host initialization. Remaining risk: guest memory/dispatch/entry configuration is unexercised. Estimate returns to 1–4 weeks. Gate readiness: not ready. Next tasks: M3-003 and M3-004. Milestone scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-002-app-lifecycle.md`.
- 2026-08-11 — M3 checkpoint 3 (immediate S1 and SDK-change review). Completed: M3-003 (3/15). Health: `GREEN — ENTRY VERIFIED, GUEST NOT STARTED`. Evidence: exact `82000000-829E0000` image, `82130000-827CD054` code, and `829E0000-8373A0A8` dispatch ranges; bounded 30,008-entry map with one sentinel/entry; 1 positive and 5 negative fixtures; Release and RelWithDebInfo loaded-XEX probes with five ordered markers and exit 0. The first probe exposed a deterministic ReXApp teardown use-after-free; CDB localized stale `MnkInputDriver::attached_window_`, and fork v0.9.0.3 now destroys Runtime/input before Window. Removed risks: image/entry/dispatch mismatch and loaded-image shutdown crash. Remaining risk: VFS completeness and write containment are not yet proven. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-004 and M3-005. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-003-module-config.md`.
- 2026-08-11 — M3 checkpoint 4 (immediate S1 and SDK-change review). Completed: M3-004 (4/15). Health: `GREEN — DISC VFS CONTAINED, GUEST NOT STARTED`. Evidence: exact `game:`/`d:`/physical-device alias resolution for representative XEX/BIK/RPF entries; root-escape rejection; read-only open/create/delete/rename/writable-map regression coverage; 26/26 focused SDK assertions; Release and RelWithDebInfo VFS probes with five ordered markers and exit 0; unchanged 15-file/6,569,586,392-byte snapshot and 15/15 full hashes. ReXGlue v0.9.0.4 closes mutation bypasses found during the audit. Removed risk: guest disc access can no longer mutate or lexically escape the private source mount through the tested VFS APIs. Remaining risk: startup import behavior has not been executed. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-005 and M3-006. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-004-vfs-disc-root.md`.
- 2026-08-11 — M3 checkpoint 5 (immediate control-flow feasibility review). Completed: M3-005 (5/15). Health: `GREEN — TITLE MAIN REACHED`. Evidence: deterministic 64-file/128,038,099-byte non-force output and 30,020-entry dispatch map; twelve reviewed runtime-discovered tail/leaf entries; all 11 xboxkrnl variables patched once; exact 10-function stock route from the 22-function conservative matrix; one title-main boundary; 1 positive and 4 negative verifier fixtures; final RelWithDebInfo trace exits cleanly at the boundary. Removed risks: startup could fail on unregistered valid guest targets or hide a missing stock-route kernel import. Remaining risk: XAM and offline-service semantics beyond the boundary are unverified. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-006 and M3-007. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-005-xboxkrnl-startup-imports.md`.
- 2026-08-11 — M3 checkpoint 6 (two-task import review). Completed: M3-005 and M3-006 (6/15). Health: `GREEN — STOCK PRE-MAIN IMPORT ROUTE VERIFIED`. Evidence: all 11 xboxkrnl variables; exact 10/22-function xboxkrnl route; exact 1/4-function XAM route; `XGetAVPack` guest result `6`; one title-main boundary; 12 combined positive/negative import-verifier cases; no fatal, invalid-function, unimplemented, or reached XAM UI-stub marker. Removed risk: successful title-main entry no longer relies on unverified normal-route kernel/XAM imports. Remaining risk: offline XONLINE/XHV behavior and post-boundary initialization semantics are unverified. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-007 and M3-008. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-005-xboxkrnl-startup-imports.md`, `docs/evidence/M3-006-xam-startup-imports.md`.
- 2026-08-11 — M3 checkpoint 7 (immediate SDK-change review). Completed: M3-007 (7/15). Health: `GREEN — DIRECT OFFLINE SERVICE STATES EXPLICIT`. Evidence: nine direct XAM/XONLINE imports no longer use generic `REX_EXPORT_STUB`; `XamVoiceHeadsetPresent` is explicitly logged false; every reviewed bridge writes deterministic `r3`; 10/10 focused assertions, 210/210 relevant unit tests, and 1,459/1,459 PPC tests pass; project verifier passes one positive and rejects three semantic regressions. Removed risk: offline calls cannot inherit a caller register and masquerade as success, a title ID, or a connection state. Remaining risk: post-boundary call reachability and broader SDK generic-stub inventory are not yet behavior-classified. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-008 and M3-009. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-007-offline-services.md`.
- 2026-08-11 — M3 checkpoint 8 (two-task and immediate SDK-change review). Completed: M3-007 and M3-008 (8/15). Health: `GREEN — EARLY TIMING AND THREAD START ORDER BOUNDED`. Evidence: exact 50-MHz guest clock routes; raw/XAPI start-plan tests; Xenia-compatible 10-ms pre-dispatch grace period; 4/4 focused SDK cases and 14 assertions; 214/214 relevant SDK tests; 1,459/1,459 PPC cases; project contract passes one positive and rejects four semantic mutations; trace verifier passes one positive and rejects three invalid traces; three isolated live runs preserve `create-thread -> system-time -> time-frequency -> start-plan` under 30-second/64-event bounds. Removed risk: guest workers can no longer race creator-side early shared-state initialization solely because the compatibility delay was omitted. Remaining risk: post-boundary crashes are not yet converted into privacy-safe actionable reports, and scheduler-dependent create counts are observed rather than required to be identical. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-009 and M3-010. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-007-offline-services.md`, `docs/evidence/M3-008-early-init.md`.

- 2026-08-11 — M3 checkpoint 9 (immediate SDK-change review). Completed: M3-009 (9/15). Health: `GREEN — C++ GUEST FAILURES HAVE PRIVACY-SAFE CONTEXT`. Evidence: ReXGlue v0.9.0.7 at `efac376998cbb0520295d308be4703574a12a995`; schema-1 reports with last tracked guest PC/basic block, current PPC function, thread, last import, and at most 16 module-relative host frames; guest memory excluded by default; 4 focused SDK cases/27 assertions; 218/218 relevant SDK unit fixtures with four existing BitStream skips; 1,459/1,459 PPC cases; one positive/four negative source-contract fixtures; one positive/four negative report fixtures; and three consecutive isolated live probes with exit 0 and five host frames. Removed risk: a generated C++ exception can no longer cross guest `XThread` execution without bounded actionable metadata. Remaining risk: hardware SEH/signals, fatal aborts, and instruction-exact PC tracking outside generated boundaries are not claimed; the intermittent cold post-relink exit latency is tracked by KI-010/M3-015; logging categories and broader startup traps remain open. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-010 and M3-011. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-009-crash-reporting.md`; upstream: `rexglue/rexglue-sdk#409`.

- 2026-08-11 — M3 checkpoint 10 (two-task diagnostics review). Completed since prior two-task review: M3-009 and M3-010; total 10/15. Health: `GREEN — DIAGNOSTICS ARE ACTIONABLE AND FILTERABLE`. Evidence: privacy-safe schema-1 C++ guest crash reports; nine exact project logging categories; existing app/PPC/VFS messages categorized; nine init-only level overrides defaulting to `inherit`; one positive/five negative source-contract fixtures; one positive/four negative log fixtures; and nine isolated category runs where global logging is off and exactly one selected schema marker is written with exit 0. Removed risks: guest C++ failures no longer lack bounded context, and subsystem logs no longer depend on an undifferentiated project logger. Remaining risks: skip-intro is not implemented or proven necessary; configuration matrix, reachable startup traps, bounded startup smoke, and repeated-exit KI-010 remain open. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-011 and M3-012. Scope and acceptance criteria remain valid. Evidence: `docs/evidence/M3-009-crash-reporting.md`, `docs/evidence/M3-010-structured-logging.md`.

- 2026-08-11 — M3 checkpoint 11 (conditional-task re-evaluation). Completed: M3-011; total 11/15. Health: `YELLOW — GPU PREREQUISITE PRECEDES INTRO`. Evidence: exact prior skip-intro address/original/replacement audit retained with zero enabled patches; a 15-second unpatched run reaches native module launch, remains alive until bounded cleanup, records both no-GPU callback/ring-buffer markers, and records no post-launch Bink/intro, fatal, unimplemented, or guest-crash marker; one positive/four negative trace fixtures protect the classification. Decision: do not add skip-intro code because its stated prerequisite is false. Removed risk: an unjustified guest-code patch will not mask the current startup order. New explicit blocker: GPU plugin integration/configuration must be exercised in M3-012/M3-013 before Bink can be reassessed. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-012 and M3-013. Conditional M3-011 scope is closed without implementation; remaining milestone criteria remain valid. Evidence: `docs/evidence/M3-011-skip-intro-decision.md`.

- 2026-08-11 — M3 checkpoint 12 (build-matrix re-evaluation). Completed: M3-012; total 12/15. Health: `GREEN — ALL NATIVE CONFIGURATIONS BUILD WITH XENOS STAGED`. Evidence: exact ReXGlue v0.9.0.7 prefix and installed CLI; three clean 71-action builds; 65/65 generated objects per configuration; Debug, RelWithDebInfo, and Release each stage only their matching runtime, Tracy, and `rexgpu-xenos` DLL variant; manifest verifier passes one positive and rejects six malformed/failed configurations; project tests 26/26; ast-grep 3/3; bootstrap 12/12. The first Debug link and initial final bootstrap exposed locally stale SDK Debug/Release artifacts, not an upstream source defect; rebuilding and installing the fork's current runtime/plugin and Release CLI resolved both the missing crash-report symbols and the overwritten 0.9.0.4 CLI without code changes. Removed risks: configuration-specific compile/link drift and unstaged GPU plugin binaries. Remaining risks: the runtime still needs an explicit `gpu_plugin=xenos` startup route, reachable `PPC_UNIMPLEMENTED` classification, bounded smoke, and repeated-exit KI-010. Estimate remains 1–4 weeks. Gate readiness: not ready. Next tasks: M3-013 and M3-014. Scope and remaining acceptance criteria remain valid. Evidence: `docs/evidence/M3-012-build-matrix.md`.

- 2026-08-11 — M3 checkpoint 13 (immediate startup-boundary re-evaluation). Completed: M3-013; total 13/15. Health: `GREEN — XENOS STARTUP RUNS BEYOND REACHABLE TRAPS`. Evidence: project-default Xenos selection with guest-free probe exemption; five runtime-discovered entries regenerated non-force as two vtable thunks, two tail/status wrappers, and one bounded leaf; deterministic mapping count 30,025; current 67-file/65-source/133,908,410-byte manifest; final clean Debug/RelWithDebInfo/Release matrix; one positive/seven negative trace fixtures; a post-clean 20-second run reaching module launch, graphics interrupt, pipeline creation, and audio callbacks with zero fatal/unregistered/`PPC_UNIMPLEMENTED`/guest-crash/post-launch-Bink markers; project tests 27/27; ast-grep 3/3; bootstrap 12/12. Removed risks: no-GPU startup and every trap reachable within the accepted window; KI-011 is closed. Remaining risks: M3-014 must promote this route into the canonical bounded startup smoke, and M3-015 must prove ten repeated launch/exit cycles including KI-010. Estimate narrows to 1–3 working days. Gate readiness: not ready. Next tasks: M3-014 and M3-015. Scope and remaining criteria remain valid. Evidence: `docs/evidence/M3-013-startup-traps.md`.
- 2026-08-11 — M3 checkpoint 14 (canonical startup-smoke review). Completed: M3-014; total 14/15. Health: `GREEN — SUPPORTED IMAGE REACHES GRAPHICS/AUDIO UNDER A FAIL-CLOSED DEADLINE`. Evidence: exact runtime Title/Media/loaded-range/Entry validation and marker; 15 ordered lifecycle/GPU/image/VFS/module/graphics/audio milestones; supported-XEX size/hash preflight; two positive plus 22 negative source/log/result/preflight cases; successful incremental RelWithDebInfo build; independent final-diff review with three race/contract findings fixed and a second review adding an explicit cleanup-duration invariant; one isolated live run reaching all markers in 8,180 ms, signalling controlled cleanup in 161 ms, and completing in 8,361 ms with immutable 56,131-byte final log SHA-256 `EEF4AD9B206A2C49DF792A3FE349BE8141D5ACF0373C8ABF4AA319A1D0C6D587`; no fatal, plugin-load, invalid-function, `PPC_UNIMPLEMENTED`, guest-crash, contract-rejection, or post-launch Bink marker. Removed risks: the canonical smoke can no longer accept the wrong Complete Edition identity, a partial subsystem route, a self-exit race, unbounded declared cleanup, or mismatched pre/post-exit evidence. Remaining risk: one controlled termination is insufficient for repeated-exit and cross-cycle data-integrity claims; M3-015 and KI-010 remain open. Estimate narrows to 1–2 working days. Gate readiness: not ready. Next task: M3-015. Scope and remaining criteria remain valid. Evidence: `docs/evidence/M3-014-startup-smoke.md`.
- 2026-08-11 — M3 checkpoint 15 (repeated-exit and KI-010 re-evaluation). Completed: M3-015; total 15/15. Health: `GREEN — REPEATED STARTUP/CLOSE AND DATA INTEGRITY VERIFIED`. Evidence: one positive/23 negative physical-evidence fixtures including two junction cases; clean-first RelWithDebInfo build; ten crash probes followed by ten consecutive normal startups; 10/10 schema/shutdown/exact hard-exit probe reports and 10/10 exact ordered `WM_CLOSE`/hard-exit tails; exit code 0 and zero force cleanup across all 20; zero exact-path orphan processes; immutable prior-cycle trees; unchanged 15-file/6,569,586,392-byte game identity and four runtime artifacts. Independent review found and closed self-reported-evidence and reparse-ancestor P1 gaps before the final run, then a P2 crash-summary binding gap before commit. KI-010 reproduced twice and thread-only CDB localized the hang to `AudioSystem::Shutdown -> XThread::Terminate -> FreeStack -> BaseHeap::Release`; the synthetic no-guest probe now flushes and hard-exits after closing its report, while normal cycles retain independent real-window closure. Removed risks: repeated process leakage, cross-cycle evidence mutation, source-data drift, unbound evidence, junction redirection, and an unclassified post-relink hang. Remaining risks: general SDK forced-thread teardown remains upstream work; frontend/gameplay behavior begins in M4. Estimate: implementation complete, closure review pending. Gate readiness: ready for final M3 closure review. Evidence: `docs/evidence/M3-015-launch-exit-cycles.md`.
- 2026-08-11 — M3 closure review. Completed: all 15 M3 tasks. Health: `GREEN`; decision: `GO M4`; release: `0.3.0.0`. Evidence: 15/15 task/evidence coverage through `ce1f0fe`; project tests 29/29; clean ast-grep scan and 3/3 rule tests; bootstrap 12/12; final current-source clean Debug/RelWithDebInfo/Release matrix `20260811-172749-d2732624` with 65/65 generated objects in each; current 67-file/65-source generated integration; M3-014 canonical startup reaches all 15 ordered markers; M3-015 clean-builds and passes ten crash probes plus ten normal `WM_CLOSE` cycles with exit 0, zero forced cleanup/orphans, and unchanged game/binary identity. The SDK branch `mcla/mcla-r-hotfixes` and tag `v0.9.0.7` are publicly readable at exact commit `efac376998cbb0520295d308be4703574a12a995`. No S0/S1 defect remains in M3 setup, image-loading, or controlled-shutdown scope. KI-010 general forced-thread teardown remains S2/contained, and frontend/playability/save/render/audio correctness remain explicitly owned by M4+. Closure evidence: `docs/evidence/M3-closure-review.md`.

## 12. Milestone M4 — Frontend, intro path, and menu stability

**Goal:** reach and operate the main frontend reliably with rendering, input, profile, and basic audio functioning.

**Status:** `CLOSED — GO M5 WITH PINNED SAVE` on 2026-08-14.

**Estimate:** 1–6 weeks.

### Prerequisites

- M3 closed
- exact configuration-matched Xenos plugin stages and loads successfully
- private stock-Xenia visual baseline and privacy-safe capture tooling are available
- SDL input and basic XAM profile behavior are M4 closure dependencies owned by M4-004 through M4-006, not entry prerequisites

### Tasks

- [x] **M4-001** Instrument and verify a guest-originated swap through successful D3D12 presentation and a nontrivial guest-output readback. ReXGlue v0.9.0.9 now binds monotonic guest-output sequences to successful DXGI presents, holds mailbox ownership through capture readback, rejects ahead-of-present captures, and exposes the exact successful HRESULT class. The canonical clean RelWithDebInfo gate passes 20/20 isolated launches with at least three guest-backed presents per cycle, 20/20 independently nontrivial 1280x720 BMPs, 20/20 capture/present watermark equality, two-second dwell, exit 0 through `WM_CLOSE`, no force cleanup/orphans, and unchanged source game/runtime/prior-run trees. The project owner classified the private legal-screen capture as recognizable, correctly oriented MCLA-derived output rather than host-only blank/solid/garbage. This does not claim frontend reachability or rendering correctness. Evidence: `scripts/run-first-frame-smoke.ps1`, `scripts/verify-first-frame-smoke.ps1`, `scripts/test-first-frame-smoke.ps1`, `docs/evidence/M4-001-first-valid-frame.md`.
- [x] **M4-002** Validate the D3D12 host-RTV render-target, staged EDRAM resolve, depth, MSAA, table-gamma, shader/PSO, and draw paths used by the autonomous title frontend. ReXGlue v0.9.0.10 adds opt-in bounded schema-1 render auditing plus a GPU-thread checkpoint that freezes four internally balanced summaries before normal hard exit; Tracy instrumentation safely no-ops outside its manual lifetime. The canonical clean RelWithDebInfo gate passes 10/10 isolated 35-second title captures with exact logo/`PRESS` UI-region correlation against the private Xenia baseline, 27 render targets, 39-40 bound-format records, 40 ownership modes, 117-217 successful PSOs, 7,597-18,153 successful common-copy resolves, 1,199,122-3,085,569 guest draws, active depth/MSAA and non-identity table gamma, exit 0, no force cleanup/orphans, and physically reverified source/runtime/evidence integrity. The owner gave the bounded frontend visual PASS and initially classified a green vehicle artifact as minor/non-blocking; a later fixed-camera gameplay repro reclassified KI-013 as an open S2 saturated local-light/specular defect, without retroactively invalidating the stable UI-region gate. This task does not claim ROV, PWL gamma, true-direct resolve, whole-frame equality, menu usability, or general rendering correctness. Evidence: `scripts/run-render-path-smoke.ps1`, `scripts/verify-render-path-smoke.ps1`, `scripts/test-render-path-smoke.ps1`, `scripts/verify-sdk-profiling-lifetime.ps1`, `scripts/test-sdk-profiling-lifetime.ps1`, `docs/evidence/M4-002-render-paths.md`.
- [x] **M4-003** Classify the native intro route as verified Bink playback, a reproducibly justified development-only skip, or a stable guest-selected bypass that reaches the title without a compatibility patch. The selected result is `guest-bypass-no-patch`: three isolated unpatched noisy-I/O runs reached the physically verified Complete Edition title in 43,263-44,659 ms, retained active successful guest-file reads through the title boundary, and contained exactly the three project-owned pre-launch `intro720.bik` VFS resolutions with zero post-launch Bink/BIK access. All runs exited 0 through `WM_CLOSE`, required no force cleanup, preserved the exact game/runtime/prior-run trees, and retained the guarded no-patch decision. This proves the intro is not a blocker on the supported boot route; it does not prove Bink decode/playback correctness, which remains M7-006 scope. Evidence: `scripts/run-intro-route-decision.ps1`, `scripts/verify-intro-route-decision.ps1`, `scripts/test-intro-route-decision.ps1`, `docs/evidence/M4-003-intro-route-decision.md`.
- [x] **M4-004** Implement and verify the XAM user sign-in/profile behavior reached by the autonomous title frontend. ReXGlue v0.9.0.11 retains Xenia-compatible single-local-user semantics, yields cooperatively on the polled sign-in-state import, and adds an init-only bounded privacy-safe audit. Three isolated clean-build runs reached the verified title with slot 0 local, stable nonzero mask-7 XUID, consistent name/sign-in info, and distinct signed-out/absent evidence for slots 1, 2, and 3 (`E/E` masks); all exited 0 through `WM_CLOSE` without force cleanup, orphan, game/runtime drift, or private-data publication. Focused SDK tests verify the deterministic local profile and voice defaults `0/0/100`. `ReadProfileSettings`, privilege masks 251/252, and `SigninUI` were zero-hit in all accepted title runs, so their runtime routes remain conditionally guarded but are not claimed as physically exercised; generalized profile writes/persistence remain M6-006 scope. Evidence: `scripts/run-xam-profile-smoke.ps1`, `scripts/verify-xam-profile-smoke.ps1`, `scripts/test-xam-profile-smoke.ps1`, `docs/evidence/M4-004-xam-profile.md`.
- [x] **M4-005** Map SDL/controller input to the expected Xbox 360 user slot. ReXGlue v0.9.0.12 deterministically assigns the first physical SDL controller to slot 0 while retaining valid free SDL player indices for later pads, and adds an init-only bounded privacy-safe causal audit. One clean physical title run bound SDL South/A down and up edges to matching guest-visible slot-0 `XamInputGetState` edges, with query/success/disconnected masks `F/1/E`, zero removal/rejection/failure/drop counters, exit 0 through `WM_CLOSE`, no force cleanup/orphan, and unchanged source-game tree/runtime artifacts. This task proves one selected controller and one A challenge only; full controls, dead zones, focus, disconnect/reconnect, multi-pad behavior, and vibration remain M4-006. Evidence: `scripts/run-input-slot-smoke.ps1`, `scripts/verify-input-slot-smoke.ps1`, `scripts/test-input-slot-smoke.ps1`, `docs/evidence/M4-005-input-slot.md`.
- [x] **M4-006** Validate dead-zone thresholds, the full standard controller surface, disconnect/reconnect, and focus-loss behavior. Three immutable physical layers prove causal `F3FF` digital and `3FF` analog coverage, focus lost/neutral/gained, and a fresh slot-0 remove/disconnect/reconnect/success PASS. The final hotplug run completed through controlled exit and is recovered from a post-run `30025`/`30026` verifier-literal drift without inventing the absent artifact snapshot. Evidence is explicitly split/non-monolithic; multi-pad physical behavior and title-driven force feedback are not claimed. Evidence: `scripts/run-controller-matrix.ps1`, `scripts/verify-controller-matrix.ps1`, `scripts/test-controller-matrix.ps1`, `docs/evidence/M4-006-controller-matrix.md`.
- [x] **M4-007** Initialize and verify the autonomous title XAUD/XMA/SDL output route. ReXGlue v0.9.0.14 adds bounded privacy-safe telemetry from XMA decode through guest XAudio submission into SDL device frames plus queue/starvation health. One exact-release clean-build run passed focused 5-case/16-assertion tests and a 300-second soak (60,594 nonzero guest submits, 60,587 nonzero SDL frames, 319,325 nonzero XMA frames, max queue depth 8, zero starvation/failures/drops), followed by controlled `WM_CLOSE`. This proves sustained basic autonomous frontend audio, not individual UI/music event identity, human fidelity, XMP correctness, gameplay audio, device switching, or long-session stability. Evidence: `scripts/run-audio-route-smoke.ps1`, `scripts/verify-audio-route-smoke.ps1`, `scripts/test-audio-route-smoke.ps1`, `docs/evidence/M4-007-audio-route.md`.
- [x] **M4-008** Provide a state-correct XMP fallback for the reached title route. ReXGlue v0.9.0.15 keeps unsupported title-play/output-capture paths explicitly failed and `Idle`, guards empty playlist navigation, and adds a bounded race-free XMP audit. One exact-release clean-build title run passed focused 4-case/20-assertion tests and observed 4,625/4,625 known status queries with zero playback calls, state changes, unexpected/inconsistent calls, overflow, or drops; it then exited 0 through exact-PID `WM_CLOSE` with source-game/runtime integrity intact. This proves the reached metadata/status fallback, not an XMP decoder, user-selected music playback, or event-level audio identity. Evidence: `scripts/run-xmp-route-smoke.ps1`, `scripts/verify-xmp-route-smoke.ps1`, `scripts/test-xmp-route-smoke.ps1`, `docs/evidence/M4-008-xmp-fallback.md`.
- [x] **M4-009** Stub retired online presence and service checks without blocking offline frontend navigation. ReXGlue v0.9.0.16 gives the ten XLiveBase message IDs found in the MCLA corpus deterministic local-offline or unavailable results and adds an init-only guest socket block plus bounded audit. One exact-release clean-build title run passed 3-case/32-assertion focused tests, reached the verified frontend with the block enabled, observed zero service dispatches and zero socket attempts, and exited through exact-PID/title `WM_CLOSE` with source-game/runtime integrity intact. This proves the network-disabled offline route does not block the frontend; it does not claim runtime reachability of all ten messages, Xbox Live functionality, or a general host firewall. Evidence: `scripts/run-offline-service-smoke.ps1`, `scripts/verify-offline-service-smoke.ps1`, `scripts/test-offline-service-smoke.ps1`, `docs/evidence/M4-009-offline-services.md`.
- [x] **M4-010** Validate locale/language selection and guest path handling. ReXGlue v0.9.0.17 returns the configured Xbox language through both locale routes, audits the title-reached XConfig value, and converts all UTF-8 path CVars plus log filenames to native Windows paths without mojibake. One clean-build EN/US, FR/FR, and RU/RU matrix passed exact locale records, pinned title/logo and localized-prompt ROI checks, real Cyrillic/accented user/cache/log roots, controlled `WM_CLOSE`, no orphan, and unchanged source-game/runtime artifacts. The title route physically reached XConfig language 25 times per cycle; `XGetLanguage` and country were zero-hit and remain static/unit-proven, not runtime-proven. This does not claim complete localized content or gameplay. Evidence: `scripts/run-locale-path-smoke.ps1`, `scripts/verify-locale-path-smoke.ps1`, `scripts/test-locale-path-smoke.ps1`, `docs/evidence/M4-010-locale-paths.md`.
- [x] **M4-011** Automate the supported saved frontend route. ReXGlue v0.9.0.18 fixes trailing-separator VFS mounted-root resolution, and the project adds a bounded synthetic slot-0 probe with 200-ms holds and a two-second tab debounce. One exact-release clean build followed by three isolated cycles from the pinned post-OOBE save: title `START` reached active nighttime free roam, gameplay `START` reached pause, and two `RB` presses reached Modes then Settings/Options. Every cycle produced four distinct 1280x720 captures, passed pinned title plus pause/options ROI classification, preserved the save seed, source-game tree, and runtime artifacts, and exited 0 through external exact-window `WM_CLOSE` without force cleanup or an orphan. MCLA has no internal Exit command, and none is claimed. First-run OOBE, race flow, gameplay correctness, and detailed parity remain open. Evidence: `scripts/run-frontend-smoke.ps1`, `scripts/verify-frontend-smoke.ps1`, `scripts/test-frontend-smoke.ps1`, `docs/evidence/M4-011-frontend-smoke.md`.
- [x] **M4-012** Compare frontend frames and audio events with the pinned Xenia baseline. The canonical clean-build run reverified all three accepted 1280x720 M4-011 cycles and added a complete 2560x1440 scale-2 title, saved gameplay, pause, and Options cycle. Stable title/logo, prompt, HUD, pause-footer, and native Options regions passed normalized edge gates (0.751092-0.946633), with pause allowing only a bounded -5/+1-pixel registration; a private contact sheet was reviewed. The Xenia log and M4-007 route share one decoder/worker/client lifecycle, while the native five-minute route retains its nonzero XMA/XAudio/SDL counts. Whole-frame equivalence and individual UI/music event identity are explicitly not claimed. Evidence: `scripts/run-frontend-parity.ps1`, `scripts/verify-frontend-parity.ps1`, `scripts/test-frontend-parity.ps1`, `docs/evidence/M4-012-frontend-parity.md`.
- [x] **M4-013** Document all remaining frontend deviations and workarounds. The accepted clean-build closure gate completed 20/20 consecutive isolated routes from the pinned post-OOBE save through active free roam, pause, and Settings/Options, followed by external exact-window `WM_CLOSE`, exit 0, no force cleanup/orphan, and unchanged source-game/seed/runtime identities. The deviation register records every remaining M4 boundary with severity, supported workaround, and owning milestone. First-run OOBE, race completion, general persistence, whole-frame parity, Bink playback, event-level audio identity, multi-pad physical behavior, title-driven force feedback, and broader render/backend variants remain explicitly unclaimed. Decision: `GO M5 WITH PINNED SAVE`. Evidence: `scripts/run-frontend-smoke.ps1`, `scripts/verify-frontend-smoke.ps1`, `scripts/test-frontend-smoke.ps1`, `docs/evidence/M4-013-known-deviations.md`.

### Deliverables

- stable main menu/frontend
- functional controller navigation
- basic frontend audio
- documented intro-video status

### Verification evidence

- 20 consecutive pinned-save boot-to-frontend routes reach free roam, pause,
  and Settings/Options
- one selected controller completes the standard digital/analog, focus, and
  disconnect/reconnect matrix; multi-pad physical behavior remains unclaimed
- no persistent GPU validation error occurs on the scoped title/frontend route
- frontend audio runs for five minutes without runaway buffers or a crash
- stable frontend-region comparisons exist at 1280x720 and 2560x1440

### Exit criteria

- Main menu is reachable without debugger intervention.
- User/profile flow does not softlock.
- Input, presentation, and frontend audio are operational.
- Remaining visual defects are S2/S3 and documented.

### Gate

`GO M5` requires a repeatable path from process start to selecting a race or starting a new game.

### Checkpoint log

- 2026-08-11 — M4 checkpoint 1. Completed: M4-001 (1/13). Health:
  `GREEN — FIRST GUEST FRAME PHYSICALLY VERIFIED`. Evidence: corrected ReXGlue
  v0.9.0.9 at `01f895afb1b084652915c0b463910d57049f06be`; clean RelWithDebInfo build;
  20/20 guest-backed present/readback cycles; 20/20 independently nontrivial
  1280x720 captures; capture/present watermark equality in every cycle; no
  device loss, force cleanup, orphan, source-game/runtime/prior-tree drift, or
  private-path leak; project-owner visual PASS. Removed risk: pipeline creation
  is no longer mistaken for presentation. Remaining risks: frontend reachability,
  EDRAM/format/shader/gamma correctness, intro, input, profile, and audio remain
  unverified. Gate readiness: not ready. Active next task: M4-002. Evidence:
  `docs/evidence/M4-001-first-valid-frame.md`.

- 2026-08-12 — M4 checkpoint 2. Completed: M4-002 (2/13). Health:
  `GREEN — AUTONOMOUS TITLE FRONTEND AND SCOPED XENOS PATHS VERIFIED`.
  Evidence: ReXGlue v0.9.0.10 at
  `f7e9df0e7923b13ae3e879d39dc827b9010a0ab9`; clean RelWithDebInfo build;
  final physical run `20260812-085022-cc01a857`; 10/10 isolated title captures
  with logo and tight `PRESS` correlation against the pinned private Xenia
  reference; 10/10 balanced RT/BIND/ownership, staged-resolve, shader/PSO,
  draw/depth/MSAA, and table-gamma audits; exit 0, zero force cleanup/orphans,
  and physically reverified source/runtime/prior-tree integrity. The owner gave
  a visual PASS and initially classified a green vehicle artifact as minor and
  non-blocking; KI-013 was later reclassified by a fixed-camera gameplay repro
  as an open S2 saturated local-light/specular defect. Remaining scope: ROV,
  PWL gamma, true-direct resolve,
  whole-frame parity, intro disposition, input/profile/audio, and menu
  usability. Gate readiness: not ready. Active next task: M4-003. Evidence:
  `docs/evidence/M4-002-render-paths.md`.

- 2026-08-12 — M4 checkpoint 3 (intro-route feasibility decision). Completed:
  M4-003 (3/13). Health: `GREEN — UNPATCHED GUEST ROUTE BYPASSES BINK AND
  REACHES TITLE`. Evidence: final physical run `20260812-095920-a6979ac7`;
  3/3 isolated noisy-I/O title captures; 43,263-44,659 ms capture time;
  3/3 exact pre-launch project VFS resolution triplets; zero post-launch
  Bink/BIK access; 4,633-4,642 successful guest reads per run with 10-23
  successful reads near the title marker; physical logo/`PRESS` correlation;
  exit 0 through `WM_CLOSE`; no force cleanup/orphans; unchanged game,
  runtime, and prior-cycle trees; skip-intro code absent and disabled.
  Decision: no compatibility patch is justified. Remaining scope: Bink
  decoding/playback fidelity is not proven and remains M7-006; profile, input,
  audio, and menu usability remain active M4 work. Gate readiness: not ready.
  Active next task: M4-004. Evidence:
  `docs/evidence/M4-003-intro-route-decision.md`.

- 2026-08-12 — M4 checkpoint 4. Completed: M4-004 (4/13). Health:
  `GREEN — REACHED SINGLE-LOCAL-USER XAM CONTRACT VERIFIED`. Evidence: ReXGlue
  v0.9.0.11 at `078ce3468b000d1f256e6a6bf4313bc4df932551`; final physical run
  `20260812-123316-db0f1cf4`; 3/3 isolated
  title captures with slot 0 locally signed in, one stable nonzero mask-7 XUID,
  consistent name/sign-in information, and one distinct signed-out/no-such-user
  record for each slot 1, 2, and 3 with `E/E` masks. Focused SDK tests pass 2
  cases / 15 assertions for deterministic local-profile and voice defaults.
  Every runtime cycle exited 0 through `WM_CLOSE` with no force cleanup,
  orphan, source-game, runtime, or prior-tree drift. `ReadProfileSettings`,
  privilege masks 251/252, and `SigninUI` were zero-hit and are not claimed as
  physically exercised; generalized writes/persistence remain M6-006. Gate
  readiness: not ready. Active next task: M4-005. Evidence:
  `docs/evidence/M4-004-xam-profile.md`.

- 2026-08-12 — M4 checkpoint 5. Completed: M4-005 (5/13). Health:
  `GREEN — PHYSICAL SDL CONTROLLER CAUSALLY REACHES XBOX SLOT 0`. Evidence:
  ReXGlue v0.9.0.12 at `b44f69149c079f4c3b62503084449d7a7f5e4412`;
  final physical run `20260812-140305-f0bd8e1b`; one selected controller was
  assigned slot 0 and its SDL South/A down/up source sequences were observed as
  matching guest-visible `XamInputGetState` slot-0 edges. Query, success, and
  disconnected masks were `F/1/E`; removal, rejection, unexpected, failure,
  and drop counters were zero. Focused SDK tests pass 5 cases / 12 assertions;
  title ROI, controlled exit 0, process cleanup, and source-game/runtime-artifact
  integrity all pass. Full mappings, dead zones, focus, reconnect, multi-pad behavior, and
  vibration remain M4-006. Gate readiness: not ready. Active next task: M4-006.
  Evidence: `docs/evidence/M4-005-input-slot.md`.

- 2026-08-13 — M4 checkpoint 6. Completed: M4-006 (6/13). Health:
  `GREEN — SPLIT PHYSICAL CONTROLLER MATRIX VERIFIED`. Evidence: immutable
  digital run `20260812-212030-5fc01c73`, analog/focus run
  `20260813-124600-293c07b3`, and recovered controlled hotplug run
  `20260813-144406-2c1974da`; causal digital `F3FF`, analog `3FF`, focus
  lost/neutral/gained, remove/disconnect/reconnect/success, exact PASS summary,
  and controlled exit. ReXGlue v0.9.0.13 focused tests pass 13 cases / 90
  assertions; fixtures pass 72 continuation negatives plus two corruption
  negatives per immutable layer. The final run is recovered after a stale
  mapping-count verifier literal and does not claim the missing runtime artifact
  snapshot. Gate readiness: not ready. Active next task: M4-007. Evidence:
  `docs/evidence/M4-006-controller-matrix.md`.

- 2026-08-13 — M4 checkpoint 7. Completed: M4-007 (7/13). Health:
  `GREEN — SUSTAINED XMA/XAUDIO/SDL TITLE AUDIO ROUTE VERIFIED`. Evidence:
  ReXGlue v0.9.0.14 bounded audio telemetry; clean SDK/app build; focused
  5-case/16-assertion tests; 300 seconds with 60,594 nonzero guest submits,
  60,587 nonzero SDL device frames, 319,325 nonzero XMA frames, queue depth 8,
  and zero starvation, invalid samples, submit failures, or drops; followed by
  controlled `WM_CLOSE`, exit 0, and no force cleanup/orphan. Remaining scope includes XMP semantics,
  event-level Xenia comparison, human fidelity, gameplay audio, and long-session
  device-change stability. Gate readiness: not ready. Active next task: M4-008.
  Evidence: `docs/evidence/M4-007-audio-route.md`.

- 2026-08-13 — M4 checkpoint 8. Completed: M4-008 (8/13). Health:
  `GREEN — REACHED XMP STATUS ROUTE IS STATE-CORRECT`. Evidence: ReXGlue
  v0.9.0.15 at `6f2a0f36c153495711a5c66487064ed86f6bb614`; canonical clean-build run
  `20260813-182745-5b65003b`; focused 4-case/20-assertion SDK tests; 4,625
  known status queries with state continuously `Idle`; zero playback calls,
  state changes, unexpected/inconsistent calls, overflow, or drops; exact title
  capture, controlled `WM_CLOSE`, exit 0, no orphan, and physically reverified
  source-game/runtime integrity. Unsupported title playlist/output-capture calls
  now fail explicitly instead of advertising fake playback. No XMP decoder or
  user-selected music playback is claimed. Gate readiness: not ready. Active
  next task: M4-009. Evidence: `docs/evidence/M4-008-xmp-fallback.md`.

- 2026-08-13 — M4 checkpoint 9. Completed: M4-009 (9/13). Health:
  `GREEN — NETWORK-DISABLED OFFLINE TITLE ROUTE VERIFIED`. Evidence: ReXGlue
  v0.9.0.16 at `218b7750d4c848c9b35d371bf78dd6c26ab93398`; canonical clean-build run
  `20260813-193514-2bb7e7ca`; focused 3-case/32-assertion tests plus a clean
  6-case/42-assertion adjacent offline/XAM subset; verified title capture with
  the guest socket block enabled; zero reached XLiveBase dispatches, socket
  attempts, unexpected results, host socket creations, overflow, or drops;
  exact title/PID `WM_CLOSE`, exit 0, no orphan, and physically reverified
  source-game/runtime integrity. The ten known MCLA XLiveBase message/result
  semantics are unit-tested, but runtime reachability of those zero-hit paths,
  Xbox Live functionality, and a general host firewall are not claimed. Gate
  readiness: not ready. Active next task: M4-010. Evidence:
  `docs/evidence/M4-009-offline-services.md`.

- 2026-08-13 — M4 checkpoint 10. Completed: M4-010 (10/13). Health:
  `GREEN — LOCALE AND UNICODE PATH MATRIX VERIFIED`. Evidence: canonical
  exact-release clean-build run `20260813-213301-505ca1ca`; EN/US, FR/FR, and RU/RU each
  returned the selected XConfig language 25 times, reached a pinned title/logo
  and language-specific prompt gate, preserved exact Cyrillic/accented
  user/cache/log roots, exited 0 through `WM_CLOSE`, and retained source-game
  and runtime-artifact integrity. `XGetLanguage` and country were zero-hit at
  this title boundary and remain static/unit-proven rather than runtime-proven.
  Gate readiness: not ready. Active next task: M4-011. Evidence:
  `docs/evidence/M4-010-locale-paths.md`.

- 2026-08-13 — M4 checkpoint 11. Completed: M4-011 (11/13). Health:
  `GREEN — SAVED FRONTEND ROUTE REPEATABLE`. Evidence: canonical
  exact-release clean-build run `20260813-230502-1f0de720`; three isolated
  cycles used the pinned post-OOBE save and followed title `START` to free
  roam, gameplay `START` to pause, then `RB`, `RB` to Settings/Options. All
  three cycles produced four distinct 1280x720 captures, passed the title and
  menu-region gates, exited 0 through external `WM_CLOSE`, required no force
  cleanup, and preserved the seed, source-game tree, and runtime artifacts.
  No internal Exit command is claimed. First-run OOBE, races, detailed
  gameplay correctness, and frontend parity remain open. Gate readiness: not
  ready. Active next task: M4-012. Evidence:
  `docs/evidence/M4-011-frontend-smoke.md`.

- 2026-08-14 — M4 checkpoint 12. Completed: M4-012 (12/13). Health:
  `GREEN — TWO-RESOLUTION FRONTEND REGIONS AND AUDIO LIFECYCLE COMPARED`.
  Evidence: canonical clean-build run `20260814-003029-963c60bd`; all three
  accepted M4-011 1280x720 cycles reverified; one complete 2560x1440 scale-2
  title/gameplay/pause/Options route; stable ROI correlations 0.751092-0.946633;
  bounded pause registration -5/+1 pixels; reviewed private contact sheet;
  exact Xenia frame/log hashes; and M4-007 nonzero audio-route counters bound
  to three shared lifecycle classes. Exit 0 through external `WM_CLOSE`, no
  force cleanup/orphan, and source-game/save/runtime integrity all pass.
  Whole-frame equality and event-level audio identity remain explicitly
  unclaimed. Gate readiness: not ready. Active next task: M4-013. Evidence:
  `docs/evidence/M4-012-frontend-parity.md`.

- 2026-08-14 — M4 checkpoint 13. Completed: M4-013 (13/13). Health:
  `GREEN — 20 CONSECUTIVE SAVED FRONTEND ROUTES PASS`. Evidence: canonical
  clean-build run `20260814-023505-e69d76a2`; 20/20 isolated pinned-save routes
  reached active free roam, pause, and Settings/Options; pause ROI
  0.926480-0.932330 and Options ROI 0.984124-0.999391; external exact-window
  `WM_CLOSE`, exit 0, zero force cleanup/orphans, and unchanged source-game,
  seed, and four runtime-artifact identities in every cycle. The recovered
  aggregate SHA-256 is
  `2CF4BE3210C260181F7C2CF9C00D07667ED12D6CA644B4E06ECBF12D7A9B0A28`;
  recovery was required only because two valid shutdown-flush tails exposed
  verifier ordering assumptions after all 20 routes had already completed.
  The final gate physically reconstructed and reverified every log/capture/tree
  twice. All remaining frontend boundaries are severity/workaround/target
  classified. Decision: `GO M5 WITH PINNED SAVE`. Gate readiness: ready for
  final M4 closure review. Evidence:
  `docs/evidence/M4-013-known-deviations.md`.

- 2026-08-14 — M4 closure review. Completed: all 13 M4 tasks. Health:
  `GREEN`; decision: `GO M5 WITH PINNED SAVE`; release: `0.4.0.0`. Evidence:
  13/13 task/evidence coverage through `807fc9e`; project tests 42/42; clean
  ast-grep scan and 3/3 rule tests; bootstrap 12/12; public ReXGlue
  v0.9.0.18 branch/tag at exact commit
  `923c92d1d1cb721cb704ac603fba263a01ba06aa`; 20/20 M4-013 saved
  free-roam/pause/Settings routes with exit 0, no force cleanup/orphan, and
  unchanged source-game/save/runtime identities; scoped presentation,
  render-path, profile, one-pad input, audio, XMP, offline, locale, and
  two-resolution frontend evidence all revalidated. Closure review also fixed
  three stale test-only contracts: the runtime-discovered `0x82554080`
  function boundary, the current SDK gitlink, and historical 30,025 versus
  current 30,026 mapping-count evidence. First-run OOBE, race completion,
  general persistence, multi-pad physical behavior, title-driven force
  feedback, Bink playback, whole-frame parity, and broader backend paths remain
  explicitly owned by M5+. Closure evidence:
  `docs/evidence/M4-closure-review.md`.

## 13. Milestone M5 — First playable race vertical slice

**Goal:** complete one representative race from frontend selection through results and return to free roam/menu at stock timing.

**Status:** `ACTIVE — M5-011 SAVE/CONTENT PERSISTENCE PREREQUISITE VERIFIED`.

**Estimate:** 2–8 weeks.

### Prerequisites

- M4 closed
- A deterministic test save or new-game path is available
- Gameplay GPU and audio diagnostics enabled

### Tasks

- [x] **M5-001** Define the canonical first-race test route, vehicle, weather/time, controller, and expected outcome. The fail-closed `pinned-save-sunset-strip-race-v1` contract fixes the supported image/runtime, exact post-OOBE seed, white 1998 Nissan 240SX at dry night on Sunset Blvd, one default-layout SDL controller in slot 0, `Sunset Strip Race` against Trevor's 1975 Datsun 280Z, a required `1/2` finish, results, return to controllable free roam, and external `WM_CLOSE`. The seed plus three private stock-Xenia free-roam/race/pause frames are physically rehashed. Event identity is normative and must be calibrated from this seed before a native route can pass; persistence and whole-frame parity remain unclaimed. Evidence: `config/first-race-route.json`, `scripts/verify-first-race-route.ps1`, `scripts/test-first-race-route.ps1`, `docs/evidence/M5-001-first-race-route.md`.
- [x] **M5-002** Load world streaming data from RPF without missing-file or path-case failures. The native VFS preflight resolves lowercase, uppercase, and mixed-case cache-RPF paths to one entry. One clean isolated pinned-save route reaches gameplay/pause/Options and exits by external `WM_CLOSE`; private noisy I/O proves 8,011 cache reads (262,821,908 bytes, 999,707-ppm highest-end coverage) and 41 audlo reads (3,290,825 bytes, 999,149 ppm), with no archive failure and exactly seven allowlisted retail development-path misses. Physical result, game/save/runtime/capture hashes, focused VFS tests, and 30 fail-closed fixtures are bound by `scripts/run-world-streaming-smoke.ps1`, `scripts/verify-world-streaming-smoke.ps1`, and `scripts/test-world-streaming-smoke.ps1`. Evidence: `docs/evidence/M5-002-world-streaming.md`.
- [x] **M5-003** Validate road, building, vehicle, traffic, sky, shadow, particle, and HUD rendering. A default-off InitOnly saved-game rendering probe now records causal title/dismiss/camera/burnout input, thirty stationary one-second traffic samples, six labeled category frames, and a gameplay-timed frozen Xenos checkpoint. The accepted clean RelWithDebInfo route preserves the pinned save/game/runtime identities, externally exits through exact-window `WM_CLOSE`, and binds 36 native 1280x720 frames plus a private Xenia/native contact sheet. The owner gave a visual PASS for all eight categories; automated evidence selects a clear AI-traffic frame, detects night-camera and particle-burst temporal changes, and records 332/332 successful PSOs, 6,195,685 draws, 39/39 render targets, and 93,261/93,261 common-copy resolves with no renderer failures. This is a bounded night free-roam slice, not whole-frame parity, every city/time/weather condition, ROV/PWL/true-direct coverage, or a diagnosis of KI-013. Evidence: `scripts/run-rendering-smoke.ps1`, `scripts/verify-rendering-smoke.ps1`, `scripts/test-rendering-smoke.ps1`, `docs/evidence/M5-003-rendering-categories.md`.
- [x] **M5-004** Fix or bound required EDRAM/ROV-like ordering behavior and depth restore issues. The accepted `host-rtv-ordering-depth-bounded-s2` report reuses and physically re-verifies the immutable M5-003 saved-gameplay route rather than inventing an unobserved defect. That route covers all eight ownership modes across 47 first-seen sample-transfer records, 536,929 ownership draws, 19,814 host-depth stores, 17 host-bound depth tuples, 1x/2x/4x depth resolves, and more than 5.7 million depth-writing draws with zero render-target/resolve/device failure. The owner-approved gameplay frames show coherent world/shadow/category rendering. A behavior patch is not justified for the reached host-RTV path; ROV/interlock, trace-player snapshot restoration, PWL gamma, and true-direct resolve remain explicitly unexercised S2 scope rather than being claimed fixed. Evidence: `scripts/run-edram-depth-report.ps1`, `scripts/verify-edram-depth-report.ps1`, `scripts/test-edram-depth-report.ps1`, `docs/evidence/M5-004-edram-depth-report.md`.
- [x] **M5-005** Validate shader translation and texture tiling/deswizzling for representative materials. The accepted `representative-material-pipeline-pass` report physically re-verifies the immutable M5-003 saved-gameplay route, its owner-approved eight-category contact sheet, and the complete private runtime log set. The reached shader path completes 163 vertex and 197 pixel translations with zero translation failures, records 256 bounded successful shader variants plus 104 explicit overflowed records, and creates 332/332 unique successful PSOs. The successful post-load texture trace contains 122,830 loads: 122,823 tiled and 7 linear, 35,085 packed-mip and 87,745 unpacked-mip, across nine representative formats and 45 dimension classes, with no invalid-fetch, load, device-loss, or renderer failure. The source audit binds each `Loaded` marker to successful texture loading and the D3D12 tiled path to dispatch, texture copy, and success ordering. Raw addresses/logs remain private; this does not claim every guest format, whole-frame parity, every material/weather/location, or a behavior patch. Evidence: `scripts/run-material-pipeline-report.ps1`, `scripts/verify-material-pipeline-report.ps1`, `scripts/test-material-pipeline-report.ps1`, `docs/evidence/M5-005-material-pipeline.md`.
- [x] **M5-006** Implement gameplay input, analog steering/throttle/brake, pause, and controller reconnect. The accepted `saved-gameplay-input-pass` route clean-builds the current host, loads the pinned post-OOBE save, and causally observes 24 source/guest records for title Start, full throttle, full brake, full-left and full-right steering under partial throttle, neutral releases, and gameplay pause. Eight 1280x720 frames prove the saved route reaches controllable free roam, visibly responds to throttle/brake/steering, and opens the pause UI; accepted sampled frame differences are 158,995, 141,223, and 143,056, with 613,358-ppm pause ROI correlation. The final gate also physically re-verifies the immutable M4-006 SDL digital/analog/focus/disconnect/reconnect evidence and confirms current SDK input sources are byte-identical to that accepted implementation. The source game, pinned save, clean-build artifacts, runtime logs, captures, and process lifecycle are hash-bound and unchanged. Scope is one default-layout controller and saved free roam; race maneuver parity, multi-pad behavior, and force feedback remain unclaimed. Evidence: `scripts/run-gameplay-input-smoke.ps1`, `scripts/verify-gameplay-input-smoke.ps1`, `scripts/test-gameplay-input-smoke.ps1`, `docs/evidence/M5-006-gameplay-input.md`.
- [x] **M5-007** Implement or validate force-feedback calls without blocking gameplay when unsupported. The accepted `ffb-withheld-host-rumble-bounded` report proves the advanced Xbox 360 `XInputdFF*` surface remains eight explicit stubs and is deliberately not advertised through `X_INPUT_CAPS_FFB_SUPPORTED`, preventing the title from entering an unsupported path. The current M5-006 saved-gameplay route resolves all eight imports for module loading but emits zero stub-call markers, delivers 24 gameplay-input records, opens pause, and exits cleanly. Basic vibration remains a concrete `XamInputSetState -> InputSystem -> SDL_RumbleGamepad` path; immutable M4-006 evidence contains six successful, supported LEFT/stop, RIGHT/stop, and BOTH/stop host commands, while the owner report of feeling those patterns remains explicitly external and not machine-verified. Physical disconnect returns `0000048F`, reconnect succeeds, and NOP no longer masks the result. This is a bounded no-block decision, not a claim that title-driven force-feedback effects are implemented. Evidence: `scripts/run-force-feedback-report.ps1`, `scripts/verify-force-feedback-report.ps1`, `scripts/test-force-feedback-report.ps1`, `docs/evidence/M5-007-force-feedback.md`.
- [x] **M5-008** Validate vehicle physics timing and guest synchronization at stock 30 FPS. The accepted `stock-30-fixed-step-and-real-time-throughput-pass` gate uses an optimized Release build and three isolated pinned-save gameplay cycles. The exact stock timer at `0x821BDA90` executes 300/300/300 times over 10.000/10.000/10.001 seconds; all sampled effective and clamped deltas are the exact `3D088889` float (33,333 microseconds), simulated-time/wall ratios are 999,990/999,990/999,939 ppm, the 50-MHz guest clock stays at 1,000,002 ppm, and guest vblank remains 600 per sample at 59,996-60,000 mHz. Guest-output publication is 300/300/299 frames, or 29,898-30,000 mHz, with visible throttle-response differences of 152,775-172,175 sampled pixels. All cycles exit 0 by exact external `WM_CLOSE` with unchanged game/save/runtime identities. Calibration also proved that RelWithDebInfo reaches only about 20 Hz on this host; it remains valid for correctness diagnostics but is not used as a performance baseline. Evidence: `scripts/run-physics-timing-smoke.ps1`, `scripts/verify-physics-timing-smoke.ps1`, `scripts/test-physics-timing-smoke.ps1`, `docs/evidence/M5-008-physics-timing.md`.
- [x] **M5-009** Validate engine, collision, ambient, voice, UI, and music audio streams. ReXGlue v0.9.0.20 adds a default-off, InitOnly, ordered, privacy-safe SDL device-output event audit. One clean RelWithDebInfo pinned-save route produced nonzero output in all six bounded windows with zero invalid frames or submission failures; the current XMA/XAudio/SDL route stayed healthy at max queue depth eight and zero starvation. The owner confirmed the presence of all six named classes, with unrelated simultaneous sounds explicitly allowed. The process exited 0 through external `WM_CLOSE` with unchanged source game, save, and four runtime artifacts. This proves bounded presence, not isolation, mix balance, exact asset identity, exhaustive event coverage, XMP playback, or long-session stability. Evidence: `scripts/run-audio-event-smoke.ps1`, `scripts/verify-audio-event-smoke.ps1`, `scripts/test-audio-event-smoke.ps1`, `docs/evidence/M5-009-audio-event-matrix.md`.
- [x] **M5-010** Fix or bound streaming-thread, file-I/O, and allocator failures encountered during the route. No target failure is reproduced across five immutable accepted processes: one noisy world-streaming run, three stock-speed gameplay cycles, and one longer audio/gameplay run, all with controlled external exit. The noisy trace has 8,054 successful reads plus one valid async-pending read, 87/87 virtual allocations, 49/49 frees, and 2,590/2,590 physical allocations, with zero failures. Cache/audlo archive coverage remains 8,011/41 reads, and only the exact seven allowlisted retail development-path misses occur per process. A behavior patch is therefore not justified; long-session leaks, all-region streaming, and repeated complete races remain explicitly open. Evidence: `scripts/run-route-failure-report.ps1`, `scripts/verify-route-failure-report.ps1`, `scripts/test-route-failure-report.ps1`, `docs/evidence/M5-010-route-failure-report.md`.
- [x] **M5-011** Implement required save/content APIs to persist race results. The exact imported content surface (`GetDeviceData`, `GetDeviceState`, `CreateEnumerator`, `CreateEx`, and `Close`) plus the title's twelve direct `NtWriteFile` sites already resolve to concrete SDK implementations. Immutable M5-002 evidence physically proves enumeration, writable `save0:` mount/open, three successful reads, and clean unmount of the pinned save. ReXGlue v0.9.0.21 now makes new saved-content creation fail closed: a failed or incomplete header write removes the partial header, rolls back the mount/package, returns the failure, and reports an unknown disposition instead of false success. Focused 2-case/16-assertion tests prove saved-game metadata survives reconstruction of `ContentManager`, remains enumerable, and rejects a truncated header. This closes the persistence API prerequisite only; no native race-result write has yet been reached, and restart persistence of an actual result remains M5-012 acceptance. Evidence: `scripts/run-save-content-contract.ps1`, `scripts/verify-save-content-contract.ps1`, `scripts/test-save-content-contract.ps1`, `docs/evidence/M5-011-save-content-contract.md`.
- [x] **M5-012** Complete race-results transition and return path. Four exact runtime-discovered PPC entry points were independently bounded, added to non-force code generation, and exercised by the continued native route. The owner completed the entire available Ian multi-event series, reached final rewards/results, returned to controllable free roam, and closed externally without a fatal marker. The route runner observed the result-window save change; the exact completed save (`711B9430...5021`) then loaded in a fresh optimized Release process, produced distinct gameplay frames, and sustained 300 presents / 600 vblanks over ten seconds at 30/60 Hz with a 999,990-ppm simulated-time/wall ratio. The accepted result binds the completed route tree, restart tree, save, captures, logs, and four Release artifacts. This is one full series plus restart, not five repeated races or a leak claim; those remain M5-013. Evidence: `scripts/run-race-results-smoke.ps1`, `scripts/verify-race-results-smoke.ps1`, `scripts/test-race-results-smoke.ps1`, `scripts/run-race-restart-smoke.ps1`, `scripts/verify-race-restart-smoke.ps1`, `scripts/test-race-restart-smoke.ps1`, `docs/evidence/M5-012-race-results.md`.
- [x] **M5-013** Run memory, handle, thread, and GPU resource leak checks across repeated races. One clean optimized Release process completed five owner-confirmed race events with five distinct 1280x720 checkpoints and controlled external exit. From checkpoint 1 through 5, peak growth was 206,409,728 private bytes, 196,239,360 working-set bytes, 14 handles, two threads, 50,446,336 dedicated-GPU bytes, and 17,563,648 shared-GPU bytes, all within the declared bounds. A runtime-discovered nine-instruction series-transition leaf at `[0x8220B810,0x8220B834)` was privately audited, non-force generated, registered, compiled, and traversed by the accepted rerun. Windows counter-status retries and completed-run finalization are fixture-covered. This is a five-race bound, not lifetime leak freedom or HUD temporal parity; intermittent minimap flicker is tracked as KI-016. Evidence: `scripts/run-race-resource-smoke.ps1`, `scripts/verify-race-resource-smoke.ps1`, `scripts/test-race-resource-smoke.ps1`, `docs/evidence/M5-013-race-resource-growth.md`.
- [x] **M5-014** Add a vertical-slice regression checklist and sanitized evidence report. The final gate revalidated the pinned M5-001 route artifact, all eleven immutable M5-002 through M5-012 accepted results, and the dynamic physical M5-013 result, then emitted and byte-normalization-bound a privacy-safe 14-task public checklist. The report declares `GO M6 — FIRST PLAYABLE RACE VERTICAL SLICE` while explicitly excluding first-run/OOBE, broad race/content coverage, multi-pad, advanced title-driven force feedback, whole-frame console parity, every graphics backend, and general leak freedom. One positive report fixture, five fail-closed negatives, and 28 source-contract checks pass. Evidence: `scripts/run-vertical-slice-report.ps1`, `scripts/verify-vertical-slice-report.ps1`, `scripts/test-vertical-slice-report.ps1`, `docs/evidence/M5-014-vertical-slice-report.md`.

### Deliverables

- one complete playable race
- stock-speed deterministic physics baseline
- representative GPU/audio/streaming coverage
- persisted result or documented temporary save limitation

### Verification evidence

- complete one full available event series through final results and controllable free roam
- load its changed save in a fresh optimized process and reach controllable gameplay
- no S0/S1 defects or fatal validation errors
- frame pacing and game speed remain consistent with the stock Xenia baseline
- memory/resource growth remains bounded across five repetitions

### Exit criteria

- A user can start, play, finish, and leave the test race without debugger intervention.
- HUD, world, vehicle, input, and core audio are usable.
- Race outcome persists or the remaining save blocker has an approved M6 plan.

### Gate

`GO M6` requires an end-to-end gameplay loop, not merely controllable free roam.

### Checkpoint log

- 2026-08-14 — M5 checkpoint 1. Completed: M5-001; total 1/14. Health: `GREEN — CANONICAL FIRST-RACE ROUTE DEFINED, NATIVE EVENT CALIBRATION PENDING`. Evidence: exact image/runtime, pinned seed, three private Xenia state frames, fixed vehicle/night/dry/controller/event/action/outcome contract, fail-closed physical verifier and fixtures. Scope remains honest: the event identity is normative rather than native-proven, and no race completion or persistence is claimed. Next task: M5-002.
- 2026-08-14 — M5 checkpoint 2. Completed: M5-002; total 2/14. Health: `GREEN — PINNED-SAVE WORLD RPF STREAMING VERIFIED`. Evidence: clean native title-to-saved-gameplay route, mixed-case VFS preflight, 8,011 cache and 41 audlo reads with near-end archive coverage, exactly seven allowlisted retail development-path misses, zero unexpected archive failures/fatal markers, external `WM_CLOSE`, physical game/save/runtime/log/capture binding, focused VFS tests, and 30 fail-closed fixtures. Scope is the canonical saved free-roam route; all-region, first-run, race, and long-session streaming remain open. Next task: M5-003.
- 2026-08-14 — M5 checkpoint 3. Completed: M5-003; total 3/14. Health: `GREEN — SAVED FREE-ROAM RENDERING CATEGORIES VERIFIED`. Evidence: one clean isolated 1280x720 title-to-saved-gameplay route; 30 hash-bound stationary traffic samples plus world/sky/street and three particle frames; clear native AI traffic and burnout dust/smoke; owner visual PASS for road, buildings, player vehicle, traffic, night sky, shadows, particles, and HUD; 332 successful PSOs, 6.20 million draws, active depth/MSAA/non-identity gamma, and 93,261 successful staged common-copy resolves; external `WM_CLOSE`, no force cleanup/orphan, unchanged game/save/runtime identities; one fixture positive, 61 fail-closed negatives, 23 source checks, clean ast-grep 3/3, and bootstrap 12/12. The category gate did not claim reflection parity; on 2026-08-17 a fixed-camera gameplay repro reclassified KI-013 as an open S2 saturated local-light/specular defect rather than broad shadow corruption. Next task: M5-004.
- 2026-08-14 — M5 checkpoint 4. Completed: M5-004; total 4/14. Health: `GREEN — HOST-RTV EDRAM/DEPTH ORDERING BOUNDED, NO REACHED DEFECT`. Evidence: immutable M5-003 result reverified against source game, save, runtime artifacts, logs, captures, and owner verdict; 47/47 ownership records with all mode IDs 0-7, 536,929 ownership draws, 19,814 host-depth-store dispatches ordered before ownership transfers, 17 host-bound depth tuples, depth resolves at 1x/2x/4x, 5.89 million depth-tested and 5.77 million depth-writing draws, and 93,261 successful resolves with zero relevant failures. The 1,599 draws carrying depth/stencil state without a host depth target are recorded and bounded rather than hidden. ROV/interlock and trace-player snapshot restoration remain unexercised S2 scope; no speculative renderer patch was made. Next task: M5-005.
- 2026-08-14 — M5 checkpoint 5. Completed: M5-005; total 5/14. Health: `GREEN — REPRESENTATIVE SHADER/TEXTURE PIPELINE VERIFIED`. Evidence: immutable M5-003 physical result and all eight owner-approved visual categories reverified; 163 vertex plus 197 pixel translations with zero failures; 256 bounded successful shader records with 104 explicit overflow records; 332/332 unique successful PSOs; 122,830 successful texture loads, including 122,823 tiled, nine representative formats, and 45 dimension classes; source ordering binds successful `Loaded` events to the D3D12 tiled dispatch/copy path; one fixture positive, 14 fail-closed negatives, and seven source checks. Raw texture addresses/logs remain private, and all-format/whole-frame equivalence is not claimed. Next task: M5-006.
- 2026-08-14 — M5 checkpoint 6. Completed: M5-006; total 6/14. Health: `GREEN — SAVED-GAMEPLAY INPUT RESPONSE AND PHYSICAL RECONNECT VERIFIED`. Evidence: current clean build and pinned-save route with 24 exact source/guest gameplay-input records, six bounded overlay-dismiss pulses, eight 1280x720 captures, accepted throttle/brake/steering sampled differences of 158,995/141,223/143,056, 613,358-ppm pause correlation, controlled external close, unchanged source game/save/runtime artifacts, and physical re-verification of the immutable M4-006 SDL digital/analog/focus/disconnect/reconnect matrix. Current SDK input sources are byte-identical to the accepted M4 implementation. Fixture coverage is one positive plus 21 fail-closed negatives. Saved free roam and one default-layout controller are covered; race maneuver parity, multi-pad behavior, and force feedback remain open. Next task: M5-007.
- 2026-08-14 — M5 checkpoint 7. Completed: M5-007; total 7/14. Health: `GREEN — UNSUPPORTED ADVANCED FFB WITHHELD, BASIC HOST RUMBLE BOUNDED`. Evidence: all eight `XInputdFF*` imports resolve but remain unadvertised stubs; current saved gameplay emits zero stub-call markers and completes input/pause/controlled-exit evidence; `XamInputSetState` and SDL rumble are concrete; six immutable supported host commands cover LEFT/RIGHT/BOTH start/stop; physical disconnect/reconnect remains visible without NOP masking; one fixture positive, 13 fail-closed negatives, and eight source checks pass. The owner's physical sensation report is external rather than run-recorded or machine-verified. Title-driven force-feedback effects are not claimed. Next task: M5-008.
- 2026-08-14 — M5 checkpoint 8. Completed: M5-008; total 8/14. Health: `GREEN — RELEASE STOCK 30-FPS FIXED STEP AND REAL-TIME THROUGHPUT VERIFIED`. Evidence: three isolated pinned-save Release cycles each record 300 exact 33,333-microsecond timer updates over approximately 10 seconds; simulated-time/wall and guest-clock ratios remain within 61 ppm and 2 ppm of real time respectively; vblank is 59.996-60.000 Hz; guest output is 29.898-30.000 FPS; all six captures are canonical and each start/end pair differs by at least 152,775 sampled pixels; all cycles exit 0 by external `WM_CLOSE` with unchanged data. RelWithDebInfo calibration is explicitly excluded from performance claims because it reaches only about 20 FPS on the same route. One physical fixture positive, 20 fail-closed negatives, 18 source checks, 42/42 focused SDK cases with 216 assertions, and ast-grep 3/3 pass. Next task: M5-009.
- 2026-08-14 — M5 checkpoint 9. Completed: M5-009; total 9/14. Health: `GREEN — SIX REQUIRED AUDIO CLASSES MACHINE-PRESENT AND OWNER-HEARD`. Evidence: one clean RelWithDebInfo pinned-save route with exact ordered music/ambient/voice/engine/collision/UI windows; 1,000,000-ppm nonzero SDL output in every window; per-class peaks 3,635-12,412 ppm; zero invalid frames or submission failures; 351,604 XMA frames, 34,572 guest submits, 34,564 SDL device frames, max queue depth eight, and zero starvation. The owner gave the exact presence-only six-class confirmation, unrelated simultaneous sounds were allowed, and the process exited 0 through external `WM_CLOSE` with unchanged game/save/runtime identities. One fixture positive, 32 fail-closed negatives, 25 source checks, and focused SDK 8-case/30-assertion tests pass. Claims are limited to bounded presence; mix/isolation/content fidelity and long-session/device-transition stability remain open. Next task: M5-010.
- 2026-08-14 — M5 checkpoint 10. Completed: M5-010; total 10/14. Health: `GREEN — FIVE-PROCESS STREAMING/I/O/ALLOCATOR ROUTE BOUNDED WITHOUT A TARGET FAILURE`. Evidence: one noisy world-streaming process, three stock-speed gameplay processes, and one longer audio/gameplay process, all controlled exits; 8,055 read results (8,054 success plus one valid async pending), 87/87 virtual allocations, 49/49 frees, 2,590/2,590 physical allocations, zero allocator/read failures, 8,011 cache and 41 audlo archive reads, and only seven exact allowlisted retail development-path misses per process. One positive, 26 fail-closed negatives, seven source checks, and bootstrap 12/12 pass; the bootstrap rerun also hardened StrictMode native exit-code initialization in the toolchain resolver. No behavior patch is justified; leak checks, all-region coverage, and repeated complete races remain open. Next task: M5-011.
- 2026-08-14 — M5 checkpoint 11. Completed: M5-011; total 11/14. Health: `GREEN — SAVE/CONTENT API PREREQUISITE DURABLE AND FAIL-CLOSED`. Evidence: the exact five imported XAM content APIs and twelve direct `NtWriteFile` sites are concrete; immutable M5-002 physically proves existing-save enumeration, writable mount/open/read/unmount; ReXGlue v0.9.0.21 propagates header-write failure and rolls back partial creation; focused restart/list/truncation coverage passes 2 cases and 16 assertions; one fixture positive, 12 fail-closed negatives, 15 source checks, clean ast-grep 3/3, and bootstrap 12/12 pass. This is implementation/prerequisite evidence, not a claim that a native race result has already been written or survived restart. Next task: M5-012.
- 2026-08-17 — M5 checkpoint 12. Completed: M5-012; total 12/14. Health: `GREEN — FULL IAN SERIES, RESULTS RETURN, AND RELEASE RESTART VERIFIED`. Evidence: four minimal runtime-discovered function intervals pass non-force generation/registration review; one operator-driven Ian multi-event series reaches the two-car start, completes every `NEXT RACE` event, shows final rewards/results, returns to controllable free roam, and exits by external `WM_CLOSE`; the exact completed save then reloads in a fresh Release process with 300 presents, 600 vblanks, exact 33,333-microsecond fixed steps, 999,990-ppm simulated-time/wall ratio, and distinct start/end frames. The accepted private result binds route/restart trees, save, logs, captures, SDK identity, and Release artifacts. Route/restart fixtures pass two positives and 12 fail-closed negatives; repeated-race resource growth remains M5-013. Next task: M5-013.
- 2026-08-17 — M5 checkpoint 13. Completed: M5-013; total 13/14. Health: `GREEN — FIVE-RACE RELEASE RESOURCE GROWTH BOUNDED`. Evidence: one optimized process completes five owner-confirmed race events, emits five distinct ordered captures, and exits 0 externally; checkpoint-1-to-5 peak growth remains bounded at 197 MiB private, 187 MiB working set, 14 handles, two threads, 48 MiB dedicated GPU, and 17 MiB shared GPU. The accepted result rebinds six numeric samples, logs, captures, save shape, build, runtime artifacts, and evidence tree. A new exact `[0x8220B810,0x8220B834)` runtime leaf repairs the reached multi-event transition. Five positive fixtures, fourteen fail-closed negatives, counter-status retry/self-test coverage, completed-run recovery, ast-grep 3/3, and bootstrap 12/12 pass. KI-016 records non-blocking intermittent minimap flicker observed during the session. Next task: M5-014.
- 2026-08-17 — M5 checkpoint 14. Completed: M5-014; total 14/14. Health: `GREEN — SANITIZED VERTICAL-SLICE REPORT COMPLETE, GO M6`. Evidence: the final report gate rehashes the pinned M5-001 route, eleven immutable M5-002 through M5-012 results, and the accepted dynamic M5-013 result; all 14 public evidence documents and accepted decisions are linked without private paths, saves, logs, captures, controller identity, proprietary content, or raw digests, and the tracked report is normalized-content-bound to the accepted private report. One positive, five fail-closed negatives, and 28 source checks pass. Milestone closure review and the `0.5.0.0` release gate follow.
- 2026-08-17 — M5 closure review. Completed: all 14 M5 tasks. Health: `GREEN`; decision: `GO M6 — FIRST PLAYABLE RACE VERTICAL SLICE`; release: `0.5.0.0`. Evidence: task commits through `a596f5d`; the pinned save enters a complete Ian event series, reaches final results, returns to controllable free roam, and reloads the changed save in a fresh Release process; stock timing is 30 FPS / 60 Hz; input, six-class audio, world streaming, representative rendering/material/depth paths, save/content, and controlled external close are bounded; five repeated race completions remain within the declared host/GPU resource limits. The final closure regression passes 18/18 M5/config/generated gates, ast-grep 3/3, and bootstrap 12/12. No S0/S1 issue remains in the accepted slice. Open S2 visual defects KI-013, KI-015, and KI-016, first-run/OOBE, broader content, and general long-session fidelity remain M6 scope. Closure evidence: `docs/evidence/M5-closure-review.md`.

## 14. Milestone M6 — Core systems and repeatable gameplay alpha

**Goal:** make all major single-player systems reliable enough for extended sessions and broad content testing.

**Estimate:** 1–3 months.

### Prerequisites

- M5 closed
- Canonical vertical slice automated or precisely documented

### Tasks

- [x] **M6-001** Stabilize free-roam world streaming across all major city regions. One continuous clean Release process used the completed M5 save to cross eight project-defined coverage zones and return to Hollywood/Sunset, with nine distinct operator-confirmed full-GPS captures, monotonic guest-present sequences, 4,920,118,313 bytes of process-read growth, and bounded peak growth of 264,323,072 private bytes, 183,648,256 working-set bytes, seven handles, and two host threads relative to the first regional checkpoint. The process exited cleanly through external `WM_CLOSE`; the persisted result rebinds the M5-002 archive prerequisite, exact save/header, Release artifacts, runtime log, numeric samples, captures, and evidence tree. The route proves broad geographic streaming coverage, not every road, event, collectible, or campaign item. Evidence: `scripts/run-city-streaming-smoke.ps1`, `scripts/verify-city-streaming-smoke.ps1`, `scripts/test-city-streaming-smoke.ps1`, `config/city-streaming-route.json`, `docs/evidence/M6-001-city-streaming.md`.
- [x] **M6-002** Validate garage, vehicle purchase, customization, tuning, paint, and vehicle switching. One clean Release build drove two isolated processes entirely through deterministic synthetic slot-0 input: 69/69 source-to-guest control pulses entered saved gameplay and the garage, purchased the unlocked 1983 Golf/GTI, one representative exterior item, the 5Zigen exhaust item, paint-shop access and dark-blue paint, switched vehicles, returned to free roam, then repeated the garage route in a fresh process. Twelve private captures visually confirm the purchases and persisted blue Golf/exterior item; the changed save, byte-exact profile handoff, two controlled external closes, logs, artifacts, and evidence tree revalidate. Scope is one representative affordable lifecycle, not the exhaustive M7-004 category matrix. Evidence: `scripts/run-garage-lifecycle-smoke.ps1`, `scripts/verify-garage-lifecycle-smoke.ps1`, `scripts/test-garage-lifecycle-smoke.ps1`, `config/garage-lifecycle-route.json`, `docs/evidence/M6-002-garage-lifecycle.md`.
- [x] **M6-003** Validate race types, opponents, traffic, police, checkpoints, finish conditions, and rewards. One current Martin head-to-head event reached a visually verified two-car start and stable reward screen, invoked the audited `Race_Finish` wrapper once, and was owner-confirmed complete with no police observed. Immutable accepted evidence additionally binds a full Ian multi-event series, five consecutive race rewards/resource checkpoints, traffic activity, and an earlier owner report of police presence. The retail Martin route produced zero optional description/checkpoint/UI-result wrapper calls; that telemetry gap is explicitly preserved and not claimed as machine coverage. The original event process was force-cleaned after its post-reward strict-summary timeout, while a separate same-binary lifecycle cycle reached a nontrivial frame and exited 0 through external `WM_CLOSE`. Evidence: `scripts/run-race-system-smoke.ps1`, `scripts/verify-race-system-smoke.ps1`, `scripts/test-race-system-smoke.ps1`, `docs/evidence/M6-003-race-system-matrix.md`.
- [x] **M6-004** Validate day/night, weather, reflections, post-processing, motion blur, shadows, and particle systems. One clean Release process autonomously compared saved dry-night free roam with an Arcade `RAINY` / `DAWN` race, using 72 causal frontend records and eight movement/particle records to produce six strictly ordered 1280x720 captures. The menu frame physically shows the selected conditions; gameplay frames show dense rain, wet-road reflections, changed ambient light, movement/stopped states, and the race particle effect, with 30,778-45,458 differing sampled pixels across the four declared comparisons. The result rebinds prior night/shadow/particle, sunset-to-night, race-motion, and five-race evidence and exits through external `WM_CLOSE`. KI-013 colored vehicle reflections, KI-015 alpha stipple/shimmer, and KI-016 intermittent minimap flicker remain explicitly open S2 defects; no ROV, every-condition, indefinite HUD, or whole-frame console parity claim is made. Evidence: `scripts/run-environment-effects-smoke.ps1`, `scripts/verify-environment-effects-smoke.ps1`, `scripts/test-environment-effects-smoke.ps1`, `docs/evidence/M6-004-environment-effects.md`.
- [x] **M6-005** Complete save creation, autosave, manual save where applicable, load, overwrite, corruption handling, and storage-full behavior. Immutable M6-002 native evidence physically proves autosave overwrite, exact handoff, fresh-process load, and controlled exits; clean-save creation remains grounded in the stock-Xenia baseline and an isolated SDK creation/restart roundtrip rather than being mislabeled as native title evidence. ReXGlue v0.9.0.23 adds fail-closed saved-game transactions: interrupted overwrites restore the prior complete save, interrupted new saves remain hidden and are removed, recovery is restart-idempotent, committed replacements survive stale backups, malformed metadata is omitted, and an InitOnly dummy-HDD capacity boundary rejects create/overwrite before mutation. A clean SDK/Release build and focused 10-case/117-assertion isolated suite pass; the canonical HANGOUT save/header are byte-identical before and after. This title route exposes autosave rather than a manual-save command, so manual save is explicitly not applicable; first-run OOBE and generalized profile persistence remain outside this claim. Evidence: `scripts/run-save-matrix-smoke.ps1`, `scripts/verify-save-matrix-smoke.ps1`, `scripts/test-save-matrix-smoke.ps1`, `docs/evidence/M6-005-save-matrix.md`.
- [x] **M6-006** Validate profile, settings, controller preferences, and language persistence. ReXGlue v0.9.0.24 persists allowlisted standard XAM scalar and Unicode profile settings under the global local-user root with typed metadata, size bounds, backup recovery, and fail-closed whole-batch write validation; title-specific binary settings remain isolated under the existing per-title root. A clean SDK/Release build passed 7 profile cases / 40 assertions and 26 CVar cases / 128 assertions, while the canonical HANGOUT save/header remained byte-identical. Immutable M4-004, M4-010, M5-006, and M6-005 results bind the physically reached local-profile, language, controller, and save routes. MCLA does not import `XamUserWriteProfileSettings`, so native title writes, zero-hit profile reads, first-run OOBE, account selection, multiple profiles, and general host configuration are explicitly not claimed. Evidence: `scripts/run-profile-settings-smoke.ps1`, `scripts/verify-profile-settings-smoke.ps1`, `scripts/test-profile-settings-smoke.ps1`, `docs/evidence/M6-006-profile-settings.md`.
- [x] **M6-007** Stabilize XMA/XAUD/XMP audio for long sessions, stream transitions, pause/resume, and device changes. Exact ReXGlue v0.9.0.25 adds real SDL pause/resume and privacy-safe default-endpoint migration/recovery telemetry. One controlled title process sustained 7,200 seconds with 1,356,394 submitted, 1,356,386 device, and 15,057,615 XMA frames; maximum queue depth was eight, invalid/submission/starvation counters were zero, and thirteen samples bounded peak growth below 1 GiB private, 512 MiB working set, 128 handles, and 16 threads. A separate same-artifact process completed two pause/resume cycles and a real Windows default-playback-device migration with recovered nonzero output and owner-heard confirmation; both exited 0 through external `WM_CLOSE`. Immutable M4-007/M4-008/M5-009/M5-013 results bind prior stream/event routes. The split result does not claim a monolithic soak, exact music/ambient mix, XMP decoding, or selected system music. Evidence: `scripts/run-audio-stability-smoke.ps1`, `scripts/verify-audio-stability-smoke.ps1`, `scripts/test-audio-stability-smoke.ps1`, `docs/evidence/M6-007-audio-stability.md`.
- [x] **M6-008** Implement offline-safe behavior for achievements, presence, leaderboards, Rate My Ride, Driving Test unlocks, voice, and unavailable services. ReXGlue v0.9.0.26 replaces the reached return-bearing achievements-UI, stats-enumerator, and voice-submit stubs with deterministic offline results, including a bounded valid empty leaderboard enumerator. A six-row policy keeps achievements and Driving Test progress local while presence, Rate My Ride, and voice remain unavailable. Clean RelWithDebInfo/Release SDK builds, a clean Release host build, and 15 focused cases / 107 assertions pass; immutable M4-004/M4-009/M5-012/M6-003/M6-005 results bind the reached local profile, offline frontend, race progression, and save/restart routes. The build-only result explicitly does not claim current physical calls to every export, guide UI, Live presence, leaderboard rows, Rate My Ride backend, voice transport, exact Driving Test unlock reach, or service-vehicle unlocks. Evidence: `scripts/run-offline-progression-report.ps1`, `scripts/verify-offline-progression-report.ps1`, `scripts/test-offline-progression-report.ps1`, `docs/evidence/M6-008-offline-progression.md`.
- [x] **M6-009** Decide project policy for retired-service unlocks such as the Audi R8; keep gameplay fixes separate from cheats. The approved canonical policy preserves retail locks and any authentic entitlement already present, never fabricates retired-service completion, and never mutates the save or source game. Rockstar identifies Rate My Ride and Driving Test unlocks as retired; contemporary documentation identifies the Audi R8 purchase gate as all twelve Social Club tasks. Because that gate is a retired entitlement rather than a broken local/on-disc contract, the canonical compatibility route does not grant it. A future explicit default-off InitOnly cheat is permitted as a separately named, non-persistent convenience surface, but is not implemented and is excluded from canonical progression/achievement evidence. Evidence: `config/retired-service-unlock-policy.json`, `scripts/run-retired-service-unlock-policy.ps1`, `scripts/verify-retired-service-unlock-policy.ps1`, `scripts/test-retired-service-unlock-policy.ps1`, `docs/evidence/M6-009-retired-service-unlock-policy.md`.
- [x] **M6-010** Validate pause, suspend-like focus loss, minimization, controller hot-plug, and clean exit. A six-row split physical matrix rebinds accepted gameplay pause, SDL focus neutralization/recovery, controller disconnect/reconnect, audio pause/default-device recovery, and twenty controlled external-close cycles. One new autonomous clean Release cycle minimized the exact title window for 5,025 ms, restored it to a distinct nontrivial 1280x720 capture, minimized it again, and exited 0 through external `WM_CLOSE` in 571 ms with no force cleanup, fatal/device-loss marker, source-game/save-tree/runtime-artifact drift, or orphan process. Scope excludes OS suspend/hibernate, graceful subsystem teardown, display-adapter removal, multi-controller identity, and a monolithic all-rows run. Evidence: `config/window-device-lifecycle-matrix.json`, `scripts/run-window-device-lifecycle-smoke.ps1`, `scripts/verify-window-device-lifecycle-smoke.ps1`, `scripts/test-window-device-lifecycle-smoke.ps1`, `docs/evidence/M6-010-window-device-lifecycle.md`.
- [x] **M6-011** Add host configuration for resolution, window mode, audio device, volume, controller, logging, and data root. ReXGlue v0.9.0.27 adds restart-safe exact-name/default SDL playback-device selection and bounded host output volume, with unavailable named devices failing initialization instead of silently changing outputs. The shipped `mcla.toml.example` documents the existing resolution/video/window, SDL controller, logging, and game/user/update/cache/metadata roots without overwriting a user's live config or command-line test roots; F4 remains the runtime settings editor. A clean SDK install, 3 cases / 27 assertions, clean RelWithDebInfo host build, 25-key source/staged-template verification, one positive fixture, and twelve bad-input/config negatives pass. Device identity and user paths are not persisted in public evidence, and no physical alternate-device or loudness claim is made. Evidence: `config/mcla.toml.example`, `scripts/run-host-config-smoke.ps1`, `scripts/verify-host-config-smoke.ps1`, `scripts/test-host-config-smoke.ps1`, `docs/evidence/M6-011-host-configuration.md`.
- [x] **M6-012** Establish performance telemetry for CPU frame time, GPU frame time, streaming stalls, audio underruns, and shader compilation. ReXGlue v0.9.0.28 adds a default-off InitOnly 300-sample audit with guest-frame/host-time correlation, real fence-resolved D3D12 timestamp queries, host streaming read/stall deltas, SDL silence-fill underruns, and shader/PSO counts, failures, and elapsed time. One autonomous clean-build title-route trace covered guest frames 1–300 with CPU p50/p95 33,151/34,687 us, GPU p50/p95 708/1,457 us, 2,904 reads / 94,885,557 bytes, zero reads at or above 5 ms, four audio underruns, four successful shader translations, and two successful PSO creations. The gate passed 2 cases / 7 assertions, one positive fixture, fourteen fail-closed negatives, and fifteen source checks. This is a bounded boot/title sample, not a gameplay benchmark or proof that underruns caused KI-017. Evidence: `scripts/run-performance-telemetry-smoke.ps1`, `scripts/verify-performance-telemetry-smoke.ps1`, `scripts/test-performance-telemetry-smoke.ps1`, `docs/evidence/M6-012-performance-telemetry.md`.
- [x] **M6-013** Fix reachable unsupported PPC instructions and unimplemented imports discovered during broad play. Six prior accepted reports bind broad race, city, garage, race-system, audio/device, and force-feedback-degradation routes; no reached `PPC_UNIMPLEMENTED` instruction was found. ReXGlue v0.9.0.29 replaces the two actually reached import stubs: file-handle dismount validates a real `XFile` and acknowledges the runtime-owned VFS mount, while console-private-key signing fails explicitly without modifying the caller buffer. One autonomous current title/present route confirms both markers, controlled exit 0, and no legacy/generic stub, invalid target, fatal, device-loss, or DRED marker. Six prior invalid targets are exact generated/registered functions; twelve imported xboxkrnl stubs remain explicitly not-observed/deferred, and eight advanced FFB imports remain safely unadvertised. Evidence: `scripts/run-reached-unsupported-report.ps1`, `scripts/verify-reached-unsupported-report.ps1`, `scripts/test-reached-unsupported-report.ps1`, `docs/evidence/M6-013-reached-unsupported-coverage.md`.
- [ ] **M6-014** Run two-hour soak tests in frontend, free roam, repeated races, garage, and pause/resume. The five-category harness and recoverable per-scenario save watcher are implemented and fixture-verified. Historical v0.9.0.30 suite `20260826-100036-1b80ac82` retains valid frontend plus mixed free-roam/race evidence, including +15 race completions, +14 wins, two freeway drivers, at least two completed series, and one pink-slip best-of-three series, but is not canonical current-artifact evidence. ReXGlue v0.10.0.0 suite `20260829-172903-5bc0bc1e` retains a valid autonomous `frontend` PASS, while its owner-progressed `free-roam` attempt stopped at about 1:42 after a host NVIDIA/Sunshine failure; its latest 35-race/29-win profile is separately archived as the exact successor seed with save hash `FFB227E87...`. ReXGlue v0.10.0.1 fixes the run's globally black Photo Mode JPEG through post-plugin `fast` GPU readback: 10-minute regression `regression-20260831-125115-bf608f12` produced an owner-visible and machine-decoded non-black JPEG. Stable virtual-pad routing also reduced latest-active changes from 849 to seven deliberate transitions, but centering still disappeared while the T300 remained active, so KI-022 stays open as an unresolved FFB lifecycle issue. Because physical wheel access is no longer available, M6-014 proceeds with gamepad-only soak coverage and explicitly carries KI-022 rather than blocking or fabricating a wheel fix. No v0.10.0.0 stage is mixed into v0.10.0.1 acceptance; successor-artifact `frontend`, `free-roam`/`races`, `garage`, and `lifecycle` remain open.
- [x] **M6-015** Add model-agnostic racing-wheel input and bounded force feedback. ReXGlue v0.10.0.0 admits SDL-classified wheels plus documented driver fallbacks, exposes configurable axes/buttons and Xbox 360 wheel guest identity, and routes guest slot zero to the latest meaningfully active device across arbitrary physical slots. The title's 64-slot Xbox 360 force-feedback surface translates constant, ramp, five periodic, spring, and damper effects into SDL with configurable 0–100% overall gain, 40% continuous-periodic engine/road texture, neutralized infinite directional constant, and focus/active-device suspension and resume. Physical T300 evidence passes the full input matrix, hotplug, centering, bounded curb/collision event force, Alt-Tab recovery, and wheel→gamepad→wheel switching. Accepted causal run `event-ffb-20260829-164110-ddb9626b` contains three finite square starts in the curb window and six in the collision window; curb force remains intermittent because the title does not submit it for every road edge. A later soak exposed KI-022: Steam Input's duplicate virtual pads can churn ownership and suspend centering without new wheel deadzone crossings. ReXGlue v0.10.0.1 contains the narrow mirror/takeover/reclaim repair and focused tests, but its physical regression remains a gate for M6-014 rather than retroactively altering the accepted v0.10.0.0 M6-015 evidence. Logitech, MOZA, and other SDL wheel-class devices remain configuration-compatible rather than physically verified, and exact force fidelity is not claimed. See `docs/evidence/M6-015-wheel-input-ffb.md`.
- [x] **M6-016** Create the alpha compatibility matrix and known-issues list. The public matrix names the exact MCLA-R `0.7.0.0` / ReXGlue `0.10.0.0` tested build, Complete Edition identity, Windows 11 / Ryzen 9 5900X / RTX 3090 / D3D12 reference host, two physical gamepad routes, the physically verified T300RS wheel, 1280x720 and 2560x1440 routes, and a privacy-safe default-audio path. Thirteen rows distinguish verified, limited, representative, bounded-offline, in-progress, and unverified coverage; every row cites public evidence and an explicit exclusion. The initial twenty `KI-001` through `KI-020` identities and the three later M6-014 findings `KI-021` through `KI-023` are machine-matched to the canonical register without changing the historical tested-build claim. Alternate OS/GPU, cross-model wheel, full-campaign, online-service, rendering-parity, exact-mix, and completed-current-soak claims remain fail-closed false. Evidence: `config/alpha-compatibility-matrix.json`, `docs/alpha-compatibility.md`, `docs/known-issues.md`, `scripts/run-alpha-compatibility-matrix.ps1`, `scripts/verify-alpha-compatibility-matrix.ps1`, `scripts/test-alpha-compatibility-matrix.ps1`, `docs/evidence/M6-016-alpha-compatibility.md`.

### Deliverables

- repeatable multi-session single-player alpha
- robust saves/settings
- broad core-system compatibility
- documented offline-service policy

### Verification evidence

- two-hour soak test without S0/S1 failure
- save/load works across process restart and version-compatible build changes
- representative race types and all major city regions are covered
- audio device and controller reconnect tests pass
- no unbounded resource growth under the soak scenario

### Exit criteria

- Extended gameplay is practical without developer intervention.
- Core progression, garage, audio, saves, and world streaming are functional.
- All remaining progression risks are catalogued for M7.

### Gate

`GO M7` requires a stable alpha and a campaign test plan.

### Checkpoint log

- 2026-08-17 — M6 checkpoint 1. Completed: M6-001; total 1/15. Health: `GREEN — ALL MAJOR CITY COVERAGE ZONES STREAMED IN ONE RELEASE PROCESS`. Evidence: the completed-save route crossed eight project-defined zones and returned to its start across nine distinct GPS captures and monotonic present watermarks; process-read I/O grew by 4,920,118,313 bytes while peak private/working-set/handle/thread growth remained within the declared bounds; controlled external close, save/header identity, Release artifacts, M5-002 archive prerequisite, logs, samples, captures, and evidence tree revalidate. Two fixture positives, fourteen fail-closed negatives, twenty-three source checks, counter self-test, Windows PowerShell 5.1 parsing regression, ast-grep, and bootstrap gates pass. Scope remains geographic coverage rather than exhaustive roads/content. Next task: M6-002.
- 2026-08-17 — M6 checkpoint 2. Completed: M6-001 through M6-002; total 2/15. Health: `GREEN — REPRESENTATIVE GARAGE PURCHASE/CUSTOMIZATION/PERSISTENCE VERIFIED AUTONOMOUSLY`. Evidence: a clean Release build completed two isolated garage processes with 69/69 deterministic slot-0 source-to-guest button pulses and no operator input. Cycle 1 purchased a Golf, exterior and performance items, paint-shop access and dark-blue paint, switched back to the 240SX, and returned to free roam; cycle 2 received the exact changed profile, showed both vehicles and the persisted blue Golf/exterior item, switched back, and returned to free roam. Twelve distinct private captures, the save hash chain, exact headers, two external `WM_CLOSE` lifecycles, runtime logs/artifacts, source-game identity, and evidence tree revalidate. Two fixture positives, twenty-six fail-closed negatives, and thirty-nine source checks pass. Scope remains one affordable representative lifecycle; M7-004 owns exhaustive garage/category coverage. Next task: M6-003.
- 2026-08-18 — M6 checkpoint 3. Completed: M6-001 through M6-003; total 3/15. Health: `GREEN — REPRESENTATIVE RACE-SYSTEM MATRIX BOUND WITH EXPLICIT TELEMETRY LIMITS`. Evidence: the current Martin head-to-head route contains distinct start/reward frames and one machine-observed `Race_Finish` call; the owner completed it with no police seen. Immutable Ian-series, five-race reward/resource, and traffic results add the prior multi-event, repeated-reward, opponent, and traffic matrix, while prior police presence remains explicitly external owner testimony. Optional description/checkpoint/UI-result wrappers were zero-hit and are not claimed as current machine telemetry. The original event process truthfully remains force-cleaned after the obsolete strict-summary timeout; a separate current-binary cycle exited 0 through `WM_CLOSE`. Three positives, twenty-eight fail-closed negatives, twenty-four source checks, a clean Release build, ast-grep scan, and ast-grep 3/3 pass. Next task: M6-004.
- 2026-08-18 — M6 checkpoint 4. Completed: M6-001 through M6-004; total 4/15. Health: `GREEN — REPRESENTATIVE DRY-NIGHT / RAINY-DAWN EFFECT MATRIX VERIFIED, KNOWN S2 DEFECTS PRESERVED`. Evidence: clean Release run `20260818-192321-c0a2276b`; exact Arcade `RAINY` / `DAWN` option capture; six monotonic 1280x720 frames; 72 causal frontend and eight render-input records; dense rain, wet-road reflection, ambient-light, moving/stopped, and particle states; 30,778-45,458 differing sampled pixels; controlled external close; exact game/save/runtime/SDK/prior-evidence bindings. M5-003/M6-001/M5-012/M5-013 provide inherited night/shadow/particle, time-transition, race-motion, and five-race context. KI-013, KI-015, and KI-016 remain open S2 and are not laundered by the category PASS. Two positives, twenty-five fail-closed negatives, twenty-two source checks, clean Release build, ast-grep scan, and ast-grep 3/3 pass. Next task: M6-005.
- 2026-08-18 — M6 checkpoint 5. Completed: M6-001 through M6-005; total 5/15. Health: `GREEN — NATIVE AUTOSAVE/LOAD/OVERWRITE AND ISOLATED CREATION/DESTRUCTIVE SAVE MATRIX VERIFIED`. Evidence: build-only run `20260818-201702-5d089d9c`; exact M6-002 result re-parsed from its physical logs/save chain for native autosave overwrite, exact fresh-process handoff/load, and two controlled exits; clean ReXGlue v0.9.0.23 plus Release host builds; focused 10-case/117-assertion tests for isolated creation/restart metadata, two corruption classes, interrupted overwrite/new-save recovery, restart-idempotence, commit point, live enumeration, committed overwrite, and storage-full rejection. The HANGOUT save/header remained byte-identical. One positive, thirty-six fail-closed negatives, twenty-eight source checks, ast-grep scan, and ast-grep 3/3 pass. Manual save is not exposed by this autosave route; first-run OOBE/general profile persistence remain open. Next task: M6-006.
- 2026-08-18 — M6 checkpoint 6. Completed: M6-001 through M6-006; total 6/15. Health: `GREEN — ISOLATED PROFILE/CONTROLLER/LANGUAGE RESTART MATRIX VERIFIED WITH PHYSICAL ROUTES REBOUND`. Evidence: build-only run `20260818-211448-f6ec1436`, result SHA-256 `D219DA53F394B2B480E808760D195894CFFE5208EBCB0B0D2223A2F86F44B410`; exact ReXGlue v0.9.0.24 and Release clean builds; 7-case/40-assertion profile restart/recovery suite; 26-case/128-assertion CVar serialization suite; immutable M4-004 profile, M4-010 locale, M5-006 controller, and M6-005 save results; byte-identical HANGOUT save/header; one positive, forty-one fail-closed negatives, and thirty source checks. Scope is deliberately isolated because MCLA does not import the write API: no native profile-write, first-run OOBE, account picker, multi-profile, or general host-config claim. Next task: M6-007.
- 2026-08-19 — M6 checkpoint 7. Completed: M6-001 through M6-007; total 7/15. Health: `GREEN — TWO-HOUR AUDIO SOAK AND DEFAULT-DEVICE RECOVERY VERIFIED`. Evidence: split physical run `20260819-122150-2ad3b961`, result SHA-256 `CE4B4700CEA1108243989165211A8C70AC0C67F72130708A129C1BD834948B5B`; exact ReXGlue v0.9.0.25; clean SDK/RelWithDebInfo builds and 10-case/42-assertion audio suite; exact 7,200-second title soak with sustained submitted/device/XMA output, queue depth eight, and zero invalid/submission/starvation counters; thirteen resource samples below the declared 1-GiB private, 512-MiB working-set, 128-handle, and 16-thread bounds; separate two-cycle pause/resume plus real default-playback-device migration and owner-heard recovery; two controlled external closes; immutable prior audio-route bindings. Scope remains split-process and does not claim exact mix fidelity, XMP decoding, selected system music, or endpoint identity. Three fixture positives, thirty fail-closed negatives, thirty source checks, build/profile regressions, ast-grep scan, and ast-grep 3/3 pass. Next task: M6-008.
- 2026-08-19 — M6 checkpoint 8. Completed: M6-001 through M6-008; total 8/15. Health: `GREEN — EXPLICIT OFFLINE PROGRESSION/SERVICE POLICY VERIFIED WITHOUT FABRICATED LIVE STATE`. Evidence: build-only run `20260819-164950-4b322fe1`, result SHA-256 `7DC8946ABD4B1BB78D52E527655D713EB4FD7037421997F4BA209689BC21903B`; exact ReXGlue v0.9.0.26; clean RelWithDebInfo and Release SDK builds plus clean Release host; 6-case/25-assertion offline-XAM, 3-case/32-assertion offline-service, and 6-case/50-assertion achievement suites. The six-row matrix makes achievements and Driving Test local, leaderboards valid-empty, and presence/Rate My Ride/voice unavailable. Five immutable prior results bind reached profile, frontend, progression, and save behavior. One fixture positive, forty fail-closed negatives, and twenty-eight source checks pass. No current physical service-call, Live, real-row, exact-unlock, or service-vehicle claim is made; M6-009 remains unchanged. Next task: M6-009.
- 2026-08-19 — M6 checkpoint 9. Completed: M6-001 through M6-009; total 9/15. Health: `GREEN — RETIRED-SERVICE ENTITLEMENTS KEPT OUT OF CANONICAL COMPATIBILITY`. Evidence: decision run `20260819-171011-98b464f4`, result SHA-256 `8CD1FD76F8C4E3449FFCB699BDBBCA05276088B8BA1E248BC889AC6EDAF57E25`; policy SHA-256 `44FEDC36A8CE9575F9E2A53AAC84EDFE3765D24759E7D9A34CAD7BA8067B48CE`; immutable M6-008 offline-service policy rebound; one fixture positive, forty fail-closed negatives, and twenty-four source checks pass. Canonical mode preserves the retail lock and authentic existing entitlements, with no fake service completion, automatic Audi R8 unlock, save/source-game mutation, or Live fabrication. A future explicit default-off InitOnly cheat is allowed only as a separately named non-persistent surface and remains unimplemented/excluded from canonical evidence. Next task: M6-010.
- 2026-08-19 — M6 checkpoint 10. Completed: M6-001 through M6-010; total 10/15. Health: `GREEN — SPLIT WINDOW/DEVICE LIFECYCLE MATRIX AND MINIMIZED EXTERNAL EXIT VERIFIED`. Evidence: physical run `20260819-180446-730942f2`, result SHA-256 `746D7DAECD475E48EEA9EE8342319A84CC2E059992A635579AB55AF7A719C71B`; exact ReXGlue v0.9.0.26 and clean Release build; immutable M5-006 gameplay-pause, M4-006 focus/hotplug, M6-007 audio-device, and M4-013 repeated-close evidence rebound. The current cycle kept the minimized process alive for 5,025 ms, restored a distinct 1280x720 frame with 234 color bins and 180 luma range, minimized again, and exited 0 through external `WM_CLOSE` in 571 ms with no force cleanup or data/artifact drift. The complete five-file, 7,472,224-byte cycle tree is SHA-bound. One positive, thirty-eight fail-closed negatives, and twenty exact source checks pass. Scope excludes OS suspend/hibernate, graceful subsystem teardown, display-adapter removal, multiple-controller identity, and a monolithic all-rows run. Next task: M6-011.
- 2026-08-19 — M6 checkpoint 11. Completed: M6-001 through M6-011; total 11/15. Health: `GREEN — HOST CONFIGURATION SURFACE VALIDATED WITHOUT OVERRIDING USER ROOTS`. Evidence: build-only run `20260819-184541-fa4b1cc6`, result SHA-256 `F9977148797A625F15B20AC53807DC5B0CCD748A7C19205611FEBD71D4337C93`; exact ReXGlue v0.9.0.27 commit `8cc7c272035a3bb4124d15060694943f848d9ee1`; clean SDK install and clean RelWithDebInfo host build; focused audio/config suite 3 cases / 27 assertions; exact 25-key `mcla.toml.example` copied byte-for-byte beside the host; one positive, twelve fail-closed bad-input/config negatives, and sixteen source checks. Existing display/window, SDL controller, logging, and five data-root CVars are documented; new exact-name/default audio-device selection fails closed and host volume is bounded to 0.0–1.0. The template is never auto-promoted to live `mcla.toml`, so canonical CLI test roots remain authoritative unless a user deliberately copies it. Scope excludes physical alternate-device/loudness proof, controller identity persistence, and a launcher UI. Next task: M6-012.
- 2026-08-19 — Mandatory M6 checkpoint review after M6-010/M6-011. Reviewer: Codex primary agent, with owner confirmation that brief music and continuing background SFX were audible. Completed since the preceding two-task review: M6-010 window/device lifecycle and M6-011 host configuration; total remains 11/15 and milestone health remains `GREEN`. New risk: KI-017 records that music stops while the broader XMA/XAudio/SDL route and ambient SFX remain alive; this is not total audio loss and is not hidden by either task PASS. Retained risks: normal external close is still a hard-exit rather than graceful subsystem teardown, alternate playback-device/loudness behavior is not physically proven, and controller identity is not persisted. Removed risk: host audio endpoint and volume were previously undocumented/unbounded at the project surface; v0.9.0.27 now validates them fail-closed. Estimate: M6-012 is a medium SDK telemetry + physical-sample task; M6-013 is a bounded reached-instruction/import inventory and repair task whose size depends on the trace. Gate readiness: not ready for M6 closure until M6-012 through M6-015 pass. Next two tasks: M6-012, then M6-013. Milestone scope and acceptance criteria remain unchanged.
- 2026-08-19 — M6 checkpoint 12. Completed: M6-001 through M6-012; total 12/15. Health: `GREEN — TIMESTAMPED CPU/GPU/STREAM/AUDIO/SHADER TELEMETRY VERIFIED`. Evidence: autonomous run `20260819-192540-2a9e82fe`, result SHA-256 `BFEA11FB1A7E8C5D380343C252612BC3BCBE471F0DC1BFB1FCA56B771FB27CB3`; exact ReXGlue v0.9.0.28 commit `6354bbe2150c7ce06bee5ffe399f17a94c948616`; clean SDK install and clean RelWithDebInfo host build; focused suite 2 cases / 7 assertions; one positive, fourteen fail-closed negatives, and fifteen source checks. The 300 samples correlate host timestamps to guest frames 1–300, use real D3D12 timestamp queries, and balance 2,904 streaming reads, four audio silence fills, four shader translations, and two PSO creations with zero failures/overflow. Scope is a boot/title sample, not a city-driving benchmark, FPS target, shader-cache-completeness claim, or diagnosis of KI-017. Next task: M6-013.
- 2026-08-19 — M6 checkpoint 13. Completed: M6-001 through M6-013; total 13/15. Health: `GREEN — REACHED UNSUPPORTED SURFACE CLASSIFIED`. Evidence: autonomous run `20260819-200712-a4cc8715`, result SHA-256 `279DA2FDEDF20D8C69D3DE6B5993A26AC625FBA9B624BF52BC3F800F8F1BC4ED`; exact ReXGlue v0.9.0.29 commit `5a7fc75713d1d43188b7574349f44a7e7923033d`; focused suite 2 cases / 5 assertions; one positive, fourteen fail-closed negatives, and eighteen source checks. The fresh trace reaches both explicit compatibility results and guest presentation, then exits 0 with no legacy/generic stub, unsupported PPC, invalid target, crash, fatal, or device-loss marker. Six broad-play invalid targets are repaired and twelve imported xboxkrnl stubs remain truthfully not-observed/deferred; M7-014 still owns the post-campaign final audit. Next task: M6-014.
- 2026-08-26 — M6-014 in-progress controller checkpoint. Completed task count remains 13/15; health: `GREEN — SOAK INPUT BLOCKER REMOVED, PHYSICAL SUITE STILL OPEN`. ReXGlue v0.9.0.30 commit `c16af0890babfbda24b7a58cf63c6573a702c581` decouples SDL physical storage from guest user slot zero, selects the latest meaningful button/analog source, routes capabilities/keystrokes/rumble consistently, and gives merged physical/synthetic input a monotonic guest packet. Focused SDK gates pass 124 assertions / 21 SDL cases, 104 assertions / 15 controller/RTL cases, and 23 assertions / 7 slot cases; project input-slot, controller-matrix, soak-fixture, ast-grep scan, and 3/3 rule tests pass. Physical run `active-controller-smoke-20260826-091721Z-258acb6b` observed Steam Controller slot 0 and Steam Input XInput wrapper slot 1, selected nonzero slot 1 for guest slot zero, and received owner-confirmed input; controlled external close preserved the exact `E34FBD...` save hash. The old incomplete two-hour run and its latest save snapshot remain private and preserved. M6-014 is not closed until five same-artifact 7,200-second scenarios pass.
- 2026-08-27 — M6-014 frontend/free-roam checkpoint. Completed task count remains 13/15; health: `GREEN — TWO OF FIVE CANONICAL SOAK STAGES PASS WITH OPEN S2 RENDERING DEFECTS`. Suite `20260826-100036-1b80ac82` retains one shared exact Release artifact set. Frontend passed autonomously; free roam completed 7,200 seconds with 25 resource samples, nine nontrivial captures, and eight owner checkpoints across BEVERLY, CRENSHAW, DOWNTOWN, HOLLYWOOD, and SANTA-MONICA. The runner originally issued its own controlled `WM_CLOSE` and falsely failed because the interactive garage-control worker makes the frontend-only audio completion marker unreachable. Version `0.6.1.2` limits that marker to frontend, runs the save watcher under the current PowerShell host, permits one operator/capture window for interactive sampling delays, and adds fail-closed recovery that revalidated shutdown-to-exit-0, no fatal markers, resource bounds, physical evidence, and the separately archived progressed profile (`6734627B...` save). Version `0.6.2.0` records the owner's additional KI-015 vehicle/shadow noise scope, KI-019 exposed neon projection geometry, and KI-020 white-paint overexposure with private hash-bound captures; the soak PASS makes no rendering-parity claim. Races, garage, and lifecycle remain.
- 2026-08-27 — M6-014 mixed race-coverage checkpoint. Completed task count remains 13/15; health: `GREEN — THREE OF FIVE CATEGORIES PASS ACROSS TWO PHYSICAL PROCESSES`. The owner clarified that the accepted free-roam process was predominantly racing: two freeway-driver events, at least two race series, and one pink-slip best-of-three win. Exact gameplay-seed/final-save parsing independently measures `gRCP` 9 -> 24 (+15 completed races) and `gWIN` 9 -> 23 (+14 wins), exceeding the ten-race/two-series floor. Version `0.6.3.0` adds a fail-closed mixed-session recovery record bound to those deltas, the active-race capture, source-stage hash, and owner attestation. The final suite will report ten category-hours but only four physical processes/eight physical hours; it cannot claim five independent processes or ten physical hours. No duplicate two-hour races run is required. Garage and lifecycle remain.
- 2026-08-27 — M6 scope extension. Completed task count is now 13/16; health remains `GREEN`, with M6 closure intentionally deferred for time-limited physical wheel access. New M6-015 owns model-agnostic SDL wheel admission, configurable control layouts, Xbox 360 wheel guest identity, latest-active routing, and bounded advanced force feedback, using the owner's Thrustmaster T300 as the first physical reference rather than a brand allowlist. The prior alpha matrix task moves unchanged to M6-016. M7-002 now also owns validated persistent local-profile renaming so the host is not permanently presented as `User`; this must remain a local offline identity rather than fabricated Xbox Live state.
- 2026-08-27 — M6-015 pre-physical checkpoint. Completed task count remains 13/16; health: `GREEN — WHEEL INPUT/SPRING PROVEN, NINE-FAMILY TITLE FFB READY FOR EVENT VALIDATION`. Physical T300 evidence already binds four-axis/two-pedal input, the complete digital/menu matrix, hotplug, latest-active routing, title-created spring centering, and post-focus recovery. The reviewed implementation expands the guest from one to 64 effect slots and translates constant, ramp, square, sine, triangle, both sawtooth families, spring, and damper, including all three condition axes and active-device/focus suspend-resume. Both RelWithDebInfo and Release SDK artifacts plus the Release host build successfully; focused gates pass 182 wheel assertions / 9 cases, 306 input assertions / 30 cases, 104 controller/RTL assertions / 15 cases, 23 slot assertions / 7 cases, the four project verifier suites, a clean ast-grep scan, and 3/3 ast-grep rule tests. Full SDK CTest remains at its known baseline: all 1,459 PPC tests pass and the same nine unrelated Template/MigrationScan unit tests fail. The event runner now installs the current Release SDK before its clean Release host build, uses the archived progressed save, runs at the owner-selected 100% host gain, and fails closed unless a short physical drive confirms centering, curb/rough-surface force, collision force, and focus recovery with at least one machine-observed non-spring title effect. M6-015 remains open until that run passes; no exact-force-fidelity or cross-model physical claim is made.
- 2026-08-28 — M6-015 event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — CENTERING/FOCUS PASS, CURB/COLLISION PHYSICAL RETEST REQUIRED`. Physical run `event-ffb-20260828-085030-21aa4a8a` truthfully records `YES/NO/NO/YES`: speed-dependent centering and post-focus recovery were felt, while curb and controlled-contact forces were not. The same clean run machine-observed title-created constant, square, spring, and damper effects with no SDL haptic failure, narrowing the defect to effect playback semantics rather than a missing title event family. Guest reverse analysis and captured calls show `{effect_index, iterations}` operation entries and distinct finite START edges. The corrected implementation carries the guest iteration count through the input stack and initially avoided restarting an SDL effect while hardware status said it was still playing; later direct probing proved that policy valid only for infinite effects because the T300 driver may report an expired finite effect as playing. Bounded parameter telemetry exposes exact guest-to-host magnitudes and durations. Review retained the physically proven signed-byte condition coefficient scaling after rejecting an intermediate interpretation that would have weakened spring force. Focused gates passed 192 wheel assertions / 9 cases and 316 SDL input assertions / 30 cases; the event fixture passed one positive, sixteen fail-closed negatives, and twenty-four source checks, with clean ast-grep scan and 3/3 rule tests. M6-015 remains open until the short physical rerun confirms or further diagnoses curb/collision force.
- 2026-08-28 — M6-015 second event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — NON-SPRING AMPLITUDE ABI FIXED, FINAL PHYSICAL RETEST REQUIRED`. Clean run `event-ffb-20260828-094324-42c1c758` again truthfully records `YES/NO/NO/YES`, but its new parameter telemetry supplies the missing causal evidence: the title submitted a 150-ms constant collision at guest level 77 and a 150-ms square transient at guest magnitude 76, while the host received only 154 and 152 respectively despite a 32767 positive SDL peak. The guest converter and compact PID field families prove that constant/ramp/periodic offset use signed bytes and periodic/envelope/phase use unsigned bytes; those families now expand across the SDL domains instead of being treated as near-16-bit inputs. Expected representative host peaks are now 19866 and 9765 at 100% gain, while the physically proven spring coefficient/saturation route is unchanged. Focus preservation now consults hardware status before replaying effects, preventing expired 150-ms impacts from restarting after Alt-Tab, and repeated deferred-start logging is bounded. Focused gates pass 196 wheel assertions / 9 cases and 324 SDL assertions / 30 cases; the event fixture passes one positive, sixteen fail-closed negatives, and twenty-four source checks; compatibility and host-config fixtures also pass. M6-015 remains open until one short physical run confirms curb and collision force.
- 2026-08-28 — M6-015 third event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — DIRECT TRANSIENTS PASS, TITLE LIFECYCLE FIX AWAITS ONE PHYSICAL RUN`. Clean run `event-ffb-20260828-101322-36a916f1` again records `YES/NO/NO/YES`; it also exposed a physically strong infinite square engine vibration that can mask short events. The owner then felt all four clean direct probes (150/400-ms square and constant) and both 150-ms effects in a mixed probe with the exact spring, damper, steering-constant, and infinite-periodic title baseline. This proves T300 duration and effect mixing are capable and isolates the title failure to lifecycle/state. Infinite periodic engine/road texture now defaults to 40% while overall FFB remains 100%; every finite START edge is submitted despite stale hardware status, finite effects are never preserved across latest-active/focus suspension, and focus-lost finite starts are dropped rather than replayed late. Latest meaningful wheel/gamepad input still owns guest slot zero regardless of physical slot; switching away stops wheel feedback and switching back resumes only infinite current effects. Clean gates pass 209 wheel assertions / 9 cases, 340 SDL assertions / 31 cases, 104 controller/RTL assertions / 15 cases, and 30 slot/latest-active assertions / 8 cases. The event fixture passes one positive, twenty fail-closed negatives, and thirty source checks; clean RelWithDebInfo/Release SDK and Release host builds pass. The final runner now requires owner-confirmed and machine-logged wheel→gamepad→wheel switching, including distinct focus and active-controller FFB-resume markers, in addition to centering, curb, collision, and focus recovery. M6-015 remains open for that single in-title physical run.
- 2026-08-28 — M6-015 fourth event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — CONDITION POLARITY FIX BUILT, ONE PHYSICAL RETEST REQUIRED`. Clean run `event-ffb-20260828-184529-9d908c7c` records `NO/NO/NO/YES/YES`: latest-active wheel→gamepad→wheel selection and post-focus recovery worked, but the wheel pulled continuously right in gameplay, reinforced full-lock steering instead of returning to center, and masked both finite event classes. Its exact telemetry shows the title supplied positive spring/damper restoring magnitudes while the host submitted positive SDL/DirectInput condition slopes. DirectInput condition force is slope times displacement/velocity, so that translation creates positive feedback rather than resistance. The translator now reverses only condition-coefficient polarity while retaining the already-proven magnitude scaling; positive guest spring/damper values become negative SDL slopes, and negative guest values retain inverse behavior. The direct and mixed hardware probes use the same corrected polarity. The final verifier additionally requires machine telemetry for both restoring spring and resisting damper polarity. Clean RelWithDebInfo/Release SDK installs and a clean 71-action Release title build pass; focused gates pass 209 wheel assertions / 9 cases, 340 SDL assertions / 31 cases, 104 controller/RTL assertions / 15 cases, and 30 slot/latest-active assertions / 8 cases. Event fixtures pass one positive, twenty-two fail-closed negatives, and thirty-two source checks; compatibility fixtures pass one positive, thirteen negatives, and twenty-three source checks; PowerShell parsing, clean ast-grep scan, and 3/3 rule tests pass. No physical fix is claimed until the owner reruns the title event matrix.
- 2026-08-29 — M6-015 fifth event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — GLOBAL CONDITION INVERSION DISPROVED, ISOLATED HARDWARE CHECK REQUIRED`. Physical run `event-ffb-20260829-125918-61f391da` was operator-interrupted after the title session, so it has no final questionnaire/result JSON, but its complete runtime log and the owner's contemporaneous `NO/NO/NO` report are retained as diagnostic evidence. Negative spring/damper coefficients removed the earlier one-sided right pull but still drove the T300 away from center; continuous engine vibration was not created in that abbreviated title session, and neither finite class was felt. SDL's shipped haptic test uses positive spring/damper coefficients, and the negative damper behavior is consistent with an anti-damper. The global inversion is therefore reverted to sign-preserving SDL condition translation. A new short direct condition-isolation probe separates positive spring, positive damper, and both constant-force directions before any further title rerun. No physical fix or event PASS is claimed.
- 2026-08-29 — M6-015 sixth event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — CONDITION SEMANTICS PROVEN, TITLE MIX ISOLATED FOR FINAL RETEST`. The owner physically confirmed the direct T300 isolation phases: positive SDL spring returned the wheel to center, positive damper resisted motion without a one-sided pull, positive constant pulled left, and negative constant pulled right. The constant phases remained very light even after the diagnostic magnitude was raised to 15000; no title-strength claim is inferred from that observation. This proves sign-preserving spring/damper translation and identifies the title's infinite negative constant as the prior persistent right pull. Infinite directional constant now defaults to 0% while finite collision constants retain 100% overall gain; continuous periodic engine/road texture remains 40%. The final verifier now requires machine-correlated full-gain finite square and constant START edges, a neutralized infinite constant, positive spring/damper coefficients, focus recovery, and wheel→gamepad→wheel recovery in addition to owner sensation. Focused wheel tests pass 217 assertions / 9 cases; host-config, event, and compatibility fixtures pass 17, 27, and 13 fail-closed negatives respectively. One in-title physical rerun remains; no event PASS is claimed.
- 2026-08-29 — M6-015 seventh event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — LIFECYCLE PASS, WEAK TITLE TRANSIENTS AMPLIFIED FOR RETEST`. Clean run `event-ffb-20260829-134047-0f2cb89e` records owner results `YES/NO/YES/YES/YES`: centering, intermittent collision, focus recovery, and latest-active switching passed; curb sensation remained unconfirmed, and no engine periodic effect was created in this session. The controlled title lifecycle completed normally even though the verifier correctly rejected the incomplete physical matrix. Machine telemetry proves nine finite 150-ms square START submissions at host magnitude 8994 and four finite 150-ms constant START submissions at host level -3096; this explains barely perceptible curb and intermittent weak collision despite correct lifecycle. Infinite directional constant remained neutral at gain 0 with no prior right pull. A configurable 50% minimum transient strength now raises only nonzero finite effects within the current overall gain: representative weak square/constant events become at least 16383 while zero and infinite effects are unchanged. The verifier requires both finite families at or above that floor. Focused wheel tests pass 224 assertions / 9 cases; host-config, event, and compatibility fixtures pass 18, 29, and 13 fail-closed negatives. A short physical rerun remains; collision need only correspond to actual title submissions rather than every contact, and no engine effect is fabricated when the title creates none.
- 2026-08-29 — M6-015 eighth event-FFB diagnostic checkpoint. Completed task count remains 13/16; health: `YELLOW — COLLISION FELT, CURB STILL ABSENT, CAUSAL ACTION WINDOWS BUILT`. Clean run `event-ffb-20260829-142458-05e3d0bd` reached a controlled exit and physically passed centering, collision, focus recovery, and latest-active switching. Although the console answer said curb `YES`, the owner's immediate correction is authoritative: curb remained `NO`, so the run is not accepted. The log contains five 150-ms square STARTs at the then-current 50% floor (16383), no finite constant, and no infinite periodic engine effect; therefore the faint background sensation cannot justify changing the existing 40% continuous-periodic policy. The verifier's assumption that every session must create both finite square and constant was false: it now accepts whichever finite title family was actually created but requires timestamped finite STARTs inside separate operator-bounded curb and collision windows. The minimum finite constant/periodic primary-amplitude floor is raised from 50% to 75% of current overall gain (24575 at 100%), a 50% increase matching the requested impact adjustment; zero, infinite effects, periodic directional offset, and ramp shape are unchanged. Focused gates pass 227 wheel assertions / 9 cases and 358 SDL assertions / 31 cases; the event fixture passes two positives, thirty fail-closed negatives, and forty-two source checks, while host-config and compatibility fixtures pass eighteen and thirteen negatives. One causal physical rerun remains before closure.
- 2026-08-29 — M6-015 closure checkpoint. Completed task count is 14/16; health: `GREEN — MODEL-AGNOSTIC WHEEL/T300 TITLE FFB PASS`. Accepted causal run `event-ffb-20260829-164110-ddb9626b` physically confirms centering, intermittent title-submitted curb force, collision force, Alt-Tab recovery, and latest-active wheel -> gamepad -> wheel switching. Its bounded action windows contain three finite square STARTs for curb attempts and six for collision attempts, with 55 active-controller transitions and no compatibility no-op or fatal marker. The owner manually closed the game with the window close button after the PowerShell host disappeared; `Window closing`, `Execution complete`, and the controlled hard-exit marker prove a normal external close rather than a crash. Recovery provenance is explicit in the private observation/result. Final compatibility run `20260829-171205-28b1671a` passes with result SHA-256 `63FEAD6AE42313844B9BBB384F9674E5D2C6FF6DEFBE0C1D407FEF1146956CCD`, ReXGlue v0.10.0.0 commit `5d3e98c064c38e0769b4f59d11729c8f6270eb83`, 227 wheel assertions / 9 cases, 358 SDL assertions / 31 cases, and the physically preserved save tree. Overall gain remains 100%, periodic texture 40%, infinite directional constant 0%, and finite transient floor 75%. Only the T300 is physically verified; other SDL wheels are configuration-compatible, curb submission is intermittent, exact Xbox 360 force fidelity is not claimed, and the accepted run created no engine periodic effect. M6-014 garage/lifecycle soak and M6-016 alpha matrix remain open.
- 2026-08-29 — M6-016 closure checkpoint. Completed task count is 15/16; health: `GREEN — BOUNDED SINGLE-HOST ALPHA MATRIX PUBLISHED`. The reviewed public matrix binds MCLA-R `0.7.0.0` commit `6c167fae0805d8f1417e8e0484c8614fef27f77a`, ReXGlue `0.10.0.0` commit `5d3e98c064c38e0769b4f59d11729c8f6270eb83`, the exact Complete Edition identity, and one Windows 11 / Ryzen 9 5900X / RTX 3090 / D3D12 host. Thirteen route rows each carry public evidence and an explicit limitation; all twenty known-issue identities match the canonical register. One positive plus thirty-one fail-closed fixtures reject identity, hardware, route, evidence, issue, and broad-claim drift. Alternate OS/GPU, cross-model wheel, full campaign, online service, rendering parity, exact audio mix, and completed current soak remain explicitly unclaimed. M6-014 is the only open M6 task; current-artifact suite `20260829-172903-5bc0bc1e` is running its autonomous frontend stage, while the older `0.9.0.30` suite remains historical evidence and is not mixed with the new artifact set.
- 2026-08-29 — M6-014 current-artifact frontend checkpoint. Completed task count remains 15/16; health: `GREEN — CURRENT SDK FRONTEND 1/5 PASS, INTERACTIVE STAGES OPEN`. Suite `20260829-172903-5bc0bc1e` binds the exact clean ReXGlue v0.10.0.0 commit `5d3e98c064c38e0769b4f59d11729c8f6270eb83` and one immutable Release artifact set. Its autonomous frontend ran 7,205 seconds, recorded 25 ordered resource samples and nine temporally distinct physical captures, preserved the exact save/header hashes, and ended through controlled external close with exit 0, no forced cleanup, and zero fatal markers. Final growth is 755,572,736 private bytes, 309,829,632 working-set bytes, +93 handles, and zero threads; peak growth remains within the canonical 1 GiB / 512 MiB limits. Version `0.7.1.1` removes the stale `0.9.0.x` acceptance pattern: runner, verifier, and fixtures now derive and enforce the exact current CMake/bootstrap SDK version, tag, and commit. Focused validation passes one positive, 44 fail-closed negatives, and 79 source-contract checks. Historical v0.9.0.30 evidence remains preserved but is not mixed. Current-artifact free-roam, races, garage, and lifecycle still require operator play, so M6-014 and M6 remain open.
- 2026-08-29 — M6-014 progressed-seed hotfix checkpoint. Completed task count remains 15/16; health: `GREEN — STALE GAMEPLAY SEED REJECTED, OWNER PROGRESSION PRESERVED`. The first current-artifact free-roam attempt exposed that the suite still copied the older M6-002 profile with embedded `gRCP/gWIN` counters 9/9. Version `0.7.2.1` archives the newest complete owner profile from the accepted M6-015 route as an immutable private seed with counters 24/23, exact save hash `27992E6B...`, exact header hash `A5A662F7...`, and a hash-bound snapshot manifest. The explicit rebind migrated suite `20260829-172903-5bc0bc1e` without changing its accepted frontend stage or Release artifacts and moved the incomplete old-seed free-roam root into `aborted-attempts`. Focused validation passes one positive, 44 fail-closed negatives, and 86 source-contract checks; a dry initialization revalidated the migrated suite. No gameplay soak time from the rejected 9/9 attempt is credited.
- 2026-08-30 — M6-014 1:42 failure-diagnosis checkpoint. Completed task count remains 15/16; health: `YELLOW — SAVE PRESERVED, HOST GPU FAILURE CLASSIFIED, TWO PHYSICAL REGRESSIONS REQUIRED`. The owner-progressed free-roam process reached 21/25 resource samples, seven captures, and six checkpoints before D3D12 reported device removal `0x887A0005`. Windows independently recorded `nvlddmkm` Event 153 and repeated Sunshine.exe `0xc0000005` crash/restarts in the same interval, so the attempt is retained as host-failure diagnostic evidence rather than called a guest or soak PASS. The newest 35-race/29-win save was archived separately with SHA-256 `FFB227E877B9A8B4F5011D82234DCAF146487A9DFDF60611A9F434C54147364E` and promoted to the exact gameplay seed for the successor suite. A Photo Album container from the process contains a structurally valid 1280x720 JPEG whose decoded luma is exactly zero; this isolates KI-021 to missing GPU-to-CPU render-to-texture readback rather than album storage. SDL telemetry also records 849 active-controller changes among the T300 and two Steam Input mirrors, matching KI-022's intermittent FFB suspension. ReXGlue v0.10.0.1 commit `7dd5cb33002a443b097c0f65d5566c0a0f2db838` adds the post-plugin fast-readback default and stable wheel ownership policy; focused SDL tests pass in both RelWithDebInfo and Release, the Release title builds, the non-black Photo Mode verifier rejects the captured black JPEG, and the soak harness now fails closed on new NVIDIA display-driver events while recording bounded Sunshine crash metadata. Short physical Photo Mode and wheel checks remain mandatory before replacing the invalidated v0.10.0.0 suite with a fresh same-artifact soak.

  - 2026-08-31 — M6-014 v0.10.0.1 regression checkpoint. Completed task count remains 15/16; health: `YELLOW — PHOTO MODE CLOSED, WHEEL CENTERING CARRIED OPEN, SUCCESSOR SOAK READY`. Ten-minute run `regression-20260831-125115-bf608f12` completed its 600-second wall clock with baseline/300/600 resource samples, two nontrivial captures, controlled external close, no runtime fatal marker, and no new NVIDIA or Sunshine event. The operator confirmed the new Photo Mode preview is visible; machine extraction found two embedded 1280x720 JPEGs, with the historical black payload at luma 0..0 and the new payload at 157 sampled color bins and luma 0..237. Album SHA-256 is `466AD5206195CB43DC6F90185FFFA9BF0D6E7AE64B2CF00D3590CAA68979B1C9`, so KI-021 is closed on ReXGlue v0.10.0.1. Latest-active telemetry fell from 849 churn transitions in the failed long run to seven deliberate wheel/pad changes, but the operator still observed centering disappear while the wheel remained active. KI-022 therefore remains open as an unresolved FFB effect/hardware-lifecycle defect; no speculative physical fix is claimed without future wheel access. A final bookkeeping exception came from applying `Measure-Object primary` to ordered dictionaries after the process had already closed; index-based aggregation and a fail-closed completed-calibration recovery path preserve the valid journals without replay. Successor M6-014 soak work now continues with gamepad-only physical interaction while the wheel limitation remains documented.

  ## 15. Milestone M7 — Full campaign and Complete Edition compatibility

**Goal:** validate the entire single-player campaign and all on-disc Complete Edition content from a clean save through completion.

**Estimate:** 2–6 months depending on discovered progression defects.

### Prerequisites

- M6 closed
- Campaign checklist and save-backup procedure approved
- Regression build pinned

### Tasks

- [ ] **M7-001** Build a chapter/event/race progression matrix for the full campaign.
- [ ] **M7-002** Verify new game, local-profile naming/renaming, tutorials, contacts, mission unlocks, reputation progression, and end-game state. Replace the current fixed `User` presentation with a validated persistent local display name without fabricating Xbox Live identity.
- [ ] **M7-003** Validate every race/event category with representative failures, retries, restarts, and wins.
- [ ] **M7-004** Validate all vehicle classes, motorcycles, customization categories, and performance upgrades.
- [ ] **M7-005** Validate South Central map expansion and all Complete Edition vehicles/events.
- [ ] **M7-006** Validate cutscenes, phone calls, scripted sequences, Bink playback, subtitles, and skip behavior.
- [ ] **M7-007** Validate all garages, shops, map transitions, interiors, and streaming boundaries.
- [ ] **M7-008** Validate police, traffic, AI, collision, damage, respawn, and recovery edge cases.
- [ ] **M7-009** Validate save compatibility throughout progression and recovery from interrupted writes.
- [ ] **M7-010** Test supported languages present on the disc.
- [ ] **M7-011** Run clean-save full campaign playthrough number one and log every defect.
- [ ] **M7-012** Fix progression blockers and rerun affected segments from earlier saves.
- [ ] **M7-013** Run clean-save full campaign playthrough number two on a release-like build.
- [ ] **M7-014** Audit all remaining XAM/xboxkrnl stub calls reached during the campaign; replace unsafe success stubs with state-correct implementations.
- [ ] **M7-015** Finalize compatibility and known-issues documentation.

### Deliverables

- two documented full-campaign runs
- Complete Edition content coverage matrix
- stable save/progression behavior
- no known single-player progression blockers

### Verification evidence

- clean save reaches campaign completion twice
- South Central and all mandatory Complete Edition content load and complete
- no S0/S1 defect remains open
- all skipped, unavailable, or retired online functionality is explicitly documented
- release-like build reproduces campaign completion without debug-only patches

### Exit criteria

- Full single-player campaign is completable from a clean save.
- Complete Edition on-disc content is accessible.
- Save data remains stable across the validated version line.
- Remaining issues are non-blocking S2/S3 items with workarounds or accepted limitations.

### Gate

`GO M8/M9` requires complete campaign evidence. Enhancements cannot substitute for compatibility.

### Checkpoint log

- No checkpoint yet. Health: `NOT STARTED`. Next tasks after gate entry: M7-001, M7-002.

## 16. Milestone M8 — Modern enhancements and secondary platforms

**Goal:** add optional improvements without changing stock behavior by default or regressing campaign compatibility.

**Estimate:** 2–10 weeks per major enhancement group.

### Prerequisites

- M6 closed for experimentation
- M7 closed before enhancement features are enabled in a public stable release
- Stock compatibility test matrix available

### Tasks

- [ ] **M8-001** Port the known Complete Edition 60 FPS/delta-time patch behind an explicit option, separating the guest timing fix from host presentation rate and VSync policy.
- [ ] **M8-002** Implement and audit a variable-frame-rate route that keeps simulation time correct through sustained 40–59 FPS dips, fixed 60/90/120 FPS targets, VRR-like fluctuation, and VSync-off uncapped presentation. Validate physics, traffic, AI, animation, UI, audio, timers, streaming, input, race logic, cutscenes, and mission scripting rather than inferring correctness from average FPS.
- [ ] **M8-003** Define supported frame-rate modes and their fallback policy: stock 30, validated fixed higher caps, validated dynamic/VRR range, and uncapped only if its full timing matrix passes. A missed target must reduce smoothness rather than slow simulation; UI/config must not expose unverified modes.
- [ ] **M8-004** Add resolution and aspect-ratio controls with correct HUD, culling, projection, and presentation behavior.
- [ ] **M8-005** Validate ultrawide modes and cutscene handling.
- [ ] **M8-006** Add optional motion-blur, MSAA, and imposter-shadow controls using verified addresses or native runtime settings.
- [ ] **M8-007** Separate compatibility fixes from gameplay cheats and accessibility options in UI/config.
- [ ] **M8-008** Evaluate shader precompilation/cache distribution without shipping proprietary shader data improperly.
- [ ] **M8-009** Prototype Linux x86-64/Vulkan only after Windows D3D12 correctness is stable.
- [ ] **M8-010** Create platform-specific build, input, audio, filesystem-case, and package tests for Linux.
- [ ] **M8-011** Benchmark enhancements against the stock mode and document hardware requirements.
- [ ] **M8-012** Run the M7 campaign regression matrix with every enhancement combination intended for release.
- [ ] **M8-013** Run a native macOS ARM64 feasibility gate after the Windows port is stable and the Vulkan backend is validated on Linux. Remove the current ReXGlue `APPLE` hard rejection only on a dedicated branch; inventory Darwin/ARM64 compiler, generated-code model, executable-memory, windowing, input, wheel/FFB, audio, filesystem, crash-reporting, threading, timing, and packaging gaps; probe the Xenos Vulkan backend against MoltenVK portability features and fail closed on any required unsupported behavior.
- [ ] **M8-014** If M8-013 passes, implement and validate a native Apple-silicon build using Vulkan over MoltenVK/Metal or a justified native Metal backend. Require clean-machine boot, frontend, saved gameplay, representative race/city/garage routes, save persistence, controller/audio/window lifecycle, resource telemetry, artifact signing/notarization strategy, and an asset-free package; do not substitute Rosetta-only execution or a window-only boot for native gameplay compatibility.

### Deliverables

- optional, configuration-controlled enhancements
- preserved stock mode
- documented performance and compatibility impact
- Linux build only if it meets its own closure requirements
- native macOS ARM64 build only if its platform/GPU feasibility and clean-machine gameplay gates pass

### Verification evidence

- stock mode remains binary/config behaviorally unchanged where intended
- fixed 60 FPS and every claimed dynamic/uncapped range pass timing-sensitive campaign scenarios, target misses, and frame-pacing transitions without simulation slowdown
- supported resolution/aspect combinations pass screenshot and input tests
- enhancement combinations pass the defined regression subset
- Linux claims are backed by clean-machine test evidence
- macOS ARM64 claims are backed by a native executable, MoltenVK/Metal capability evidence, and clean Apple-silicon gameplay/package results

### Exit criteria

- Enhancements are opt-in unless proven universally safe.
- No enhancement introduces a campaign blocker or save incompatibility.
- Unsupported combinations are rejected or clearly labeled.

### Gate

Enhancements enter M9 packaging only after their individual compatibility gates pass.

### Checkpoint log

- No checkpoint yet. Health: `NOT STARTED`. Next tasks after gate entry: M8-001, M8-002.

## 17. Milestone M9 — Release hardening, packaging, and public distribution

**Goal:** produce a reproducible, legally clean, supportable public release that requires users to supply their own supported game dump.

**Estimate:** 2–6 weeks.

### Prerequisites

- M7 closed
- Selected M8 features individually closed
- No S0/S1 defects
- Release version chosen

### Tasks

- [ ] **M9-001** Freeze the release candidate against exact project and ReXGlue SHAs.
- [ ] **M9-002** Review all code, scripts, dependencies, licenses, and generated artifacts.
- [ ] **M9-003** Run `ast-grep scan`, `ast-grep test`, compiler warnings, sanitizers where supported, unit tests, integration tests, and campaign regression tests.
- [ ] **M9-004** Run secret, proprietary-file, large-file, and license scans over the full Git history and release staging directory.
- [ ] **M9-005** Build an asset-free launcher/installer or portable package that validates the user's dump, performs contained local extraction plus codegen/compilation, atomically publishes a content/toolchain-fingerprinted prepared-game directory, and reuses it on later launches without bundling game assets or generated proprietary guest code.
- [ ] **M9-006** Ensure extraction/import never overwrites the source ISO and never targets broad or unresolved paths.
- [ ] **M9-007** Test install, first launch, data import, update, repair, uninstall, and rollback on clean Windows machines.
- [ ] **M9-008** Verify uninstall never removes user game dumps, saves, unrelated directories, or shared dependencies.
- [ ] **M9-009** Document minimum/recommended hardware, supported OS/GPU APIs, data hash, known issues, logs, and support bundle creation.
- [ ] **M9-010** Define save/config compatibility policy and migration behavior.
- [ ] **M9-011** Create deterministic release archives and checksums.
- [ ] **M9-012** Bump the four-part version only after review and tests pass.
- [ ] **M9-013** Update README-AI, README, changelog, compatibility matrix, build guide, and troubleshooting guide.
- [ ] **M9-014** Explicitly stage only reviewed release files.
- [ ] **M9-015** Commit and push the reviewed release candidate.
- [ ] **M9-016** Verify the exact protected-merge/deployment head SHA.
- [ ] **M9-017** Create and dereference the release tag; verify `HEAD == origin/main == dereferenced release tag` at release parity.
- [ ] **M9-018** Publish checksums and release notes, then perform a post-release clean install from public artifacts.

### Deliverables

- legally clean release package
- reproducible checksums
- clean-machine installation evidence
- complete user and developer documentation
- exact release SHA/tag parity record

### Verification evidence

- two clean Windows installations from the final public artifact succeed
- wrong-region/wrong-hash dumps fail safely with actionable messages
- uninstall preserves source data and saves according to policy
- Git history and release archive contain no prohibited game data or secrets
- packaged runtime passes the same smoke and representative gameplay tests as the development build

### Exit criteria

- Release artifact is reproducible and supportable.
- Exact head SHA and tag parity are verified.
- Required legal/license notices are present.
- Public documentation does not imply inclusion of the game.
- Post-release install test passes.

### Checkpoint log

- No checkpoint yet. Health: `NOT STARTED`. Next tasks after gate entry: M9-001, M9-002.

## 18. Task acceptance criteria register

The criterion below is additional to the task wording and universal Definition of Done. Text after `/` is the minimum evidence.

### M0 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M0-001 | README-AI contains current architecture, all milestones, live-ledger rules, and closure gates / document review |
| M0-002 | `main` repository exists and reports a clean, understood status / `git status` and `git branch --show-current` |
| M0-003 | Every prohibited extension and private/build path is ignored without hiding source files / ignored-fixture audit |
| M0-004 | README states scope, ownership requirement, status, and non-turnkey limitations / rendered README review |
| M0-005 | License is selected, applicable only to original project work, and referenced by README / license review |
| M0-006 | VERSION parses as exactly four numeric components / validation command or test |
| M0-007 | Planned directories exist with no proprietary payload / tree listing and prohibited-file scan |
| M0-008 | ast-grep config discovers rules and tests successfully / `sg scan` and `sg test` outputs |
| M0-009 | Each initial rule has positive and negative fixtures and no false positive in current source / rule-test report |
| M0-010 | Four required docs exist with owners, purpose, and update triggers / documentation inventory |
| M0-011 | Policy explicitly covers Git, release archives, logs, traces, and dumps / policy review |
| M0-012 | Host `gh auth status` shows active `2FED`; remote mutation recheck is documented / sanitized auth output |
| M0-013 | Visibility and remote URL are approved and recorded without pushing unreviewed content / decision-log entry |

### M1 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M1-001 | `clang-cl --version` reports supported 20+ compiler in configured environment / version output |
| M1-002 | Fresh PowerShell locates required CMake/Ninja deterministically / bootstrap transcript |
| M1-003 | Recommended `ast-grep` executable is runnable and reports a pinned/recorded version; project scan/test are gated by M0-008 / command output |
| M1-004 | SDK source resolves to recorded v0.9.0 tag and immutable SHA with submodules complete / SHA manifest |
| M1-005 | ReXGlue installs/builds from documented preset without manual source edits / clean build log |
| M1-006 | A new contributor can follow build.md without undisclosed paths / dry-run review |
| M1-007 | Extractor is provenance-reviewed and lists/extracts this XDVDFS image read-only / tool record and extraction log |
| M1-008 | Script validates all declared identifiers and fails a bad fixture / Pester or equivalent results |
| M1-009 | Script enforces contained destination and safe overwrite behavior / positive and rejection tests |
| M1-010 | Private extraction contains exact expected inventory and hashes / generated manifest |
| M1-011 | All RPF/BIK names and sizes match local manifest / comparison report |
| M1-012 | Private directory explains regeneration and remains ignored / ignore audit |
| M1-013 | Pinned Xenia launches and reports its build identifier / baseline tool log |
| M1-014 | Selected RE/GPU tools open a representative target or documented sample / setup evidence |
| M1-015 | Bootstrap reports every prerequisite and returns nonzero for a missing required tool / test transcript |

### M2 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M2-001 | Exact dump reaches reproducible stock gameplay in pinned Xenia / sanitized run log |
| M2-002 | Local module hash and runtime metadata are captured with private paths removed / baseline report |
| M2-003 | Required visual states have labeled private comparison captures / capture index |
| M2-004 | Save creation/load and controller baseline are repeatable / baseline checklist |
| M2-005 | ReXGlue scaffold configures and builds its empty host target / build log |
| M2-006 | TOML resolves only approved private/generated paths and parses successfully / codegen parse log |
| M2-007 | Config audit confirms every optimization flag is disabled / config review |
| M2-008 | Non-force codegen result and exit code are retained and classified / log artifact |
| M2-009 | Force run produces a complete error inventory without replacing the non-force gate / inventory report |
| M2-010 | Every unique finding has category, owner, severity, and next action / triage table |
| M2-011 | Helper/EH/jump mechanisms have verified addresses or explicit not-used evidence / RE notes |
| M2-012 | Every manual override cites disassembly/control-flow evidence and passes rerun / config diff and log |
| M2-013 | Import records map to symbols or explicit unknown entries with reachability / coverage matrix |
| M2-014 | Minimum startup import set is identified from trace/control flow / startup dependency report |
| M2-015 | Each public patch address is byte-verified or rejected for this module / patch audit |
| M2-016 | Applicable SDK tests pass after analyzer changes / test results |
| M2-017 | Report chooses one allowed decision and justifies effort, risks, and blockers / approved report |

### M3 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M3-001 | Clean CMake build consumes generated/private inputs without tracking them / build and Git audit |
| M3-002 | ReXApp lifecycle hooks are minimal, documented, and exercised / startup log |
| M3-003 | Runtime verifies base, entry point, and dispatch range before launch / assertion/test log |
| M3-004 | Guest disc paths resolve expected files and reject escape paths / VFS tests |
| M3-005 | Every implemented kernel import has behavior test or trace comparison / import test matrix |
| M3-006 | Every implemented XAM import has behavior test or trace comparison / import test matrix |
| M3-007 | Offline stubs expose intentional state and never hide required failure / stub review/tests |
| M3-008 | Repeated startup produces bounded, consistent timing/thread ordering / trace comparison |
| M3-009 | Synthetic crash report contains required context and excludes guest memory by default / crash fixture |
| M3-010 | All required categories can be filtered and written safely / logging test |
| M3-011 | If Bink blocks, skip-intro is off by default and byte-guarded; otherwise an unpatched trace proves the earlier blocker and zero patch state / trace and decision tests |
| M3-012 | Debug, RelWithDebInfo, and Release clean builds succeed / build matrix |
| M3-013 | No unresolved link or reachable startup PPC trap remains / build and smoke logs |
| M3-014 | Smoke test enforces timeout and expected ordered markers / automated result |
| M3-015 | Ten launch/exit cycles leave no process and preserve approved data / cycle report |

### M4 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M4-001 | Twenty physical-evidence runs each bind at least three monotonic guest-output sequences to successful D3D12 presents, recompute a nontrivial same-or-later readback, dwell without device loss, close without force cleanup/orphans/drift, and include one private human game-derived-output classification / logs, captures, physical verifier, and sanitized aggregate |
| M4-002 | Scoped host-RTV target/depth/MSAA/table-gamma paths are physically audited and stable title UI regions match the private baseline within documented tolerance / GPU comparison |
| M4-003 | The intro route is fail-closed as verified playback, a reproducibly justified safe development skip, or a repeatable unpatched guest bypass; the selected branch and remaining Bink-fidelity scope are explicit / decision, physical trace, and tests |
| M4-004 | Three isolated title runs prove slot-0 local identity plus distinct signed-out/absent slots 1-3; deterministic profile defaults pass focused SDK tests, while unhit optional XAM routes remain explicitly unclaimed / physical profile matrix and focused tests |
| M4-005 | One selected controller maps correctly to user slot 0 / input trace |
| M4-006 | Dead-zone, reconnect, and focus scenarios pass / controller checklist |
| M4-007 | UI audio/music run five minutes without fatal underrun/distortion / audio log |
| M4-008 | XMP transitions are state-correct or explicitly unsupported without softlock / transition tests |
| M4-009 | Offline service checks finish without network access or frontend block / network-disabled test |
| M4-010 | Supported locale and paths load expected resources / locale matrix |
| M4-011 | Frontend route is repeatable from documented steps / automation or signed checklist |
| M4-012 | Required baseline comparisons are indexed and reviewed / comparison report |
| M4-013 | Every known deviation has severity, workaround, and target milestone / known-issues audit |

### M5 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M5-001 | Canonical route is fully specified and reproducible / test-case document |
| M5-002 | Route streams with zero unexpected VFS misses / VFS trace |
| M5-003 | Listed visual categories are present and usable / labeled captures |
| M5-004 | Target ordering/depth defects are fixed or bounded as accepted S2 / GPU validation report |
| M5-005 | Representative shaders/textures match baseline / capture and shader audit |
| M5-006 | All driving and pause controls work through full route / input checklist |
| M5-007 | FFB behaves correctly or degrades without blocking / device matrix |
| M5-008 | Physics/game speed match stock baseline over fixed route / timing measurements |
| M5-009 | Listed audio classes play and transition correctly / audio event matrix |
| M5-010 | No fatal allocator, I/O, or streaming event occurs / sanitized route log |
| M5-011 | Result survives restart or approved M6 blocker is recorded / save evidence |
| M5-012 | One full event series reaches final results and free roam, then its changed save loads into controllable optimized-Release gameplay / route checkpoints plus restart report |
| M5-013 | Five repetitions show bounded resource growth / telemetry report |
| M5-014 | Vertical-slice report links every required artifact / report review |

### M6 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M6-001 | Coverage route crosses all major regions without fatal stream failure / route log |
| M6-002 | Garage lifecycle works across save/restart / feature checklist |
| M6-003 | Representative instance of every race-system category completes / matrix |
| M6-004 | Listed environment effects pass day/night comparison / capture matrix |
| M6-005 | Save matrix including interruption and full storage passes safely / destructive-scenario test |
| M6-006 | Profile/settings persist only intended values / restart tests |
| M6-007 | Two-hour audio soak has no fatal underrun/leak / telemetry |
| M6-008 | Every retired service has explicit offline behavior and progression test / service matrix |
| M6-009 | Unlock policy is approved and separates compatibility from cheats / decision log |
| M6-010 | Window/device lifecycle scenarios pass / platform checklist |
| M6-011 | Config validates values, defaults, and bad input / config tests |
| M6-012 | Required telemetry is timestamped and correlates to guest events / sample trace |
| M6-013 | All reached unsupported operations are fixed or classified non-blocking / coverage report |
| M6-014 | Each required two-hour soak completes without S0/S1 / soak reports |
| M6-015 | SDL wheel-class devices use configurable controls, guest wheel subtype, latest-active routing, and physically bounded FFB / unit tests plus T300 input/force matrix |
| M6-016 | Alpha matrix names tested builds, hardware, paths, and limitations / review |

### M7 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M7-001 | Matrix enumerates mandatory campaign/content nodes and dependencies / reviewed matrix |
| M7-002 | Core progression chain completes from clean save and a validated persistent local profile name can be changed without fabricating Live identity / playthrough and restart log |
| M7-003 | Every event category has win/fail/retry evidence / matrix |
| M7-004 | All vehicle classes and customization categories are exercised / coverage sheet |
| M7-005 | South Central mandatory content loads and completes / coverage evidence |
| M7-006 | Scripted media and subtitles pass representative and blocker cases / media matrix |
| M7-007 | Listed transitions complete without stream softlock / transition log |
| M7-008 | Gameplay edge cases recover without progression corruption / scenario tests |
| M7-009 | Save recovery tests preserve valid prior state / recovery report |
| M7-010 | Every claimed language boots and reaches gameplay / locale matrix |
| M7-011 | First clean campaign reaches completion with defects logged / run record |
| M7-012 | Each progression blocker has regression evidence after fix / defect links |
| M7-013 | Second clean campaign completes on release-like build / run record |
| M7-014 | Reached stubs are state-correct or documented unavailable / final import audit |
| M7-015 | Compatibility docs match both full-run evidence sets / documentation review |

### M8 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M8-001 | 60 FPS/delta-time patch is byte-guarded, optional, disabled by default, and independent of host VSync/cap selection / patch and presentation-policy tests |
| M8-002 | Timing-sensitive systems pass stock 30, sustained 40–59 dips, fixed 60/90/120, fluctuating/VRR-like, and VSync-off uncapped comparisons without simulation slowdown / timing and campaign matrix |
| M8-003 | UI exposes only validated stock, fixed-cap, dynamic-range, or uncapped modes and falls back safely after missed targets / config/UI/frame-pacing tests |
| M8-004 | Claimed aspect ratios render HUD/world correctly / capture matrix |
| M8-005 | Ultrawide gameplay and cutscene policy are documented and tested / report |
| M8-006 | Each graphics option independently toggles and restores stock behavior / option tests |
| M8-007 | Compatibility, accessibility, and cheat categories are visibly separated / UI review |
| M8-008 | Cache design passes legal/content audit and invalidation tests / audit |
| M8-009 | Linux prototype builds without weakening Windows correctness / build evidence |
| M8-010 | Linux-specific filesystem/input/audio/package matrix passes / results |
| M8-011 | Benchmarks are reproducible with hardware/build/config recorded / benchmark report |
| M8-012 | Intended option combinations pass campaign regression scope / regression report |
| M8-013 | ReXGlue Darwin/ARM64 and MoltenVK/Metal feasibility inventory closes every required CPU, OS, GPU, input/audio/storage, and packaging dependency as supported or an explicit blocker / feasibility report and compile/capability probes |
| M8-014 | Native Apple-silicon package reaches representative gameplay and persistence on a clean Mac without Rosetta or proprietary bundled data / native artifact, route matrix, and package audit |

### M9 criteria

| ID | Individual acceptance criterion / minimum evidence |
|---|---|
| M9-001 | RC records exact project, SDK, compiler, and dependency SHAs/versions / manifest |
| M9-002 | Review has no unresolved release-blocking finding / signed review summary |
| M9-003 | Entire required analysis/test suite passes on RC / consolidated test report |
| M9-004 | Git history and staging scans find no secret/proprietary violation / scan reports |
| M9-005 | Asset-free package validates the supported dump, locally prepares an atomic fingerprinted game directory, reuses it without reimport on ordinary launches, and contains no game/generated proprietary content / clean-machine prepare-reuse and content audit |
| M9-006 | Import rejects broad/unresolved targets and preserves source ISO / safety tests |
| M9-007 | Install/update/repair/uninstall/rollback matrix passes clean machines / matrix |
| M9-008 | Uninstall preservation tests prove user data safety / filesystem diff |
| M9-009 | User documentation covers all claimed requirements and diagnostics / doc review |
| M9-010 | Save/config compatibility and migration are tested and documented / migration report |
| M9-011 | Two clean builds produce matching intended artifacts/checksums / reproducibility report |
| M9-012 | Version bump follows highest-impact included change after tests / version review |
| M9-013 | All named docs match RC behavior / documentation diff review |
| M9-014 | Staged set equals reviewed release manifest / `git diff --cached` review |
| M9-015 | Commit/push succeed only after gates and point to approved RC / remote evidence |
| M9-016 | Protected operation SHA equals approved RC SHA / SHA record |
| M9-017 | HEAD, origin/main, and dereferenced tag are identical / parity command output |
| M9-018 | Public artifact clean-installs and checksums match published values / post-release report |

## 19. Risk register

| ID | Risk | Impact | Mitigation | First gate |
|---|---|---|---|---|
| R-001 | ReXGlue cannot reconstruct critical RAGE control flow | Critical | Manual functions/switch tables; analyzer fixes; SDK fork | M2 |
| R-002 | Unsupported PPC instructions or exceptions are widespread | Critical | Opcode inventory, reachability analysis, SDK tests | M2–M3 |
| R-003 | Xenos EDRAM/ROV behavior causes major rendering defects | High | D3D12-first, Xenia comparison, RenderDoc/PIX | M4–M5 |
| R-004 | XMA/XAUD/XMP streaming is unstable | High | Audio diagnostics, long-session tests, upstream fixes | M4–M6 |
| R-005 | XONLINE/retired service checks block progression | High | Explicit offline state model and progression tests | M4–M7 |
| R-006 | Save/content APIs corrupt progression | Critical | Atomic host storage, backups, interruption tests | M5–M7 |
| R-007 | 60 FPS changes physics or scripting | High | Stock 30 FPS baseline; opt-in timing matrix | M8 |
| R-008 | Proprietary data enters Git or packages | Critical | Ignore rules, staged-file scan, history/release audit | M0/M9 |
| R-009 | ReXGlue updates break the project | High | Pin SHA; explicit upgrade branches and regression suite | all |
| R-010 | Build/generated code is too large for contributors | Medium | Disk check, split TUs, PCH, incremental presets | M1–M3 |
| R-011 | Tooling paths depend on one developer machine | Medium | Bootstrap scripts and clean-environment tests | M1/M9 |
| R-012 | External patches do not match the local XEX | High | Verify module hash and bytes before applying | M2/M8 |
| R-013 | Variable or uncapped FPS leaves hidden fixed-step physics, scripting, streaming, input, audio, or cutscene assumptions | High | Preserve stock 30; separate delta-time from presentation pacing; test sustained dips, transitions, high caps, and campaign-critical sequences before exposing each mode | M8 |
| R-014 | ReXGlue or the Xenos Vulkan backend depends on Windows/Linux behavior or Vulkan features unavailable through MoltenVK/Metal | High | Dedicated Darwin/ARM64 feasibility branch; capability inventory and shader/render-path probes before promising or packaging macOS support | M8 |

## 20. Decision log

Record architecture decisions here or link to ADR files.

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-08-10 | Use ReXGlue rather than bare XenonRecomp as primary base | ReXGlue supplies the runtime subsystems required by a full game | Accepted |
| 2026-08-10 | Target Windows x86-64/D3D12 first | Best fit for current environment and MCLA's likely graphics requirements | Accepted |
| 2026-08-10 | Preserve stock 30 FPS until gameplay correctness | Known 60 FPS mode requires delta-time changes and broader validation | Accepted |
| 2026-08-29 | Treat variable/unlocked FPS as a separate validated timing mode, not merely a disabled present cap | The known Complete Edition patch enables delta-time, but missed targets and 40–120+ FPS transitions can still expose fixed-step game systems | Planned for M8 |
| 2026-08-29 | Gate native macOS ARM64 behind ReXGlue Darwin/ARM64 and MoltenVK/Metal feasibility | Generated code and Vulkan are ARM64-capable in principle, while the current SDK explicitly supports only Windows/Linux and may require unavailable portability features | Planned for M8 |
| 2026-08-10 | Keep game assets and generated proprietary output private | Legal and repository hygiene requirement | Accepted |
| 2026-08-10 | Use `https://github.com/2FED/RE-MCLA.git` as the project remote | Repository supplied by project owner | Accepted |
| 2026-08-10 | Commit after every closed task; closure commit and push after every closed milestone | Preserve auditable task boundaries and milestone gates | Accepted |
| 2026-08-14 | Enter M5 through the pinned post-OOBE save | M4 proves a repeatable saved free-roam/frontend route while first-run OOBE remains explicitly unverified | Accepted |

## 21. Immediate next task queue

Execute in this order unless a new blocker changes the plan:

1. [x] Close M0 repository foundation and governance.
2. [x] Close M1 reproducible toolchain and source ingestion at version `0.1.0.0`.
3. [x] Complete M2-001 through M2-012: baseline, deterministic codegen, finding triage, and manual analysis configuration.
4. [x] Complete M2-013: map all XAM/xboxkrnl import records and static reachability classes.
5. [x] Complete M2-016: add SDK regression coverage for the twenty FLOAT16_4 sites.
6. [x] Complete M2-017 and close M2 at version `0.2.0.0` with `GO WITH SDK FORK`.
7. [x] Complete M3-001 and M3-002: generated integration and the minimal project lifecycle are verified; the Windows Unicode-path S1 remains fixed in the current ReXGlue v0.9.0.10 fork.
8. [x] Complete M3-003 and M3-004: guest image/entry/dispatch plus private game-root VFS/write containment are verified.
9. [x] Complete M3-005 and M3-006: minimum stock-route xboxkrnl and XAM imports are runtime- and behavior-verified.
10. [x] Complete M3-007: explicit logged offline-service behavior is deterministic and regression-tested.
11. [x] Complete M3-008: deterministic 50-MHz timebase and bounded guest-thread startup ordering are verified.
12. [x] Complete M3-009: privacy-safe actionable C++ guest crash reporting is verified.
13. [x] Complete M3-010: structured subsystem categories are independently filterable and safely written.
14. [x] Complete M3-011: Bink is not the first native blocker, so no skip-intro patch is justified or added.
15. [x] Complete M3-012: Debug, RelWithDebInfo, and Release clean-build and stage their exact runtime, Tracy, and Xenos artifacts.
16. [x] Complete M3-013: project-default Xenos startup survives the bounded window with no reachable fatal, invalid-function, or `PPC_UNIMPLEMENTED` trap.
17. [x] Complete M3-014: the canonical bounded smoke fail-closes on exact supported image identity and requires all expected subsystem markers.
18. [x] Complete M3-015: ten crash probes plus ten controlled normal launch/exit cycles prove private-root isolation, source-data integrity, zero orphan processes, and a localized/contained KI-010 disposition.
19. [x] Close M3 at version `0.3.0.0` with `GO M4` after the aggregate review, public SDK fetch verification, documentation update, closure commit, and push.
20. [x] Complete M4-001: twenty physical-evidence cycles and a project-owner
    visual PASS verify the first guest-derived frame without claiming frontend
    reachability or render correctness.
21. [x] Complete M4-002: validate the scoped host-RTV frontend render-target,
    staged EDRAM resolve, depth, MSAA, table-gamma, shader/PSO, and draw paths
    against stable UI regions from the private Xenia baseline.
22. [x] Complete M4-003: three physical noisy-I/O runs classify the supported
    native boot as a stable unpatched guest-selected Bink bypass; actual Bink
    playback fidelity remains explicitly deferred to M7-006.
23. [x] Complete M4-004: verify the reached single-local-user XAM contract at
    the title frontend and unit-test the deterministic profile defaults.
24. [x] Complete M4-005: map one selected SDL controller to Xbox user slot 0.
25. [x] Complete M4-006: validate one selected controller's standard surface,
    raw-XInput threshold classification, disconnect/reconnect, focus-loss
    behavior, and the separately scoped host-rumble diagnostic; multi-pad
    behavior remains unit-tested rather than physically claimed.
26. [x] Complete M4-007 through M4-011: verify sustained frontend audio, XMP
    fallback, offline services, locale/paths, and the repeatable saved frontend
    route.
27. [x] Complete M4-012: index and review 1280x720 and 2560x1440 frontend
    comparisons plus the bounded Xenia/native audio-lifecycle comparison.
28. [x] Complete M4-013 and close M4 after the aggregate known-issues,
    verification-count, milestone-gate, version, and release review.

Do not skip directly to enhancement patches, 60 FPS, or public packaging.

## 22. README-AI maintenance checklist

Update this document when any of the following changes:

- supported game hash, edition, Title ID, Media ID, or title update
- ReXGlue/Xenia/compiler version
- build or bootstrap command
- repository layout
- architecture or hook strategy
- milestone status or gate decision
- supported platform, renderer, controller, audio path, or frame-rate mode
- known legal/distribution constraint
- release workflow, version, or compatibility claim

Before closing a milestone, verify that this file reflects the actual repository rather than the intended repository.
