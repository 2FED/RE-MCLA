# Known issues

Owner: MCLA-R maintainers

Purpose: maintain user-visible and developer-relevant limitations with severity, affected builds, workaround, owner, and target milestone.

## Current limitations

| ID | Severity | Status | Description | Workaround | Target |
|---|---|---|---|---|---|
| KI-001 | S1 | Open | Canonical native startup verifies the Complete Edition title, local profile, one-pad controller matrix, basic audio, saved free roam, one complete Ian event series, and one representative garage purchase/customization lifecycle whose changed save reloads in a fresh Release process. M6-005 additionally proves transactional interruption recovery, malformed-metadata rejection, and storage-full handling in isolated platform tests. The garage route explicitly selects offline-compatible guest sign-in state 2, but first-run OOBE and generalized profile availability/persistence remain unvalidated. A separate visual-only diagnostic contained the exact completed save on disk yet showed the title's not-signed-in prompt under a different route. | Use the accepted completed save and explicit state-2 compatibility option for bounded development routes, or stock Xenia for clean-new-game behavior; do not treat the current single-user route as a complete account/profile implementation. | M6-006, M7-002 |
| KI-002 | S1 | Closed | ReXGlue feasibility for this exact XEX was unmeasured; M2 now has deterministic diagnostic-free codegen and a `GO WITH SDK FORK` decision. | See `docs/evidence/M2-feasibility-report.md`. | M2 |
| KI-003 | S2 | Open | M4-003 proves that the supported unpatched guest route reaches the verified title in 3/3 runs without post-launch Bink access, so the intro is not a native startup blocker and no skip patch is justified. Actual Bink decode/playback fidelity remains unverified. M4-012 separately closes stable frontend-region comparison at two resolutions, but not whole-frame or gameplay parity. | Keep the unpatched guest-selected bypass; validate representative Bink playback/skip behavior in M7-006 and broader gameplay rendering in M5/M6. | M5-003, M6-004, M7-006 |
| KI-004 | S2 | Open | MCLA-R currently requires its pinned ReXGlue v0.9.0.23 fork for valid FLOAT16_4 mask-3 diagnostics, Unicode-safe Windows paths/log filenames, configured Xbox language selection, safe input/window teardown, fail-closed read-only VFS behavior including mounted-root save access, transactional save interruption/corruption/storage-full handling, deterministic direct and XLiveBase offline-service results, Xenia-compatible guest-thread startup ordering, privacy-safe guest crash context, guest-backed D3D12 presentation/render/timing auditing, bounded single-local-user XAM profile auditing, causal SDL controller auditing, privacy-safe audio event windows, and the state-correct XMP fallback. | The submodule pin is automatic; do not replace it with upstream v0.9.0 or unpinned `main`. | SDK/upstream |
| KI-005 | S1 | Closed | ReXApp destroyed the Window before Runtime/input drivers, causing a teardown use-after-free after XEX load. | Fixed in fork v0.9.0.3; see `docs/evidence/M3-003-module-config.md`. | SDK/upstream #336 |
| KI-006 | S1 | Closed | Read-only HostPathDevice write opens were downgraded, while rename and writable mappings could bypass the device policy. | Fixed fail-closed in fork v0.9.0.4; see `docs/evidence/M3-004-vfs-disc-root.md`. | SDK/upstream pending approval |
| KI-007 | S2 | Open | Generic ReXGlue `REX_EXPORT_STUB` preserves caller `r3` for return-bearing exports. MCLA-R's ten direct offline-service imports are fixed, but the broader SDK inventory remains unclassified. | Keep the exact v0.9.0.23 fork pin; do not add a return-bearing generic stub. | SDK/upstream #407 |
| KI-008 | S2 | Open | ReXGlue guest threads omitted Xenia's 10-ms compatibility grace period and could race creator-side shared-state initialization. | Fixed since fork v0.9.0.6 and retained in v0.9.0.23; keep the exact pin until upstream resolves the report. | SDK/upstream #408 |
| KI-009 | S2 | Open | Upstream ReXGlue lets generated C++ exceptions escape guest `XThread` execution without structured guest PC/function/thread/import context. | Fixed and regression-tested in fork v0.9.0.7 for C++ exceptions; hardware SEH/signals and fatal aborts remain separate work. | SDK/upstream #409 |
| KI-010 | S2 | Contained | Repeated probes reproduced the post-report hang on both cold and warm runs. Thread-only CDB localized it to ReXGlue force-terminating the Audio Worker and then blocking in `XThread::FreeStack -> BaseHeap::Release` on a poisoned guest-heap mutex. | The guest-free synthetic probe closes its report, emits shutdown, flushes, and hard-exits; the final 10/10 probes and independent 10/10 normal `WM_CLOSE` cycles exit 0 with no cleanup/orphans. A sanitized upstream issue draft is pending explicit publication approval. | SDK/upstream |
| KI-011 | S1 | Closed | Normal launches now select the staged Xenos plugin by project default; the verified route initializes graphics interrupts/pipelines and audio without a no-GPU marker or post-launch Bink blocker. | Fixed and bounded in `docs/evidence/M3-013-startup-traps.md`; guest-free probes intentionally remain GPU-independent. | M3-013 |
| KI-012 | S2 | Closed | Non-Release ReXGlue host tests could call a profiled `REX_HOOK` before `Runtime::Setup` and dereference Tracy's null manual-lifetime profiler at address `0x2C8`. ReXGlue v0.9.0.10 makes hook/debug instrumentation conditional on `TracyIsStarted`; the focused `[offline]` suite now passes 3/3 test cases and 10/10 assertions without starting Tracy. | Fixed and source-regression-tested by `scripts/test-sdk-profiling-lifetime.ps1`; upstream issue #410 remains the public report. | SDK/upstream #410 |
| KI-013 | S2 | Open | The artifact previously described from one title frame as a minor green vehicle shadow is a reproducible vehicle-lighting defect. Three fixed-camera native gameplay captures show the right rear body highlight changing from red to excessively bright neon green and then disappearing in step with a nearby traffic signal. Console-reference gameplay near red/green signals does not show comparable body-color amplification. The defect is therefore not broad shadow corruption, but an over-strength or incorrect transfer of saturated local-light/specular color on vehicle paint. M6-004 now proves representative `RAINY` / `DAWN` wet-road and local-light reflections are present, but explicitly does not treat that category PASS as reflection parity or a fix. The gamma-render-target UNorm16 storage behavior aligns with current Xenia and accepted gameplay uses table rather than PWL output gamma; HDR light accumulation, packed-float/blend, ownership transfer, and resolve remain candidates. | Preserve the fixed-camera repro; first A/B the same scene on host-RTV versus ROV and capture the lighting-pass formats/blend state before changing gamma or material behavior. | Graphics follow-up |
| KI-014 | S2 | Contained | ReXGlue has no XMP decoder or system music library. The reached title route only polls status, while autonomous frontend audio uses the separately verified XMA/XAudio/SDL path. | The v0.9.0.15 behavior is retained in v0.9.0.23: unsupported XMP playback/output capture fails explicitly and remains `Idle`; do not claim user-selected music playback until a decoder-backed route is implemented and tested. | M6-007 |
| KI-015 | S2 | Open | Some traffic-signal housings and foliage render with a visible stippled/partially transparent coverage pattern instead of a stable opaque or alpha-tested silhouette. The pattern is visible in the fixed-camera KI-013 screenshots, and the owner reports that the affected signals and trees shimmer in motion. M6-004 preserves all three fixed-camera hashes and proves broader Rainy/Dawn environment effects remain usable, but does not sample the defect at the cadence needed to claim temporal stability. Source comparison confirms the Xenos 2x2 dithered alpha-to-mask implementation matches current Xenia and fuzzy alpha epsilon remains default-off; alpha-to-mask/MSAA mapping, texture LOD, or resolve behavior remain candidates. | Preserve the fixed-camera captures; compare identical motion on host-RTV/ROV and record reached alpha-test/alpha-to-mask, guest MSAA, texture LOD, and resolve state before changing thresholds or sample masks. | Graphics follow-up |
| KI-016 | S2 | Open | During the accepted M5-013 five-race session, the owner observed intermittent minimap flicker. M6-004 rebinds that five-race result but captures only six sparse environment frames; absence of a flicker there is explicitly not evidence of a fix. The existing checkpoint images/logs prove the HUD remains usable without renderer/device failure but do not identify whether the transient comes from HUD render-target reuse, texture upload, compositing, or resolve ordering. | Capture a high-cadence private HUD ROI plus render-target/resolve telemetry during a reproducible flicker before changing texture or compositing behavior. | Graphics follow-up |

Do not remove an issue without linking verification evidence. Update this document when a new recurring defect is discovered, severity changes, a workaround changes, or a release claim is added.
