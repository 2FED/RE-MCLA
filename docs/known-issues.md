# Known issues

Owner: MCLA-R maintainers

Purpose: maintain user-visible and developer-relevant limitations with severity, affected builds, workaround, owner, and target milestone.

## Current limitations

| ID | Severity | Status | Description | Workaround | Target |
|---|---|---|---|---|---|
| KI-001 | S1 | Open | Canonical native startup now autonomously verifies the Complete Edition title, local profile, standard one-pad controller matrix, sustained basic XMA/XAudio/SDL audio, and a pinned post-OOBE route through saved free roam, pause, and Settings/Options. First-run OOBE, event-level audio identity/fidelity, race selection/completion, detailed gameplay correctness, multi-pad behavior, title-driven force feedback, and general playability are not yet validated. | Use the original game or Xenia; do not treat the bounded saved-route evidence as a playable release. | M4–M5 |
| KI-002 | S1 | Closed | ReXGlue feasibility for this exact XEX was unmeasured; M2 now has deterministic diagnostic-free codegen and a `GO WITH SDK FORK` decision. | See `docs/evidence/M2-feasibility-report.md`. | M2 |
| KI-003 | S2 | Open | M4-003 proves that the supported unpatched guest route reaches the verified title in 3/3 runs without any post-launch Bink file access, so the intro is not a native startup blocker and no skip patch is justified. Actual Bink decode/playback fidelity remains unverified, as do ROV, PWL gamma, true-direct resolves, whole-frame/frontend/gameplay parity, audio correctness, and deeper post-boundary offline-service behavior. | Keep boot-route claims scoped to `docs/evidence/M4-003-intro-route-decision.md`; validate representative Bink playback and skip behavior in M7-006, with broader parity in M4-012 and later. | M4–M7 |
| KI-004 | S2 | Open | MCLA-R currently requires its pinned ReXGlue v0.9.0.18 fork for valid FLOAT16_4 mask-3 diagnostics, Unicode-safe Windows paths/log filenames, configured Xbox language selection, safe input/window teardown, fail-closed read-only VFS behavior including mounted-root save access, deterministic direct and XLiveBase offline-service results, Xenia-compatible guest-thread startup ordering, privacy-safe guest crash context, guest-backed D3D12 presentation/render auditing, bounded single-local-user XAM profile auditing, causal SDL controller auditing, and the state-correct XMP fallback. | The submodule pin is automatic; do not replace it with upstream v0.9.0 or unpinned `main`. | SDK/upstream |
| KI-005 | S1 | Closed | ReXApp destroyed the Window before Runtime/input drivers, causing a teardown use-after-free after XEX load. | Fixed in fork v0.9.0.3; see `docs/evidence/M3-003-module-config.md`. | SDK/upstream #336 |
| KI-006 | S1 | Closed | Read-only HostPathDevice write opens were downgraded, while rename and writable mappings could bypass the device policy. | Fixed fail-closed in fork v0.9.0.4; see `docs/evidence/M3-004-vfs-disc-root.md`. | SDK/upstream pending approval |
| KI-007 | S2 | Open | Generic ReXGlue `REX_EXPORT_STUB` preserves caller `r3` for return-bearing exports. MCLA-R's ten direct offline-service imports are fixed, but the broader SDK inventory remains unclassified. | Keep the exact v0.9.0.18 fork pin; do not add a return-bearing generic stub. | SDK/upstream #407 |
| KI-008 | S2 | Open | ReXGlue guest threads omitted Xenia's 10-ms compatibility grace period and could race creator-side shared-state initialization. | Fixed since fork v0.9.0.6 and retained in v0.9.0.18; keep the exact pin until upstream resolves the report. | SDK/upstream #408 |
| KI-009 | S2 | Open | Upstream ReXGlue lets generated C++ exceptions escape guest `XThread` execution without structured guest PC/function/thread/import context. | Fixed and regression-tested in fork v0.9.0.7 for C++ exceptions; hardware SEH/signals and fatal aborts remain separate work. | SDK/upstream #409 |
| KI-010 | S2 | Contained | Repeated probes reproduced the post-report hang on both cold and warm runs. Thread-only CDB localized it to ReXGlue force-terminating the Audio Worker and then blocking in `XThread::FreeStack -> BaseHeap::Release` on a poisoned guest-heap mutex. | The guest-free synthetic probe closes its report, emits shutdown, flushes, and hard-exits; the final 10/10 probes and independent 10/10 normal `WM_CLOSE` cycles exit 0 with no cleanup/orphans. A sanitized upstream issue draft is pending explicit publication approval. | SDK/upstream |
| KI-011 | S1 | Closed | Normal launches now select the staged Xenos plugin by project default; the verified route initializes graphics interrupts/pipelines and audio without a no-GPU marker or post-launch Bink blocker. | Fixed and bounded in `docs/evidence/M3-013-startup-traps.md`; guest-free probes intentionally remain GPU-independent. | M3-013 |
| KI-012 | S2 | Closed | Non-Release ReXGlue host tests could call a profiled `REX_HOOK` before `Runtime::Setup` and dereference Tracy's null manual-lifetime profiler at address `0x2C8`. ReXGlue v0.9.0.10 makes hook/debug instrumentation conditional on `TracyIsStarted`; the focused `[offline]` suite now passes 3/3 test cases and 10/10 assertions without starting Tracy. | Fixed and source-regression-tested by `scripts/test-sdk-profiling-lifetime.ps1`; upstream issue #410 remains the public report. | SDK/upstream #410 |
| KI-013 | S3 | Open | One accepted native title frame shows a green-tinted shadow beneath a vehicle near the lower-left edge. The owner classified the title frame as a visual PASS and this isolated artifact as minor/non-blocking for M4-002, but its compositing/shadow cause is not diagnosed. | Treat as a known visual deviation; compare and diagnose during M4-012 parity work. | M4-012 |
| KI-014 | S2 | Contained | ReXGlue has no XMP decoder or system music library. The reached title route only polls status, while autonomous frontend audio uses the separately verified XMA/XAudio/SDL path. | The v0.9.0.15 behavior is retained in v0.9.0.18: unsupported XMP playback/output capture fails explicitly and remains `Idle`; do not claim user-selected music playback until a decoder-backed route is implemented and tested. | M4-012 / later audio scope |

Do not remove an issue without linking verification evidence. Update this document when a new recurring defect is discovered, severity changes, a workaround changes, or a release claim is added.
