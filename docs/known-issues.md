# Known issues

Owner: MCLA-R maintainers

Purpose: maintain user-visible and developer-relevant limitations with severity, affected builds, workaround, owner, and target milestone.

The tested-build and route-level support summary is maintained separately in
[`alpha-compatibility.md`](alpha-compatibility.md). This file remains the
canonical source for issue identity, severity, status, workaround, and target.

## Current limitations

| ID | Severity | Status | Description | Workaround | Target |
|---|---|---|---|---|---|
| KI-001 | S1 | Open | Canonical native startup verifies the Complete Edition title, local profile, one-pad controller matrix, basic audio, saved free roam, one complete Ian event series, and one representative garage purchase/customization lifecycle whose changed save reloads in a fresh Release process. M6-005 proves transactional save recovery, while M6-006 adds isolated typed persistence/recovery for known standard XAM settings and binds language/controller TOML restart tests. MCLA does not import the profile write API, and first-run OOBE/account selection remain unvalidated; a separate visual-only diagnostic contained the completed save yet showed the not-signed-in prompt under a different route. | Use the accepted completed save and explicit state-2 compatibility option for bounded development routes, or stock Xenia for clean-new-game behavior; do not treat the isolated M6-006 platform matrix as native first-run/account proof. | M7-002 |
| KI-002 | S1 | Closed | ReXGlue feasibility for this exact XEX was unmeasured; M2 now has deterministic diagnostic-free codegen and a `GO WITH SDK FORK` decision. | See `docs/evidence/M2-feasibility-report.md`. | M2 |
| KI-003 | S2 | Open | M4-003 proves that the supported unpatched guest route reaches the verified title in 3/3 runs without post-launch Bink access, so the intro is not a native startup blocker and no skip patch is justified. Actual Bink decode/playback fidelity remains unverified. M4-012 separately closes stable frontend-region comparison at two resolutions, but not whole-frame or gameplay parity. | Keep the unpatched guest-selected bypass; validate representative Bink playback/skip behavior in M7-006 and broader gameplay rendering in M5/M6. | M5-003, M6-004, M7-006 |
| KI-004 | S2 | Open | MCLA-R currently requires its pinned ReXGlue v0.10.0.1 fork for valid FLOAT16_4 mask-3 diagnostics, Unicode-safe Windows paths/log filenames, configured Xbox language selection, safe input/window teardown, fail-closed read-only VFS behavior including mounted-root save access, transactional save interruption/corruption/storage-full handling, typed standard-profile persistence, deterministic direct, XLiveBase, and offline-progression service results, Xenia-compatible guest-thread startup ordering, privacy-safe guest crash context, guest-backed D3D12 presentation/render/timing auditing, bounded single-local-user XAM profile auditing, stable latest-active SDL controller routing across virtual mirrors, model-agnostic wheel input and title force feedback, privacy-safe audio event windows, long-session audio pause/default-device recovery, restart-safe host audio device/volume configuration, timestamped CPU/GPU/stream/audio-underrun/shader/PSO telemetry, explicit reached-import compatibility results, and the state-correct XMP fallback. | The submodule pin is automatic; do not replace it with upstream v0.9.0 or unpinned `main`. | SDK/upstream |
| KI-005 | S1 | Closed | ReXApp destroyed the Window before Runtime/input drivers, causing a teardown use-after-free after XEX load. | Fixed in fork v0.9.0.3; see `docs/evidence/M3-003-module-config.md`. | SDK/upstream #336 |
| KI-006 | S1 | Closed | Read-only HostPathDevice write opens were downgraded, while rename and writable mappings could bypass the device policy. | Fixed fail-closed in fork v0.9.0.4; see `docs/evidence/M3-004-vfs-disc-root.md`. | SDK/upstream pending approval |
| KI-007 | S2 | Open | Generic ReXGlue `REX_EXPORT_STUB` preserves caller `r3` for return-bearing exports. MCLA-R's reached direct/offline/progression surface is classified through M6-013, but twelve imported xboxkrnl stubs remain not-observed/deferred and the broader SDK inventory is not claimed complete. | Keep the exact v0.10.0.1 fork pin; do not add a return-bearing generic stub. Reopen the reached report if a deferred import executes. | SDK/upstream #407 |
| KI-008 | S2 | Open | ReXGlue guest threads omitted Xenia's 10-ms compatibility grace period and could race creator-side shared-state initialization. | Fixed since fork v0.9.0.6 and retained in v0.10.0.1; keep the exact pin until upstream resolves the report. | SDK/upstream #408 |
| KI-009 | S2 | Open | Upstream ReXGlue lets generated C++ exceptions escape guest `XThread` execution without structured guest PC/function/thread/import context. | Fixed and regression-tested in fork v0.9.0.7 for C++ exceptions; hardware SEH/signals and fatal aborts remain separate work. | SDK/upstream #409 |
| KI-010 | S2 | Contained | Repeated probes reproduced the post-report hang on both cold and warm runs. Thread-only CDB localized it to ReXGlue force-terminating the Audio Worker and then blocking in `XThread::FreeStack -> BaseHeap::Release` on a poisoned guest-heap mutex. | The guest-free synthetic probe closes its report, emits shutdown, flushes, and hard-exits; the final 10/10 probes and independent 10/10 normal `WM_CLOSE` cycles exit 0 with no cleanup/orphans. A sanitized upstream issue draft is pending explicit publication approval. | SDK/upstream |
| KI-011 | S1 | Closed | Normal launches now select the staged Xenos plugin by project default; the verified route initializes graphics interrupts/pipelines and audio without a no-GPU marker or post-launch Bink blocker. | Fixed and bounded in `docs/evidence/M3-013-startup-traps.md`; guest-free probes intentionally remain GPU-independent. | M3-013 |
| KI-012 | S2 | Closed | Non-Release ReXGlue host tests could call a profiled `REX_HOOK` before `Runtime::Setup` and dereference Tracy's null manual-lifetime profiler at address `0x2C8`. ReXGlue v0.9.0.10 makes hook/debug instrumentation conditional on `TracyIsStarted`; the focused `[offline]` suite now passes 3/3 test cases and 10/10 assertions without starting Tracy. | Fixed and source-regression-tested by `scripts/test-sdk-profiling-lifetime.ps1`; upstream issue #410 remains the public report. | SDK/upstream #410 |
| KI-013 | S2 | Open | The artifact previously described from one title frame as a minor green vehicle shadow is a reproducible vehicle-lighting defect. Three fixed-camera native gameplay captures show the right rear body highlight changing from red to excessively bright neon green and then disappearing in step with a nearby traffic signal. Console-reference gameplay near red/green signals does not show comparable body-color amplification. The defect is therefore not broad shadow corruption, but an over-strength or incorrect transfer of saturated local-light/specular color on vehicle paint. M6-004 now proves representative `RAINY` / `DAWN` wet-road and local-light reflections are present, but explicitly does not treat that category PASS as reflection parity or a fix. The gamma-render-target UNorm16 storage behavior aligns with current Xenia and accepted gameplay uses table rather than PWL output gamma; HDR light accumulation, packed-float/blend, ownership transfer, and resolve remain candidates. | Preserve the fixed-camera repro; first A/B the same scene on host-RTV versus ROV and capture the lighting-pass formats/blend state before changing gamma or material behavior. | Graphics follow-up |
| KI-014 | S2 | Contained | ReXGlue has no XMP decoder or system music library. The reached title route only polls status, while autonomous frontend audio uses the separately verified XMA/XAudio/SDL path. | The v0.9.0.15 behavior is retained in v0.10.0.1: unsupported XMP playback/output capture fails explicitly and remains `Idle`; do not claim user-selected music playback until a decoder-backed route is implemented and tested. | M6-007 |
| KI-015 | S2 | Open | Some traffic-signal housings and foliage render with a visible stippled/partially transparent coverage pattern instead of a stable opaque or alpha-tested silhouette. The pattern is visible in the fixed-camera KI-013 screenshots, and the owner reports that affected signals, trees, shadowed/translucent objects, and intermittently vehicle surfaces shimmer or “noise” in motion. M6-004 preserves the original fixed-camera hashes and proves broader Rainy/Dawn environment effects remain usable, but neither it nor the later M6-014 owner observation samples the defect at the cadence needed to claim temporal stability or one shared root cause. Source comparison confirms the Xenos 2x2 dithered alpha-to-mask implementation matches current Xenia and fuzzy alpha epsilon remains default-off; alpha-to-mask/MSAA mapping, shadow/compositing coverage, texture LOD, or resolve behavior remain candidates. | Preserve the fixed-camera captures; add a high-cadence ROI burst covering one opaque vehicle, one shadowed object, and one alpha-tested signal/foliage draw; compare host-RTV/ROV and record reached alpha-test/alpha-to-mask, guest MSAA, shadow/compositing, texture LOD, and resolve state before changing thresholds or sample masks. | Graphics follow-up |
| KI-016 | S2 | Open | During the accepted M5-013 five-race session, the owner observed intermittent minimap flicker. M6-004 rebinds that five-race result but captures only six sparse environment frames; absence of a flicker there is explicitly not evidence of a fix. The existing checkpoint images/logs prove the HUD remains usable without renderer/device failure but do not identify whether the transient comes from HUD render-target reuse, texture upload, compositing, or resolve ordering. | Capture a high-cadence private HUD ROI plus render-target/resolve telemetry during a reproducible flicker before changing texture or compositing behavior. | Graphics follow-up |
| KI-017 | S2 | Open | During M6-007 device-recovery confirmation, the owner reported that music played briefly and then stopped while ambient/background SFX remained audible. Machine telemetry continued to show nonzero XMA/XAudio/SDL output with no starvation or submission failure, so this is not evidence of total audio-route loss; current counters cannot distinguish track lifetime, music mix state, and ambient/SFX content. | Preserve the long-session audio PASS, but do not claim music continuity or exact mix. Add privacy-safe stream-class lifetime/mix telemetry and compare the same save/route with console reference before changing decoder or mixer behavior. | Audio follow-up |
| KI-018 | S3 | Policy | Rockstar lists MCLA Rate My Ride and Driving Test unlocks as retired/unavailable. Historical documentation ties Audi R8 garage access to completing all twelve Social Club tasks, so a save without that authentic entitlement cannot obtain it through the retired service. M6-009 classifies this as a lost service entitlement, not a local gameplay compatibility defect. | Canonical builds preserve retail progression and existing authentic entitlements without granting the R8. A future separately named, explicit default-off non-persistent cheat may offer convenience access, but it must remain excluded from canonical progression/achievement evidence; none is implemented today. | Optional post-alpha cheat surface |
| KI-019 | S2 | Open | Enabled underbody neon can expose the projected primitive itself as a large translucent diamond/quad beneath the vehicle instead of producing only a soft bounded glow. A 2026-08-27 garage customization capture shows a hard geometric footprint extending around the car while `NEON` is `ON`; the owner reports the artifact is intermittent in normal play. This is not accepted neon art direction and is distinct from KI-013 vehicle-light amplification. Projection UVs/clip geometry, depth intersection, blend factors, texture border sampling, and resolve/compositing remain candidates. | Preserve the fixed garage capture and reproduce with the same car/camera while toggling neon off/on and cycling color. Capture the neon draw's shader, texture addressing, blend/depth state, target format, and resolve path before changing geometry or falloff. | Graphics follow-up |
| KI-020 | S2 | Open | White vehicle paint can become emissive-looking and clip to a broad near-solid white silhouette under ordinary daylight/local lighting, erasing expected body shading and material detail. A 2026-08-27 free-roam capture shows the rear roof, pillars, deck, bumper, and body panels glowing far beyond the surrounding scene. This may share HDR/specular, accumulation, resolve, gamma, or tone-map stages with KI-013, but the neutral-white overexposure is tracked separately until a controlled comparison proves a common cause. | Preserve the free-roam capture; compare the same white vehicle, camera, time, and exposure against a console/Xenia reference and a neutral gray paint, then record pre/post-resolve HDR ranges, material/specular inputs, output gamma, and tone-map state. Do not “fix” it with a global brightness clamp. | Graphics follow-up |
| KI-021 | S2 | Closed | Photo Mode previously created a structurally valid but globally black 1280x720 JPEG because render-to-texture CPU readback defaulted to disabled. ReXGlue v0.10.0.1 exposes the required post-plugin hook and MCLA-R selects delayed `fast` readback unless the operator explicitly overrides it. Accepted 10-minute regression `regression-20260831-125115-bf608f12` produced a new owner-visible non-black album preview and a machine-decoded JPEG with 157 sampled color bins and luma 0..237; album SHA-256 is `466AD5206195CB43DC6F90185FFFA9BF0D6E7AE64B2CF00D3590CAA68979B1C9`. | Keep the v0.10.0.1 pin and validate future Photo Mode regressions with `scripts/verify-photo-mode-readback.ps1`. | M6-014 |
| KI-022 | S2 | Open | Wheel centering force can still disappear during play. The failed 1h42 M6-014 run logged 849 latest-active transitions among the T300 and two Steam Input mirrors. ReXGlue v0.10.0.1 suppresses mirrored pad edges, requires deliberate analog takeover, and permits small inactive-wheel reclaim; the 10-minute physical regression reduced changes to seven deliberate wheel/pad transitions, proving that routing fix works, but centering nevertheless disappeared while the wheel remained active. The remaining failure is therefore an unresolved FFB effect/hardware lifecycle issue rather than only virtual-endpoint churn. | A deliberate move to gamepad and back to the wheel may restore force; otherwise restart the title. No further physical T300 fix is claimed until hardware is available again. | M6-014 / SDK |
| KI-023 | S1 | Contained | The 2026-08-30 M6-014 free-roam process ended at about 1h42 with D3D12 `DXGI_ERROR_DEVICE_REMOVED` (`0x887A0005`). Windows recorded contemporaneous `nvlddmkm` event 153 errors, while Sunshine 2026.808.1642.19 repeatedly faulted with `0xc0000005` before and after removal. This is classified as a host NVIDIA/streaming failure on the reference machine, not a normal guest crash or save loss; the latest complete owner profile was archived. | Stabilize or stop the failing Sunshine route before canonical soak. The harness now writes `host-display-audit.json`, checks new NVIDIA and Sunshine application errors every minute and on process exit, and aborts the evidence run instead of misclassifying it as title health. | Host / M6-014 |
| KI-024 | S1 | Open | The 2026-09-01 successor free-roam process crashed when a delivery mission started because indirect guest target `0x8220DA40` was executable but absent from the generated dispatcher. Ghidra proves an exact four-instruction thunk and the title's constructor registry explicitly registers it. A full registry comparison found two additional omitted callbacks (`0x8220B7D0`, `0x8220DAA0`). All three minimal entries now pass non-force codegen and all 102 analyzed constructor targets are registered, but the delivery transition has not yet been physically replayed. The 37-race/31-win save was archived 39 seconds before the crash. | Use the archived progressed save and rerun the same delivery transition on the rebuilt artifact. Do not credit the failed soak or close this issue until the transition continues without another invalid-function target. | M6-014 |

Do not remove an issue without linking verification evidence. Update this document when a new recurring defect is discovered, severity changes, a workaround changes, or a release claim is added.
